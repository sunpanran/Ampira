# Ampira MV3 QA

本文件只描述当前 Manifest V3 Chrome 扩展的验证状态。已移除的 Node 版实现不属于当前运行架构；旧 `dashboard-cache/` 仅是用户自行管理的历史数据，本项目不会读取、迁移、删除或打包它。

## Current Architecture

- `dashboard.html` 是新标签页单页入口；浏览器代码使用本地 ESM，不加载远程脚本或执行远程代码。
- `extension/service-worker.mjs` 注册 Chrome 事件和消息入口；设置归一化、有界网络读取、配额和客户端状态写入位于 `extension/core/` 的可测试模块。
- 必需权限保持为 `bookmarks`、`storage`、`alarms`；`favicon` 与网站来源访问均为可选权限，只通过用户手势申请。
- 部分非凭据设置位于 Chrome Sync；本机凭据位于 `chrome.storage.local`，Feed、Reader、摘要和阅读状态位于 IndexedDB。
- UI 支持简体中文、繁体中文和英文；远程文本仅通过结构化 DOM 与 `textContent` 渲染。

## Automated Verification

每次功能修改必须运行：

```powershell
.\scripts\verify-extension.ps1
```

当前测试覆盖：

- Manifest 必需/可选权限、原生 favicon URL 编码与回退、CSP、三语键一致性和远程代码禁令。
- 根页面资源与 `docs/` 全部 HTML 子页的本地资源和链接完整性。
- 书签角色去重、嵌套分类上限、来源排除和精确域名生成。
- RSS、Atom、JSON Feed、XML 实体 URL、来源安全上限、网络失败和去重排序。
- 设置白名单与边界归一化、本机密钥隔离、缓存裁剪和旧凭据清理。
- 大型设置的 Chrome Sync 分块与并发写入串行化、客户端状态串行合并与批量发送、非法状态载荷拒绝、并发 AI 配额预留。
- Reader 主体提取、安全链接、图片/视频结构、缓存回退、HTTP 分类、超时语义及无长度响应的流式大小限制。
- Brave 图片预览的权限、大小限制、安全搜索、低价值图片过滤、并发请求去重和缓存降级路径。
- 刷新启动锁、强制刷新排队、缓存清理代际失效，以及失败来源保留最后成功内容、有效空来源清理旧内容。
- 设置草稿差异、取消恢复、未知字段隔离、网站权限批量操作状态，以及优先级、去重、未读筛选和分页等前端纯选择器。

## Browser Verification

UI 或交互修改后，在安全本地预览或加载已解压扩展中验证：

- 视口：`1280×800`、`1440×1000`、`390×844`。
- 文档、设置弹窗、引导页和 Reader 横向溢出均为 `0`。
- 控制台无错误；刷新、搜索、换批、右键菜单、稍后读、已读和跨重载持久化正常。
- 权限只能由用户操作触发；拒绝权限后页面保持可用，非本地 HTTP 不进入 Reader。网站权限汇总在设置页可见，全部授权后不再显示批量按钮，Chrome 权限增删与页面重新聚焦均会自动同步。
- 深色、浅色、系统模式及三种语言均保持现有信息终端布局和断点。
- 有意保留的截图写入 `output/playwright/`。

## Release Verification

```powershell
.\scripts\verify-extension.ps1 -Package
```

打包脚本从 `manifest.json` 读取版本，使用允许列表复制文件，并在压缩前拒绝以下内容：

- `dashboard-cache/`、测试、输出、隐藏配置或非允许列表文件。
- 疑似 API 密钥、本机绝对路径和远程脚本。
- 非 Manifest V3 清单或与显式版本参数不一致的版本。

发布脚本要求 PowerShell 7 与 Node.js 20+。打包模式会拒绝未替换占位和未配置或不匹配的 `REQUIRED_SUPPORT_URL`。它在覆盖已有版本化 ZIP 前完成环境和输入检查，按稳定路径顺序与固定时间戳创建 ZIP，并同时生成 `.zip.sha256` 与包含逐文件 SHA-256、可选 `GITHUB_SHA` 的 `.manifest.json`。Windows CI 固定使用 Node.js 20 和 `pwsh` 运行验证/打包并上传这三项产物；没有真实支持 URL 时按设计阻止发布。

上传前仍需人工确认 ZIP 根目录包含 `manifest.json`，隐私/支持页面已发布，商店文案、权限说明和实际行为一致。

## 2026-07-11 Quality Pass

- 修复嵌套文件夹突破分类上限、资讯/灵感复用同一文件夹、单源 `0` 退回 5、Feed URL 实体未解码和 URL 端口丢失。
- Feed、Reader、AI 和图片搜索统一使用限时、限流读取；来源失败与空来源分开记录。
- 设置响应只包含已知字段；反馈和客户端状态写入增加大小、类型与动作校验。
- AI 配额改为串行预留，失败调用释放同日预留；客户端状态改为 50ms 合并写入和退避重试。
- 将设置存储、网络、配额、客户端状态、AI 搜索控制器、Reader 控制器和纯分页/随机逻辑拆为独立模块，并移除未使用导出。
- 来源刷新超过 80 个时使用持久化游标轮转；删除或屏蔽来源会清理旧缓存，未轮到的有效来源保留上次结果。
- Reader 在读取重定向正文前复核最终来源权限；AI 与 Brave POST/请求不跟随重定向。
- Brave 图片兜底从占位路由改为真实的有界搜索、筛选、负缓存与客户端失败隔离。

## 2026-07-11 Quality Pass 2

- 刷新协调器在首次异步读取状态前同步占位，消除双刷新窗口；清缓存同时失效旧代际并重置来源游标。
- 来源刷新失败不再删除上一版成功文章，空来源不再计为失败；IndexedDB 分类清理和裁剪改为单事务完成。
- Reader 缓存与 AI URL 搜索缓存按当前精确来源权限复核；权限撤销错误不再被旧 Reader 缓存掩盖。
- Chrome Sync 设置公开写入统一串行，事务回调使用内部写入避免自锁；预览搜索对同一键复用在途请求，缓存写失败不影响有效结果。
- 设置弹窗只提交相对打开快照的变更，取消会恢复独立副本并清除密钥草稿；异步设置动作使用会话代际防止旧响应串入新弹窗。
- 今日内容选择器改为先过滤已看再截断，修复第 34 条以后内容无法补位以及优先级 `0` 被当成缺失；同 key 且内容未变的卡片会复用 DOM。
- Feed 缓存缺失或为空时保持真正的空状态，不再把资讯书签名称合成为资讯卡片；自动化测试覆盖缺失、空和有效缓存三种情况。
- HTML 来源页只有在明确标记为文章时才允许成为资讯；普通网站首页无 Feed、无文章链接时返回空结果，并补充 `/detail/` 新闻链接识别。
- 验证脚本会运行全部顶层测试、递归检查本地模块与导入，并校验 Manifest、本地化、HTML 资源和发布包绝对路径。
- 合成数据浏览器回归通过 `1280×800`、`1440×1000`、`390×844`：文档与设置弹窗无横向溢出，设置取消、键盘事件隔离和 keyed DOM 复用正常，控制台零错误。

## 2026-07-11 Release Hardening

- OpenAI 密钥、服务地址、调用方式、模型与凭据代际改为同一条本机原子 Provider 记录；旧本机密钥安全迁移，Brave 密钥继续独立保存在扩展本地。
- 书签同意、首次引导和 AI 数据传输同意改为设备级本机状态；同意必须匹配当前版本，AI 同意还必须匹配当前 Provider origin。
- Provider origin 改变时界面会撤销旧勾选并要求重新确认；测试连接、正常 AI 调用和缓存身份均使用当前本机 Provider 代际。
- Feed 与 Reader 禁止自动重定向；Feed 显式省略凭据和 Referer。权限列表会区分当前需要、宽泛覆盖与遗留 origin，并检查权限 API 的真实布尔结果。缓存提交已串行化且带失效代际；撤权会按 Feed、AI、Brave、Reader 能力定向清理，Feed、摘要、问答与预览还会在读取时按当前权限 fail-closed 复核。
- 三语引导、隐私政策、商店隐私字段和审核说明补齐公共 Feed、Chrome Sync、AI URL/API key、远程图片请求与 Limited Use 披露。
- 发布版本提升至 `26.1.5`；PowerShell 7/Node.js 20 发布门禁、确定性 ZIP、SHA-256/逐文件清单侧车和 Windows CI 已建立。真实支持入口固定为 `https://github.com/sunpanran/Ampira/issues`，当前版本发布包仍需通过同值环境变量与完整发布门禁生成。

## 2026-07-11 Heading Rule Cleanup

- 移除 `DASHBOARD`、`SIGNAL FEED`、`ARCHIVE INDEX` 上方会在英文模式重复含义的本地化文案；保留的装饰横线加长并与标题左缘对齐，首次引导步骤文案不受影响。
- 英文模式浏览器回归通过 `1280×800`、`1440×1000`、`390×844`：三处横线宽度分别为 128px、144px、96px，文案为空，文档横向溢出为 0，控制台零错误。
- 有意保留的截图位于 `output/playwright/heading-rules-en-*.png`。其余顶层测试与全部浏览器模块语法检查通过；`favicon` 现作为可选权限单独断言，必需权限仍保持 `bookmarks`、`storage`、`alarms`。

## 2026-07-11 Permission UX Pass

- 将新标签页接管与网站访问权限拆成两个明确区域，说明精确域名权限可能服务于资讯来源、AI 服务与图片搜索。
- 移除无可见反馈的手动权限刷新按钮；权限事件与页面重新聚焦会自动重新读取 Chrome 权限。
- 批量授权按钮只在存在未授权必需域名时出现，并显示待授权数量；全部授权、无需授权、处理中、拒绝与失败状态均在设置页内联显示。
- 增加权限 UI 纯状态模型与回归断言，覆盖部分授权、全部授权和仅有遗留权限的批量操作状态。
- 合成权限状态浏览器回归通过 `1280×800`、`1440×1000`、`390×844`：部分授权时显示精确待授权数量，全部授权后批量按钮隐藏，三个视口的文档与设置弹窗横向溢出均为 `0`，控制台零页面错误；截图位于 `output/playwright/permission-settings-*.png`。

## 2026-07-11 AI Setup Gate

- AI 设置改为分阶段开放：服务地址可先编辑；确认数据传输说明后才能申请当前精确域名；授权成功后才开放密钥、调用方式、模型、额度和连接测试。
- Provider origin 改变、授权被拒绝或权限被撤销时，AI 表单立即重新锁定；旧 origin 的异步权限结果不能解锁当前草稿。
- Brave 图片搜索授权保持独立，不受 AI 正文传输同意控制；未授权 AI origin 时仍可移除已保存密钥和保存其他设置。
- 合成本地权限回归覆盖同意前、待授权、已授权和切换 Provider 后重新锁定；`1280×800`、`1440×1000`、`390×844` 均无横向溢出，三种语言的阶段提示完成抽查。截图位于 `output/playwright/ai-setup-*.png`。
- 发布前仍需在隔离的解压扩展中复核真实 Chrome 权限弹窗、拒绝、外部撤权与页面重新聚焦；这些路径同时由前端状态、权限事件和后台保存校验保持 fail-closed。

## 2026-07-11 Bookmark Favicons

- 书签、Feed 与 Reader 共用 Chrome 原生 `/_favicon/` 地址；页面 URL 通过 `URLSearchParams` 编码，固定请求 32px 图标，失败或非 HTTP(S) URL 回退到打包的 `favicon.svg`。
- `favicon` 声明为可选权限，首次引导与“设置 → 浏览器”均提供明确用户手势；授权变化立即重绘，用户可随时撤销，不会因扩展升级新增警告权限而停用已有安装。
- 不申请 `tabs` 或 `history`，不使用第三方 favicon 服务，不扩大 CSP 或主机权限。
- 隔离 Chromium 回归覆盖未授权本地图标回退、预置授权后的真实 32px `/_favicon/` 渲染、非网页 URL 回退，以及 `1280×800`、`1440×1000`、`390×844` 三种视口；无横向溢出或控制台错误，截图位于 `output/playwright/favicon-*.png`。发布前仍需人工确认原生授权弹窗、拒绝与撤销流程。

## 2026-07-11 Original Inspiration Images

- 今日灵感改为原站优先：用户授权对应灵感站点的精确 origin 后，后台只读取页面 HTML 前 1MiB 的惰性元数据，依次检查 Open Graph、Twitter Image、`image_src`、JSON-LD、图片预加载、响应式 `picture/srcset` 与语义正文主图；不执行页面脚本、不携带凭据或 Referer，并继续禁止自动重定向。
- 原站图路径与 Brave 配置完全解耦；仅在原站未授权、无可用主图、读取失败，或浏览器实际加载原图失败时才进入 Brave-only 兜底。没有 Brave 密钥或关闭兜底时，已授权原站图仍正常工作。
- 原站与 Brave 使用独立缓存身份和权限复核；原站身份提升到 v3 以主动绕过旧版解析器留下的无图缓存，原站命中保留 24 小时、无图结果仅保留 2 小时，未改变的 Brave v2 正负命中继续保留 24 小时以避免不必要地消耗搜索额度。原站缓存按书签 URL 与站点权限清理，Brave 缓存按 URL、标题、开关、密钥和 API origin 清理，任一权限变化都会失效客户端空结果；书签被删除、改名、移出灵感文件夹或切换灵感文件夹时会立即清除不再匹配的预览记录，Brave 设置或密钥变化也会单独清除 Brave 记录而保留原站记录。
- 权限页、首次引导、三语设置文案、隐私页、商店隐私实践、审核说明和提交清单均补充灵感原站元数据用途及 Brave 仅兜底说明。
- 自动化覆盖元数据属性顺序、JSON-LD、预加载与响应式图片、正文懒加载主图、1MiB 有界扫描、相对 URL、危险协议与私网字面地址、原站命中时零 Brave 请求、无权限/无图/网络失败、正负缓存、过期重试、并发去重、缓存写失败、非灵感 URL 拒绝，以及原图 DOM 加载失败后的 Brave-only 重试。完整 `verify-extension.ps1` 通过。

## 2026-07-12 Original Image Coverage

- 原站解析覆盖从声明式社交元数据扩展到 JSON-LD `ImageObject`、`link[rel=preload][as=image]`、`picture/source/srcset` 和常见懒加载属性；语义评分优先正文、主图、高优先级和大尺寸候选，并排除导航、页脚、头像、Logo、像素图与占位图。
- HTML 前缀上限由 512KiB 提升到 1MiB、超时由 8 秒调整为 10 秒，仍保持有界读取、无凭据、无 Referer、禁止自动重定向和精确 origin 权限复核。
- 原站图片预览缓存身份提升到 v3，确保升级后不被旧版 24 小时无图缓存挡住；原站无图缓存缩短为 2 小时，原站命中时仍不会读取 Brave 设置或发出 Brave 请求，Brave v2 缓存则继续复用以保护搜索额度。
- 隔离临时 Chromium 配置使用合成资讯与灵感书签完成 `1280×800`、`1440×1000`、`390×844` 回归：权限列表包含灵感精确 origin，首次引导与权限说明正确，文档和设置弹窗横向溢出均为 `0`，控制台零错误；截图位于 `output/playwright/original-preview-*.png`。
- 发布前仍需在可操作浏览器外壳中人工接受一次灵感站点的 Chrome 原生可选权限确认，并复核真实站点的原图、防盗链失败与 Brave 兜底；原生浏览器确认气泡不属于页面 DOM，当前无头回归只验证到申请列表和用户手势入口。

## 2026-07-12 Five-step Onboarding

- 首次引导重排为 5 步：产品介绍、权限说明与可选授权、书签文件夹选择、可选 API Key、开始使用；文件夹和 API Key 均可跳过，结束页会汇总当前状态。
- 权限页明确区分安装时已授予的 `bookmarks`、`storage`、`alarms` 基础权限与可跳过的精确网站来源、`favicon` 权限；拒绝可选权限不会阻止打开书签，只暂停对应的在线读取或图标能力。
- 书签文件夹直接通过现有设置保存边界持久化，并阻止资讯与灵感选择同一文件夹；API Key 继续要求 AI 数据传输同意、当前 Provider 精确 origin 用户手势授权与后台 fail-closed 校验，密钥不回显、不进入 Sync。
- 自动化增加 5 步结构、两个可跳过步骤、文件夹与 API Key 保存边界断言。隔离合成状态浏览器回归逐步覆盖 `1280×800`、`1440×1000`、`390×844`，文档和弹窗横向溢出均为 `0`，控制台零页面错误；截图位于 `output/playwright/onboarding-v2-*.png`。
- 发布前仍需在加载的未打包扩展中人工确认来源、`favicon` 与 AI Provider 的 Chrome 原生授权气泡，以及拒绝后停留、跳过和最终首次刷新行为。
