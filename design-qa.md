# Ampira MV3 QA

本文件只保留当前 Manifest V3 扩展的 QA 基线和最近一次仍有效的界面结论，不作为逐次改动日志。历史实现与发布记录应通过 Git 历史查询。

已移除的 Node 版不属于当前架构。旧 `dashboard-cache/` 仅包含用户自行管理的历史数据；验证、迁移和打包均不得读取、删除或发布它。

## Current Architecture

- `dashboard.html` 是新标签页单页入口；`assets/client/` 使用本地 ESM 组装控制器、视图、状态和消息客户端，不加载或执行远程代码。
- `extension/service-worker.mjs` 只负责 Chrome 事件注册和运行时组装；Chrome 工作流位于 `extension/runtime/`，确定性领域逻辑与适配器位于 `extension/core/`。
- `assets/dashboard.css` 按固定顺序导入 `assets/styles/`；界面保持高密度信息终端风格、单一网格纹理、桌面浮动导航、移动底部导航，以及 `1120px`、`820px`、`520px` 断点。
- 必需权限为 `activeTab`、`alarms`、`bookmarks`、`storage`。`activeTab` 仅用于用户点击工具栏后的当前页稍后读捕获；书签只读。`favicon`、`search` 和精确网站来源均为用户手势触发的可选权限。
- 非凭据设置可进入 Chrome Sync；API Key 只存于 `chrome.storage.local`。Feed、Reader、摘要、阅读状态及其他缓存通过现有 IndexedDB/存储抽象管理。
- UI 同步支持简体中文、繁体中文和英文。远程文本保持惰性，只能通过结构化 DOM 和 `textContent` 渲染。
- Microsoft Edge 使用浏览器中立文案和扩展管理入口；不请求 Edge 不支持的 Chrome 原生 `favicon` 权限，并回退到随包图标。

## Current UI Baseline

- Dashboard、首次引导和设置使用克制的玻璃表面、细边线与现有主题令牌；不得恢复装饰性卡片堆叠、宽幅渐变或营销式大标题。
- 首次引导保留产品说明、来源选择、网站授权和 AI 交接四步。桌面面板目标宽度为 `600px`；窄屏能力标签使用单列，主操作获得初始焦点。
- 设置保留 AI、资讯、书签、屏蔽、外观、浏览器和关于七类。桌面弹窗目标尺寸为 `1040px × 84dvh`；窄屏使用可横向滚动的分类导航，并恢复各分类滚动位置。AI 顶部的额度、缓存和自动整理状态必须常驻：桌面使用中性磨砂底、带列分隔线的三列状态带，不使用紫色装饰渐变；`820px` 以下纵向排列，不使用折叠容器或独立状态卡片。
- 首页视觉减重仅作用于 `#daily`；设置弹窗和其他页面不得继承首页的透明承载层规则。快捷入口继续支持滚动、拖动、渐隐、空状态和键盘焦点。
- “显示高度”和“模糊强度”常驻为“标题 → 滑动条 → 当前值”布局；禁用时保留可见的原生 `disabled` 状态，拖动预览不得改变轨道宽度。
- 深色、浅色和跟随系统主题均须可用；`prefers-reduced-motion` 下不得保留必要性之外的循环或入场动画；强制色模式及不支持 `backdrop-filter` 时必须回退到可读实色表面。
- 浮动选择器、菜单和弹出列表不得使用 `scrollbar-gutter: stable`，短列表左右内边距应保持对称。

## Automated Verification

每次代码或内容修改运行：

```powershell
.\scripts\verify-extension.ps1
```

验证至少应覆盖：

- `node tests/extension.mjs` 统一测试入口，以及全部扩展 ESM 的语法检查。
- Manifest 权限、CSP、静态本地资源、非字面量动态导入、远程代码和不安全 HTML sink 禁令。
- 三种客户端语言与三套 Chrome Manifest locale 的键和值完整性。
- 消息信封、请求关联、过期响应保护、权限代际、刷新代际与并发写入顺序。
- 设置归一化、Chrome Sync 边界、本机凭据隔离、IndexedDB 缓存配额、裁剪和失效策略。
- Feed、Reader、图片、网络地址与权限策略的安全边界和失败回退。
- `extension/service-worker.mjs` 的 composition-root 约束、core/runtime 依赖方向和本地 CSS import 完整性。
- 生产模块入口可达性、已删除迁移/版本兼容标识符，以及 CSS 选择器的静态引用卫生；动态类名须在测试白名单中显式登记。
- 文档、商店材料、支持 URL、打包允许列表及敏感内容检查。

对策略、存储、权限、解析器、运行时或 UI 模型的行为改动，应在 `tests/suites/` 增加聚焦覆盖并从 `tests/extension.mjs` 注册。

## Browser Verification

UI 或交互修改后，使用安全合成数据的本地预览或加载已解压扩展验证：

- 深色 `1280×800`、浅色 `1440×1000` 和窄屏 `390×844`；必要时补充相反主题和系统主题。
- 页面、设置、引导、Reader、菜单和弹窗横向溢出均为 `0`，控制台无错误。
- 键盘焦点、Escape/背景关闭、关闭后焦点恢复、滚动位置恢复、加载/空/错误状态均正常。
- 减少动态效果下无持续动画；强制色模式下文字、边界、选中态和焦点仍可辨识。
- 权限只能从用户手势发起；拒绝或外部撤权后界面保持可用并立即反映状态。
- 新标签页安装状态必须如实说明，不得显示可写的伪开关；内部搜索必须明确标为 Ampira 内容搜索。
- 不得使用真实 API Key、私人书签、Chrome Profile 或运行时缓存进行测试或截图。
- 仅将有意保留且隐私安全的截图写入 `output/playwright/`；未实际执行加载已解压扩展的手动 QA 时必须明确说明。

安全合成预览记录只能作为当次变更证据，不构成持续有效的手动浏览器 QA；是否完成真实 Chrome/Edge 验证必须在发布检查中重新确认。

2026-07-17 的 AI 运行状态带使用安全合成数据覆盖深色 `1280×800`、浅色 `1440×1000` 和浅色 `390×844`：桌面三列、窄屏三行，分隔线方向正确，三个状态值无截断，页面与设置横向溢出均为 `0`，控制台零错误与警告。截图位于 `output/playwright/runtime-status-band/`；未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-19 — 摘要卡片操作悬停统一

- “手动整理摘要”移除独有的悬停底色与图标放大，和稍后读、已读按钮统一为仅改变前景色与图标滤镜；点击回弹、禁用状态、键盘焦点和减少动态效果行为保持不变。
- 架构测试固定三枚按钮共享同一悬停规则，并防止重新引入仅供手动整理使用的背景或缩放动画。
- 隔离 Playwright Chromium 加载当前工作区扩展并使用安全合成资讯检查深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844`：三枚按钮悬停背景均为透明，图标均保持 `16×16`，按钮与图标无 transform，悬停动画名为 `none`；三种视口横向溢出均为 `0`，控制台错误与警告均为 `0`。本次未保存截图，也未使用真实书签、密钥、Chrome Profile 或运行时缓存；未执行人工 Chrome/Edge QA。

## 2026-07-19 — 商店文案与品牌广告语

- 首屏定位改为“从书签里的网站获取新资讯，再用 AI 整理和解读。”，繁体中文与英文按各自语言习惯改写。Manifest、Chrome Web Store 与 Microsoft Edge Add-ons 的三语短描述完全一致，长度均不超过 132 个字符。
- 六份商店正文保留收藏后很少再打开、关注来源分散这一痛点，并加入长期跟踪行业或研究主题的现有用法。主要功能按书签资讯、排序、AI、每日灵感、阅读流程和辅助工具排列；数据与权限部分继续说明书签只读、本地保存与同步边界、API Key 隔离和精确来源授权。
- 文案没有写入 Pro、价格、订阅、路线图或尚未上线的功能。`humanizer-zh` 复核后，按直接性、节奏、可信度、真实感和精炼度五项各 10 分评分：简体中文 48/50，繁体中文 47/50，英文 47/50。

## 2026-07-19 — 文案规范

- 新增 `copy-guidelines.md`，集中记录核心定位、锁定文案、功能顺序、专业场景边界、权限事实、三语术语、各页面结构和变更检查。文档用于约束消费者文案，不替代隐私实践、权限说明和审核材料。
- 一致性测试确认规范包含三语广告语、商店摘要、痛点开场、专业场景、AI 设置与首次设置标题，以及当前 Chrome Web Store 地址。修改这些锁定内容时，必须同时更新实际页面和规范。
- 规范按 `humanizer-zh` 检查直接性、节奏、可信度、真实感和精炼度，评分为 47/50。本次只新增文档与断言，没有界面或运行逻辑变化，因此未执行手动 Chrome/Edge QA。
- 三语 Manifest 简介改用品牌广告语，使浏览器“扩展程序”管理页直接显示产品核心定位。Chrome、Edge 商店短描述和官网 Meta 描述继续使用包含排序、AI 与本地保存信息的功能摘要。
- 三语官网首页、设置“关于”以及首次设置第 1 步和第 4 步均通过本机只读预览，在 `1440×1000` 与 `390×844` 下检查。页面与目标容器没有横向溢出或截断；英文“关于”广告语在窄屏自然换为两行。隔离的布局预览没有控制台错误或警告。本次没有保存截图，也未加载已解压扩展执行手动 Chrome/Edge QA。

## 2026-07-19 — 官网品牌 Logo

- 三语官网首页在 AMPIRA 字标前加入现有产品 Logo，固定显示为 `48×48`。GitHub Pages 使用的 SVG 位于 `docs/assets/`，一致性测试会核对它与扩展内图标完全相同。
- 本机只读预览覆盖深色 `1280×800`、深浅色 `1440×1000` 与 `390×844`。三语 Logo 均以 `104×104` 原始尺寸正常载入，显示尺寸、字标和语言导航没有挤压或截断，页面横向溢出为 `0`，控制台没有错误或警告。本次没有保存截图。

## 2026-07-19 — 官网与 GitHub 商店入口

- 三语官网首页在痛点说明后加入 Chrome Web Store 安装按钮；GitHub README 的三语顶部链接行同步加入同一商店地址。按钮使用直接的本地化动作名称，并以新标签页安全打开。
- 安装按钮最小高度为 `44px`，白字与按钮背景对比度约为 `4.9:1`，并提供 `:focus-visible` 焦点轮廓。本机只读预览覆盖深色 `1280×800`、深浅色 `1440×1000`、`390×844`，并补测 `320×800`。三语页面均无横向溢出；繁中与英文按钮只在 `320px` 下自然换为两行。控制台没有错误或警告，本次没有保存截图。

## 2026-07-19 — 首页滚动导航无视觉性能收敛

- 首页导航活动态由逐帧 `scroll` 回调改为 `IntersectionObserver` 的栏目边界观察；栏目激活线、隐藏书签栏目过滤、平滑定位及设置/搜索关闭后的活动态恢复保持不变。
- 导航按钮与栏目节点在控制器内缓存，只有活动入口实际变化时才写入 `active` 类；设置和 AI 搜索入口复用同一更新路径，避免状态缓存与直接 DOM 写入分叉。
- 固定或全屏封面的实时模糊没有改为预生成位图：远程图片的 Canvas 跨域限制和可调模糊半径无法保证像素与实时预览严格一致。本次未修改任何首页 CSS、颜色、透明度、模糊强度、间距或动画。
- 自动验证覆盖观察器、无逐帧滚动监听、隐藏栏目选择和活动态写入去重。安全本地 Chromium `1440×1000` 预览确认滚轮栏目切换、AI 搜索/设置活动态及关闭后恢复正常；控制台仅出现静态预览预期的 `/api/*` 404，没有新增运行时错误。本次未在 macOS 真机或加载已解压扩展后执行手动 Chrome/Edge QA。

## 2026-07-19 — 首页玻璃模糊开关

- “外观 → 交互效果”新增“首页玻璃模糊”开关，默认开启，现有用户与新安装的默认视觉保持不变；非凭据设置继续通过既有 Chrome Sync 设置链路保存，并支持设置导入导出。
- 关闭时只移除 `.main` 首页内容区元素及伪元素的 `backdrop-filter`；侧栏、设置、确认弹窗、搜索、Reader 和头图自身的图片模糊不受影响。透明背景、颜色、边线、阴影、间距与动效保持原样。
- 本地首帧提示会在样式加载前恢复开关状态，避免已关闭用户刷新时先短暂出现玻璃模糊。简体中文、繁体中文和英文文案明确说明性能作用范围。
- 安全本地 Chromium 计算样式确认首页效率卡从开启时的 `blur(18px)` 切换为关闭时的 `none`；同一时刻侧栏仍为 `blur(14px) saturate(1.06)`、设置遮罩仍为 `blur(14px) saturate(1.1)`，头图 `filter` 数值不变。
- 浅色 `1440×1000`、深色 `1280×800` 和深色 `390×844` 的页面与设置横向溢出均为 `0`，窄屏开关完整位于设置面板内。控制台仅有静态预览缺少 `/api/settings`、`/api/settings/header-cover` 和 `/api/dashboard` 的预期 404；本次未在 macOS 真机或加载已解压扩展后执行手动 Chrome/Edge QA。

## 2026-07-19 — 天气多模型降水与极端天气校正

- 天气数据源继续只使用 Open-Meteo。中国地点以 CMA GRAPES Global 为主要降水与天气现象依据，同时请求 ECMWF IFS、GFS 和 ICON 交叉验证；CMA 字段缺失或包含 CMA 的整次请求失败时自动重试全球模型，不复用不同日期或不同模型批次的旧 CMA 数据。
- 降水概率改为 ECMWF 与 GFS 的逐小时均值；未来日期只统计当地 `08:00–20:00`，当天只统计当前小时之后。天气现象先在同一小时内完成跨模型验证，再汇总为日况，错开时段的模型结果不能互相凑票；小于 `0.2 mm` 的原始逐小时降水忽略，雷暴小时原始降水至少需要 `0.5 mm`，全球模型至少两个独立支持才显示降水或雷暴；CMA 雷暴也必须获得同小时全球模型支持。天气现象覆盖 CMA 时，降水量同步改用支持该现象的全球模型；被日况拒绝的低置信度孤立降雨不进入累计，单模型确实主导为雨时仍保留其降水证据，避免天气现象与降水量来源分叉。
- 天气缓存使用尚未发布的日期感知 v3 记录，地点、国家代码、本地日期或模型批次不一致时不复用。界面优先显示平均概率、峰值、预计降水量和有雨小时数；CMA 同意最终现象且至少两个全球模型支持，或三个全球模型一致时才标记高置信度。简体中文、繁体中文和英文文案同步更新。
- 安全合成天气数据在深色 `1440×1000`、浅色 `1280×800` 和深色 `390×844` 下均无横向溢出；长说明使用现有省略样式，完整内容保留在 `title` 中。本次未加载已解压扩展，也未使用真实位置缓存、书签或密钥。
- 聚焦测试覆盖 CMA 可用、字段缺失与请求级失败、同小时/错时模型支持、天气现象与降水证据同步、CMA 冲突置信度、单模型极端天气降级、原始降水阈值、当天已过小时排除、空数值拒绝、国家模型选择和跨日期缓存失效。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-07-19 — 通用图片质量与双用途预览

- 页面图片候选改为统一的带来源记录，并以 `article`、`visual` 两个 profile 分离资讯与审美卡片；结构、声明尺寸、客户端真实尺寸、缓存身份和并发请求身份均使用当前图片策略版本，不包含站点专属规则。
- 安全合成夹具覆盖页头箭头、二维码、页脚 Logo、纯文字新闻、`main` 大图、响应式 `srcset`、固定元数据图、Hero 背景、旧缓存、重复默认图、RSS 图片和 profile 隔离。客户端覆盖成功加载但真实尺寸不足时的候选轮换、会话拒绝缓存、资讯 favicon 回退及审美主视觉保留。
- 缺陷回归补充覆盖 JSON-LD 内嵌 publisher/author 图片、图片节点自身的分享类名、无图与正文图共同参与的默认图占比、Feed 补图复用抑制、已拒绝图片的缓存快路径，以及 visual 候选被会话拒绝后继续 Brave 回退；默认图统计现由 Preview 与 Feed 共用同一串行记录器。
- P1 修复后，article 的 Preview 与 Feed 缓存额外保留最多三条原始候选，缓存重读继续使用原始 metadata/main 签名统计，已抑制的站点默认图不会因空候选或正文回退候选而重新出现；缺少原始候选字段的旧 article 缓存自动失效。结构夹具同时确认 `hidden` 容器及 `aria-hidden="true"` 图片不会进入新闻候选。
- P2 修复后，Hero、Main、Cover 等正向语义只按完整 token 或合法的图片后缀匹配，`maintainer-photo` 不再因包含 `main` 子串被误判，同时保留 `hero-masthead` 等主视觉写法。文章复用身份会移除 UTM、点击 ID 和常见邮件活动参数并排序剩余查询参数，同一文章的跟踪链接变体只占一个观察位。
- 36氪二维码回归定位到正文根节点过宽：通用选择器现会在长文章壳内继续收敛到承载大部分正文与段落的更深正向容器，已有明确正文根节点时不再把外层正向壳中的辅助图片视作 `main` 回退。两篇公开文章的实时 HTML 均只保留 OG 与正文图片，辅助区静态二维码候选为零；没有加入域名或类名专属规则。图片策略缓存提升到版本 3，旧二维码候选自动失效，无需手动清缓存。
- 本地 Chromium 使用 `36×36` 合成坏图确认资讯自动轮换到 `640×360` 正文候选；只有坏图的资讯回退为随包 favicon；五张审美卡均保留 `640×360` visual Hero。预览请求同时实际出现 `article` 与 `visual`，资讯没有进入 Brave 回退。
- 深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 的页面横向溢出均为 `0`，控制台错误与警告均为 `0`。截图和安全合成服务位于 `output/playwright/image-quality-v2/`，不含真实 API Key、私人书签、Chrome Profile 或运行时缓存。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过。本次使用本地 Chromium 静态运行时完成界面验证，未加载已解压扩展，也未执行真实中新网、36Kr 网络抓取。

## 2026-07-19 — 后台启动资源 P0 收敛

- 中国县级以上地点索引从服务工作线程的静态模块图移出，改为用户提交有效天气地点搜索后才读取扩展包内 JSON；首次并发搜索共用一次读取和解析，成功后在当前服务工作线程生命周期内复用。索引响应、大小、记录数、字段和坐标均经过边界校验，读取失败时仅回退到原有、已获授权的 Open-Meteo 地理编码流程。
- 客户端继续保留完整的简体中文、繁体中文和英文词典；服务工作线程改为使用由同一词典按明确键策略生成的精简目录，locale 归一化和格式化逻辑提取为无词典依赖的共享模块。自动验证约束后台依赖图不得重新引入完整客户端词典或旧地点模块，生成词典必须与三套客户端源词典逐键一致。
- 发布白名单仅新增固定的 `extension/data/china-locations.json` 和三份生成后台词典；旧的 eager 地点模块会触发打包失败。地点数据生成器直接输出压缩 JSON，后台词典生成器支持 `--check`，完整验证会拒绝过期生成物。
- 服务工作线程静态模块图实测为 82 个模块、582,579 字节；其中后台三语词典共 55,505 字节，完整客户端词典与 508,524 字节地点 JSON 均不在启动图。架构测试设置 640 KiB 启动预算，并持续约束完整词典和旧地点模块不得重新进入后台图。
- `node tests/extension.mjs`、`.\scripts\verify-extension.ps1` 与设置真实支持地址后的 `.\scripts\verify-extension.ps1 -Package` 均通过。发布 ZIP 为 2,258,847 字节、281 个文件；根 Manifest、地点 JSON、三份后台词典、包清单与 SHA-256 边车均经复核，旧地点模块不存在，三处 ZIP 哈希一致。
- 使用全新临时配置加载 Playwright 开发版 Chromium：service worker 正常启动，打开首页前没有地点 JSON 请求；首次 `慈溪` 搜索读取一次 JSON，后续 `Cixi`、重复搜索和 `390×844` 窄屏 UI 搜索均复用同一解析结果，候选与 GeoNames 署名正常且横向溢出为 `0`。UI 流程仅模拟原生权限气泡返回“已同意”，扩展实际未获得网站来源权限；`Paris` 搜索因此显示本地化授权提示，并确认没有发出 Open-Meteo 请求。页面与 worker 控制台错误和警告均为 `0`，未读取真实 Chrome Profile、书签、密钥或缓存，也未保留截图。

## Release Verification

发布前设置真实公开 HTTPS 支持地址并运行：

```powershell
$env:REQUIRED_SUPPORT_URL = "https://github.com/sunpanran/Ampira/issues"
.\scripts\verify-extension.ps1 -Package
```

- 版本只从 `manifest.json` 读取，不在文档或自动化中硬编码。
- 检查生成的版本化 ZIP、SHA-256 sidecar 和 release manifest；ZIP 根目录必须直接包含 `manifest.json`。
- 确认产物不含 `dashboard-cache/`、测试、输出、隐藏配置、密钥、本机绝对路径、远程代码或允许列表外文件。
- 确认 `docs/` 页面已发布，三语商店文案、隐私披露、权限理由、审核说明与实际行为一致。
- 不得手工编辑 `dist/` 生成物。

## 2026-07-17 — Feed 与刷新策略解耦

- `extension/core/feed.mjs` 保持原公开接口，通用 Feed 文本、URL、去重与标记解析下沉到 `feed-utils.mjs`，降级日报生成下沉到 `feed-digest.mjs`；主模块由 1031 行降至 837 行。
- 刷新源状态、缓存复用和多来源证据选择从刷新编排中拆到 `extension/runtime/refresh-policy.mjs`，保留 `refresh-service.mjs` 的兼容导出和现有调用方。
- 架构测试增加聚焦模块体积上限，防止新边界重新回流成巨石；统一测试覆盖 Feed 解析、摘要、刷新策略和安全约束。

## 2026-07-17 — 三个主模块回退与局部保留

- 主页效率模块、内容栏和摘要卡片恢复基础表面与间距；仅保留每日快捷书签的透明承载层、`4px 0` 内边距、无二次模糊与溢出渐隐收口。
- 资讯卡片恢复基础边线、玻璃底色、阴影、正文间距和 favicon 占位光；资讯标签、时间/重要排序、“换一批”和“缓存”按钮保持不变。
- 书签页不保留任何 `#library` 卡片减重覆盖；分类面板和入口行恢复基础规则，同时保留类型选择器、分类选择器及分类/类型标签。
- 首页、资讯和书签的鼠标跟随光晕恢复为基础规则：大模块 `.10 / 260px`，可点击内容 `.09 / 240px`，显隐使用 `--motion-state-duration`；光晕开关与减少动态效果行为保持不变。
- 主页条目、资讯摘要、书签内容和资讯列表标题恢复共享的原始强调色悬停规则。
- 使用安全合成数据完成深色 `1280×800`、浅色 `1440×1000`、窄屏深色 `390×844` 检查：三个主模块在全部视口的横向溢出均为 `0`，控制台错误为 `0`。主页效率卡和内容栏恢复 `12px / 14px` 间距与内边距；资讯卡恢复 `10px / 16px` 桌面正文参数与 `.46 / blur(22px) / saturate(1.65)` favicon 占位光；书签分类恢复 `12px / 14px`。
- 合成预览包含 4 个每日快捷书签、10 个资讯标签和操作区、2 个资讯排序按钮、3 个书签分类标签及具体类型下的 4 个分类选项。设置样式和 DOM 未修改；截图位于 `output/playwright/dashboard-partial-rollback/`。
- `git diff --check`、`node tests/extension.mjs` 和 `.\scripts\verify-extension.ps1` 通过。
- 截图和预览不含真实 API Key、私人书签、Chrome Profile 或运行时缓存。本次未执行加载已解压扩展的手动 Chrome QA。

## 2026-07-17 — 五入口导航轻量化

- 今日、资讯、书签、搜索、设置五个入口的顺序、图标、文案、DOM 标识和交互保持不变；静态选中态继续使用既有强调色边线、胶囊底色、强文字和阴影。桌面按钮维持 `42px`，`1440px` 以上维持 `48px`，移动端维持 `42px` 纯图标触控目标。
- 桌面轨道改用导航专属的半透明主题表面、弱边线、轻量模糊和克制阴影；移除按钮鼠标径向光晕。未选中项悬停仅显示低对比表面，选中项仅在悬停时轻微调整底色；悬停与 `focus-within` 展开、三语动态宽度和标签淡入逻辑保留，并增加语言安全余量。
- 使用安全合成数据完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查：页面横向溢出均为 `0`，英文 `Settings` 展开标签无裁切，移动端标签保持隐藏，书签关闭后底栏由 5 项正确重排为 4 项。键盘焦点具有清晰 `2px` 轮廓，减少动态效果下标签过渡时长降为 `0.00001s`。
- 今日、资讯、书签滚动定位正常；搜索和设置打开时活动态正确切换，关闭后恢复至当前内容入口。控制台错误和警告均为 `0`。截图位于 `output/playwright/navigation-lightweight/`。
- `git diff --check`、`node tests/extension.mjs` 和 `.\scripts\verify-extension.ps1` 通过。截图和预览不含真实 API Key、私人书签、Chrome Profile 或运行时缓存；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — 浅色确认弹窗表面层级调整

- 共享确认弹窗的浅色主题统一使用同一种浅色叠层材质，内容区与操作区不再出现上浅、下深的材质断层；未保存、导入、清空、删除等所有复用该组件的确认弹窗同步生效，深色主题保持不变。
- 显式浅色与跟随系统浅色使用相同规则；弹窗结构、边线、按钮、焦点、背景关闭和焦点恢复逻辑不变。

## 2026-07-17 — SIGNAL FEED 资讯卡片内部排版微调

- 顶部信息行收敛为分类标签与紧凑操作组；稍后读、已读和手动整理继续保留原交互与可访问名称，桌面使用 `28px` 紧凑控件，窄屏恢复为 `32px` 触控尺寸。
- 来源与发布时间合并到标题下方的单行署名区域，时间改用语义化 `time` 元素；长来源允许安全省略，完整来源保留在 `title` 中，时间保持等宽数字且不换行。
- 标题使用更稳定的两行层级与紧凑字距；摘要紧跟来源信息从顶部自然排列，不使用底部固定、额外留白或分隔线，空摘要也不会占位。真实图片与 favicon 占位光的既有遮罩、卡片尺寸和五/六行摘要额度保持不变。
- 使用安全合成数据完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查：三个视口横向溢出均为 `0`，来源与时间保持同排，四列桌面卡片顶部不再因操作组产生换行，控制台错误与警告均为 `0`。截图位于 `output/playwright/signal-feed-card-layout/`。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过。截图和预览不含真实 API Key、私人书签、Chrome Profile 或运行时缓存；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — 客户端状态写保护生命周期收敛

- `assets/client/storage.mjs` 的本地写保护现在只覆盖仍待确认的有效持久化写入；成功写入或明确不可重试的拒绝都会释放对应键，使内容同步或其他页面之后发来的合法外部状态能够生效。
- 较旧批次完成时不会释放同一键上更新的待写值，继续保留 hydration、运行时回显和重叠写入期间的防陈旧覆盖语义；超限或非法键值只隔离自身，不会永久锁住后续有效外部状态。
- 后台客户端状态仓库跳过空补丁的存储读取，并在补丁值与当前记录一致时跳过整份 IndexedDB 重写，减少同步回显与重复保存造成的写放大；显式替换与重置仍保持原语义。
- 状态补丁复用同一份条目列表完成空值与相等性判断，前后台字节边界校验复用模块级 UTF-8 编码器，减少高频保存路径中的短生命周期分配。
- 状态仓库与浏览器端存储测试分别整理到 `tests/suites/client-state-store.mjs` 和 `tests/suites/client-storage.mjs`；统一入口仅负责注册，覆盖成功确认、重叠写入、不可重试拒绝和超限隔离后的外部恢复。

## 2026-07-17 — CSS 基础规则整理

- 合并资讯、效率、分类等卡片共享选择器的连续重复规则，保留原有边线、圆角、磨砂、隔离和过渡计算值。
- 将天气地点列表与待办列表的共享布局和各自间距拆清，移除待办列表 `3px` 后立即覆盖为 `0` 的冗余层叠。
- 弹窗头部与操作栏只共享布局声明，边线分别归属头部底边和操作栏顶边，不再依赖操作栏覆盖公共底边；选择器特异性与最终视觉保持不变。

## 2026-07-17 — 自定义重点色 HEX 取色器

- 将“自定义”重点色从浏览器原生 `type="color"` 面板替换为 Ampira 自绘顶层浮层；复用现有胶囊按钮、磨砂表面、紧凑控件、焦点环与颜色令牌，默认并仅以大写六位 HEX 作为文本格式，设置字段和保存流程保持不变。
- 浮层提供二维饱和度/亮度区域、色相滑杆、颜色预览和 HEX 输入；拖动、粘贴、Enter 提交、Escape 关闭、外部点击关闭与窗口重排均可用，纯亮色边缘的选中点不会被裁切。
- 二维区域支持方向键微调、Shift 加速、Page Up/Down 与 Home/End；关闭按钮和 Escape 会把焦点返回“自定义”触发按钮，Escape 不再冒泡触发设置页未保存确认。简体中文、繁体中文和英文文案同步补齐，并提供强制高对比度回退。
- 修正取色器处于通用 `.field` 容器时的样式串扰：HEX 输入和色相滑杆不再继承普通文本框的高度、内边距、边框与焦点光圈，HEX 行恢复单层对齐；二维调色区域改用四边内缩的独立渐变层，四个圆角都由中性底色和边框完整包覆，不再透出颜色像素。
- 使用不读取扩展缓存、书签或密钥的本地静态预览完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查；三种视口均无取色器横向溢出，移动端打开时会先将触发按钮滚入可用视区，再把浮层锚定在按钮下方，避免覆盖上方“语言与模式”设置。截图位于 `output/playwright/accent-picker-qa/`；控制台仅出现静态预览刻意不提供 `/api/*` 运行时数据产生的 404，没有取色器脚本错误。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 新增 HEX/HSV 双向转换与边界测试，并通过统一扩展测试和完整验证脚本。

## 2026-07-17 — 今日灵感换批响应优化

- “今日灵感”与“今日资讯”换批现在只重建被点击的栏目；批次、计数和卡片会在点击处理内立即更新，不再等待旧卡片完成 `120ms` 退场，也不会连带创建另外两个栏目的候选 DOM。
- 个人灵感预览继续预热完整三批图片，但预热已移出点击关键路径；新批次先使用现有图片或安全占位渲染，网络图片完成后再原位补齐。栏目头替换后会把焦点恢复到新的“换一换”按钮，支持键盘连续操作且不触发滚动跳动。
- 使用仓库现有的安全合成个人书签数据完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查：桌面与窄屏均能立即推进批次，换批后退场卡片数为 `0`、焦点仍在操作按钮、五张灵感卡完整保留，三个视口横向溢出均为 `0`，控制台错误与警告均为 `0`。本次未记录新截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 架构回归测试约束换批必须先渲染选中栏目、再后台预热图片，并保留即时替换与焦点恢复；`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过。

## 2026-07-17 — 资讯卡片操作图标悬停回弹

- SIGNAL FEED 资讯卡片的稍后读、已读归档与手动整理图标在鼠标悬停时增加 `220ms` 的轻微过冲回弹；最终放大约 `4%`，过冲峰值约 `8%`，不改变按钮尺寸、卡片布局、点击反馈或键盘焦点。
- 书签分类内条目的已读归档图标同步使用相同的悬停回弹；资讯手动整理的禁用态不触发，离开时平滑复位，并继续受全局 `prefers-reduced-motion` 规则约束。

## 2026-07-17 — 换批按钮弹性反馈

- 今日栏目“换一换”与 SIGNAL FEED“换一批”在点击并完成即时换批后，由整个按钮执行一次 `360ms` 轻量弹性缩放；图标不再单独旋转或缩放，悬停不触发动效。
- 修复“换一批”在更新批次文案时替换整个按钮内容、导致换批图标消失的问题；现在只更新 `.btn-label`，图标节点与无障碍结构持续保留。按钮尺寸、焦点恢复与换批逻辑不变，动画继续受全局 `prefers-reduced-motion` 规则约束。
- SIGNAL FEED 换批按钮的三语文案与今日栏目统一为“换一换 / 換一換 / Shuffle”，初始 HTML 与运行时本地化保持一致。
- SIGNAL FEED 顶部“缓存”按钮复用相同的点击后整按钮弹性回弹；图标不单独变形，禁用状态不会产生点击动画。
- 缓存按钮的整按钮回弹改用独立 CSS `scale` 属性，不再与加载图标的 `transform: rotate()` 共享变换通道，避免点击后旋转出现停顿；回弹时长与幅度保持不变。
- 缓存完成时不再从任意旋转角度直接替换图标；按钮等待当前 `spin` 动画触发下一次 `animationiteration`，在完整一圈的终点切回静止图标，使最后角度与起始角度一致。轮询期间复用现有图标节点，避免重复渲染重启动画。
- SIGNAL FEED 卡片的稍后读与已读归档复用“换一批”的整按钮 `360ms` 点击回弹；手动整理继续使用更克制的 `180ms`、峰值约 `2.5%` 回弹。图标不单独变形。
- 已读归档在正常动效模式下等待按钮完成短回弹后再提交状态，避免同步重建卡片使动画不可见；减少动态效果模式不增加等待。
- 已看归档卡片在取消归档、阅读队列卡片在移出队列时统一使用 `160ms` 纯透明度淡出，不再伴随上移；持久化状态先完成，淡出后再重绘对应列表。减少动态效果模式保持立即移除。
- “一键已读”与已看归档“清空”同步覆盖批量退场：当前列表中的全部卡片同时淡出，再统一重绘；批量状态写入及“一键已读”可选的新标签页打开仍在原用户手势流程内完成。

## 2026-07-17 — 封面调节预览取消全屏模糊

- 调整“显示高度”或“模糊强度”时，设置遮罩在现有封面预览状态中明确关闭 `backdrop-filter`，仪表盘封面与内容不再被全屏模糊层遮蔽；滑杆浮层、键盘操作、指针捕获和预览结束恢复行为保持不变。
- 每轮指针或键盘调节开始时平滑滚动到网页顶部，让封面立即进入视区；同一轮持续输入不会重复触发滚动或造成抖动。
- 全屏封面的显示高度下限由 100% 放宽到 70%，与其他封面布局使用相同的 70%–140% 范围；模糊强度上限由 24 px 提升到 50 px，界面、实时预览、首帧恢复、设置归一化和导入导出校验保持一致。
- 全屏高度低于 100% 时，头图本身保持至少一个视口高并从顶部锚定，仅由封面容器裁切底部；拖动高度不会再缩放或纵向移动头图构图。高于 100% 时仍可随容器扩展到 140%。
- 新增源码、设置边界和保存事务回归约束，确保两种调节共用的预览状态同时关闭标准与 WebKit 前缀滤镜、保留单次顶部滚动，并验证 50 px 模糊在实际写入与保存响应中不回落。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — AI 输出语言强一致性

- 所有用户可见 AI 输出统一声明并校验当前界面 locale；AI 问答、文章与网站说明、连续追问、每日简报、资讯卡片整理和阅读器翻译均在请求前后检查语言，连接测试保持原行为。
- 首次输出语言不合格时使用相同原始输入和更强语言约束自动重试一次；第二次仍不合格则丢弃 AI 文本并进入现有本地内容、原文或本地化错误兜底。被拒绝的结果不会进入缓存或追问历史，URL 类响应不再通过仅改写 `locale` 冒充新语言内容。
- AI 搜索缓存、卡片摘要策略和每日简报 Schema 升级至版本 6；缓存读取会再次验证生成正文。一次自动任务的初次调用与语言纠正共用一个任务额度，最终语言失败会保留该次额度以阻止后台无限重试；手动 Token 提示已同步说明最多一次纠正调用。
- 使用不含真实密钥、书签或缓存的内存模拟服务验证简体中文、繁体中文和英文的“错误语言 → 自动纠正”，并覆盖连续两次错误、请求期间切换语言、错误语言缓存和结构化字段混用语言。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过；本次无视觉改动，未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — AI 加载视觉语言统一

- AI 搜索、今日简报与资讯卡片手动整理继续使用 `160ms` 静态延迟、`700ms` 环境动效门槛和 `1600ms` 扫光周期；AI 回答与追问复用最终几何骨架，资讯卡片保留原内容并通过扫描、轻脉冲、按钮旋转和 `aria-busy` 表达处理中。
- 移除状态文字的原生 RAF 解码、随机字形池、逐字锁定、专用视觉节点与取消控制器。AI 搜索和今日简报的处理状态现在直接通过 `textContent` 完整显示；`role="status"`、`aria-live="polite"` 和减少动态效果规则继续保留。
- 使用安全合成数据复核深色 `1280×800`、浅色 `1440×1000` 和浅色窄屏 `390×844`：今日简报进入整理状态 `120ms` 时文本已完整为“整理中”，子元素数与解码节点数均为 `0`，没有额外 `aria-label`；`920ms` 时共享卡片扫光正常进入 `ambient` 且 `aria-busy="true"`。
- 三种视口横向溢出均为 `0`，控制台错误与警告均为 `0`；仅保留 Chromium 对页面现有多个独立表单的 verbose 建议。截图位于 `output/playwright/ai-loading-no-decode/`，不含真实 API Key、私人书签、Chrome Profile 或运行时缓存。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。
## 2026-07-17 — 新安装个人资讯书签默认关闭公共来源

- 新安装引导中选择个人资讯书签文件夹并保存时，会明确关闭“公共来源（Ampira）”；保留公共来源作为资讯来源时仍保持开启。
- 全局默认值、设置页来源切换、用户手动开关与现有用户数据均未调整，也不执行设置迁移或版本兼容。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — 普通 AI 问答支持连续追问

- 普通问题的首轮 AI 回答完成后会保留在当前搜索弹窗，并将输入框切换为连续追问；文章总结原有的网页追问能力保持不变。
- 普通问答上下文仅携带受限的首问、首答和最近 6 轮已完成问答；每次追问都会重新检索当前仍获准访问的 Ampira 内容，并在调用服务前复检来源权限。普通追问不写入持久 AI 搜索缓存。
- 简体中文、繁体中文和英文的追问状态、输入提示及系统提示词同步补齐；新增普通上下文边界和服务链路测试。`node tests/extension.mjs` 通过；由于当前 Windows PowerShell 7 进程会话不可用，未能执行 `.\scripts\verify-extension.ps1`。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — 发布候选浏览器验证

- 使用隔离配置从仓库根目录加载未打包扩展。Playwright Chromium 完成四步引导、无网站权限降级、深色 `1280×800`、浅色 `1440×1000`、浅色 `390×844` 和减少动态效果复核；页面横向溢出为 `0`，控制台错误与警告为 `0`。
- Microsoft Edge `150.0.0.0` 成功接管 `edge://newtab`，完成四步引导和无权限降级；“设置 → 浏览器”显示原生 favicon 不受支持并使用随包图标，“打开扩展管理页”实际打开 `edge://extensions/`。窄屏横向溢出为 `0`，控制台错误与警告为 `0`。
- Edge 实测发现首次引导仍显示“授权网站并启用图标”；现已按浏览器能力切换三语按钮文案，Edge 只显示“授权网站”，并保留不请求 `favicon` 权限的既有行为和自动化约束。
- 商店 Dashboard、精确来源权限和 AI 设置截图已从隔离配置重新捕获为 `1280×800`，不含真实 API Key、私人书签、个人浏览资料或运行时缓存。
- Google Chrome `150.0.0.0` 使用独立临时配置从仓库根目录成功加载未打包扩展；`chrome://extensions` 显示 Ampira 已启用、Service Worker 入口正常且无扩展警告。四步引导的无网站授权与稍后配置 AI 路径可用，真实 `chrome://newtab` 被 Ampira 接管；设置中的安装状态说明准确，“打开扩展管理页”实际打开带当前扩展 ID 的 Chrome 管理页。
- 真实 Chrome 在深色 `1280×800`、浅色 `1440×1000` 和浅色 `390×844` 下均无横向溢出，窄屏五入口导航完整可见；减少动态效果媒体查询生效，动画与过渡降至 `0.00001s`。上述状态下控制台错误与警告均为 `0`。
- Chrome 隔离配置内完成可选权限回归：`favicon` 授予后启用 Chrome 原生图标，撤销后即时回退随包图标；9 个精确资讯来源全部授权后资讯正常加载，单独撤销 `36kr.com` 后来源状态立即变为未授权；恢复出厂设置清理了其余可选来源权限。
- `search` 授予后顶部字段切换为“浏览器搜索”，非敏感测试查询由 Chrome 当前默认服务（Google）打开；撤销后即时回退为“搜索 Ampira”。恢复出厂设置确认清理 Ampira 本机/同步数据及可选权限、看板回到四步引导，未接触 Chrome 书签。
- 工具栏 `activeTab` 捕获、真实网络 Reader、非生产 AI 服务商、双实例 Sync，以及 Edge 垂直标签页与分屏仍需在商店私有测试前人工完成。
- 公开 README 已改为 Chrome 与 Edge 双浏览器口径，补充浏览器默认搜索、Edge favicon 回退、两家商店材料和不硬编码版本的 Latest Release、隐私及支持入口。

## 2026-07-17 — 26.2.9 发布与品牌收口

- `_locales/en`、`_locales/zh_CN` 与 `_locales/zh_TW` 的 Manifest 摘要和 Chrome Web Store、Microsoft Edge Add-ons 三语简短说明保持一致；当前用语以 2026-07-19 的商店文案章节为准。
- `assets/icons/ampira-logo.svg` 是唯一可编辑品牌源；移除光晕、渐变、旧纯紫色标记以及商店目录中的重复矢量副本。关于页、工具栏弹窗和 GitHub README 直接使用该矢量源。
- 扩展必需的 16/32/48/128px PNG 与 Chrome Web Store 使用的 128px 透明 PNG 均从唯一矢量源生成，不保留并行旧版本；128px 商店图标保留透明安全区。
- `store/privacy-practices.md` 提供与当前行为一致的 `favicon`、`search` 及精确来源权限说明；软件包、商店文案和测试统一以 26.2.9 为最终发布版本。

## 2026-07-19 — 三语 UI 文案与 AI 输出长度收敛

- 简体中文、繁体中文和英文同步清理“补盲”“兜底”“候选池”“入口”等内部术语；主动获取内容的按钮与空状态统一使用“刷新／重新整理／Refresh”，技术意义上的本地缓存、缓存清理与缓存记录保持不变。
- 来源、权限、错误、恢复出厂、来源诊断、浏览器搜索和 AI 免责声明改为直接、具体的用户表述；内置来源统一显示为“公共来源（Ampira）／Public sources (Ampira)”。公共来源和灵感预设均不再受规范名称断言约束。首次引导的“让收藏已久的书签，重新流动起来”保持不变。
- 三语支持页将“书签入口／书籤項目／Bookmark entries”统一为“书签／書籤／Bookmarks”；三语隐私页、商店隐私实践和审核说明同步采用“备用图片搜索／全网图片搜索”的界面名称，Brave 的启用条件、来源授权和数据传输行为没有变化。
- AI 输出提示词已恢复原策略：今日简报固定三行；资讯卡片固定“标题＋三行摘要”，信息充分时目标为 130–180 个字符且不少于 110；网站介绍固定三个栏目，中文 250–450 字、英文 250–450 词。
- `dashboard.html` 与工具栏浮窗的加载前文案已和简体 locale 对齐，三套客户端 locale 保持相同键集合。`.\\scripts\\verify-extension.ps1` 已通过；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-19 — P1 客户端语言包按需加载

- 首页不再静态导入三套完整客户端词典；启动时只通过本地字面量动态导入加载当前语言，切换语言前先完成目标词典准备，并复用并发中的同一加载任务。设置读取、保存、取消、导入和首次引导均保留原有语言状态与持久化语义。
- 10 个跨语言状态识别文案和工具栏浮窗使用的 11 个文案分别生成独立子集；工具栏浮窗改为直接使用微型三语词典与运行时消息端口，不再引入首页 API 和完整 i18n 依赖图。旧的生产完整词典适配器已删除，完整词典一致性比较仅保留在测试辅助模块。
- 首页两个入口的静态模块并集由 105 个模块、957,126 B 降至 102 个模块、740,080 B，减少约 22.7%；工具栏浮窗由 12 个模块、244,767 B 降至 6 个模块、14,846 B，减少约 93.9%。两条静态图均不包含 `en`、`zh-CN` 或 `zh-Hant` 完整词典，并由 760 KiB 与 40 KiB 回归预算约束。
- 使用隔离配置加载未打包扩展完成 Playwright Chromium 回归：英文冷启动只请求英文完整词典，之后繁中与简中各按需请求一次；并发繁中准备、设置页三语切换、繁中保存和浮窗跟随均正常。浮窗只请求 `locales/popup.mjs`，没有请求完整词典。
- 深色 `1280×800`、`1440×1000` 和繁中窄屏 `390×844` 均无页面或设置浮层横向溢出；冷启动没有长任务，页面与浮窗控制台错误均为 `0`。测试使用空白临时浏览器资料目录，结束后已关闭并删除；未记录截图，也未读取真实密钥、私人书签或运行时缓存。
- 客户端词典生成器 `--check`、统一扩展测试、`.\\scripts\\verify-extension.ps1` 和带 `-Package` 的最终验证均通过。`dist/ampira-26.2.9.zip` 包含 283 个文件、大小 2,260,971 B，SHA-256 为 `13559cdec9a1441cb8e98d4b6d293c4b747ee36482e2348758c0d0f414e140aa`；ZIP、sidecar 与发布清单一致，包内无旧完整词典适配器、测试、脚本或 `dashboard-cache/`。

## 2026-07-19 — 天气主导状态与次要信息收敛

- 天气请求不再显式组合 CMA、ECMWF、GFS 与 ICON，也不再执行自定义投票、降水阈值、云量回退和 CMA 失败重试；省略 `models` 参数，由 Open-Meteo 原生 `best_match` 自动选用当地适用模型。
- 今天直接使用官方 `current` 状态、当前温度和体感温度，并从当前小时读取降水概率；明天、后天直接使用官方 `daily` 天气码、最高/最低温度和每日降水概率峰值。
- 天气响应保持独立的实时 `current` 与两项未来 `daily`。缓存继续使用尚未发布的 v3 键，不增加数据版本；缓存按坐标和当地日期失效。
- 小字恢复旧版的信息组合：今天显示当前温度、体感温度和降水概率，明天、后天仅显示降水概率。由于当前 Windows PowerShell 登录会话不可用，未能执行 `.\\scripts\\verify-extension.ps1`；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。
