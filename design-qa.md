# Ampira MV3 QA

本文件只描述当前 Manifest V3 Chrome 扩展的验证状态。已移除的 Node 版实现不属于当前运行架构；旧 `dashboard-cache/` 仅是用户自行管理的历史数据，本项目不会读取、迁移、删除或打包它。

## 2026-07-15 — Light cover heading and date contrast

- 全屏头图上的标题背景阴影由不透明主题底色收敛为更短、更透明的两层阴影，减弱浅色模式下的白色光晕，同时保留复杂图片上的文字分离度。
- 浅色与跟随系统浅色模式下，日期主文字改用强文字色，星期、时间、分隔线、资讯批次状态以及资料库筛选／分类导航的未选中文字和图标同步提高对比；深色模式与非全屏头图保持原样。
- 资料库类型筛选与分类导航的悬停底色在深浅主题下统一使用 68% 主题悬停表面混合透明色，避免遮成不透底的实色块；选中态不变。
- 桌面浮动导航改为 84% 主题表面与 `12px` 背景模糊，保留轻度头图透出，同时低于首页卡片的 72% 表面与 `18px` 模糊层级。
- 资讯优先级／时间选择器、换一批与缓存按钮、书签类型和分类选择器的选中底统一复用首页卡片的 `--blur-panel-bg` 与 `--card-backdrop-blur`；强调色继续作为透明叠层，不再以不透明控制表面遮住头图。
- 浅色与跟随系统浅色模式下，热门资讯卡片的发布时间和来源域名由 `--muted-2` 克制地向 `--muted` 混合 58%，提高浅色玻璃表面上的辨识度；其他辅助文字与深色模式不变。
- 本次为定向样式调整；未进行加载已解压扩展后的手动 Chrome QA。

## 2026-07-15 — Bookmark empty-state settings action

- “暂无书签入口”空状态增加“打开设置”按钮，复用已有空状态按钮、三语文案与设置路由，并直接定位到“书签”设置页；筛选无结果时不显示该按钮，全部分类隐藏时继续显示原有“书签设置”操作。
- 自动化增加源码约束，确保无入口分支持续绑定书签设置操作；未进行加载已解压扩展后的手动 Chrome QA。

## 2026-07-15 — Toolbar Read-later Restoration

- 恢复工具栏即时稍后读：`action-popup.html` 在用户点击 Ampira 图标后读取当前活动标签的标题与网址，通过独立后台服务原子写入阅读队列，重复网址去重，并从当天及保留的已读状态中恢复同页。工具栏不再打开新的 Ampira 标签页。
- Manifest 恢复最小范围的 `activeTab`：只在用户明确点击图标时临时读取当前标签页的 `title`/`url`，导航或关闭后撤销；未增加 `tabs`、内容脚本、`scripting` 或必需主机权限。HTTPS 与 localhost/127.0.0.1 可加入，普通 HTTP、Chrome 内部页、文件页、含凭据网址和超长网址拒绝写入。
- 工具栏写入复用现有本地阅读队列；默认不上传。用户另行开启“同步稍后读”后，新增记录通过同一有界 Chrome Sync 服务同步。浮窗、三语 Manifest 标题、README、三语隐私页、商店披露、审核说明、清单与打包入口已同步恢复。
- 自动化新增捕获规范化、去重、150 条上限、已读恢复、原子并发、同步转发、徽标反馈、失败与不支持状态覆盖；`node tests\extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过。
- Playwright CLI 以隔离临时配置加载真实解压扩展，仅使用 `example.com` 合成标题和网址，不读取真实标签、书签或密钥。简中深色新增、英文浅色重复、繁中深色不支持状态的正文高度分别为 `208px`、`208px`、`196px`，固定宽度 `352px`、横向溢出均为 `0`，控制台零错误与警告；截图位于 `output/playwright/toolbar-read-later-*.png`。浏览器顶部工具栏不属于页面自动化范围，本次未声称真实工具栏点击手工 QA。

## 2026-07-15 — Search and AI conversation glass surfaces

- 搜索输入弹窗与 AI 回答／连续对话容器复用主界面卡片的半透明背景、边线、圆角和 `--card-backdrop-blur`；焦点、滚动和会话行为不变。
- AI 回答／连续对话容器移除左侧紫色线性渐变，改为统一的中性玻璃底色；用户问题行也不再铺设左右截断的单色色块，仅通过序号、文字和分隔线建立层级。
- 回答段落与连续对话统一为标签列／正文列网格：桌面使用 `30px + 14px`，窄屏收敛为 `24px + 10px`；正文限制到 `72ch` 并增加行距，问题使用较紧的行高和字重。对话模式不再继承普通文本回答的外层 `18px` 内边距，消息分隔线可完整贯穿玻璃容器。
- 首轮 AI 回答右上角与每条 AI 回复正文右侧增加纯图标复制按钮；用户问题不显示。按钮复用本地 copy/check 图标，支持悬停、按下、键盘焦点、复制成功和失败状态，并以三语 `aria-label`／`title` 提供反馈；剪贴板不可用时沿用本地选择复制降级。
- 浅色搜索遮罩从不透明中性表面改为 72% 主题背景与共享卡片模糊，确保玻璃效果可见，同时维持文字对比。
- Playwright CLI 使用合成文章与两轮追问覆盖深色 `1280×800`、浅色 `1440×1000` 与窄屏 `390×844`。桌面对话面板宽 `740px`，对话容器外层内边距为 `0`，消息网格为 `30px + 14px`；窄屏实测为 `24px + 10px`。AI 对话容器的 `background-image` 为 `none`，用户问题行背景完全透明；三个视口横向溢出均为 `0`，控制台零错误与警告。截图为 `output/playwright/search-glass/ai-layout-*`，本次未声称真实 Provider 请求或加载已解压扩展后的手动 Chrome QA。
- 同一安全场景复核 1 条首轮回答与 2 条 AI 回复共 3 个复制按钮：按钮均为 `30×30px`，首轮按钮距回答区右侧 `14px`；窄屏回复网格实测为 `24px + 256.667px + 30px`，键盘聚焦命中 `:focus-visible` 且计算为实线外框。深浅主题与窄屏横向溢出均为 `0`，控制台零错误与警告；截图为 `output/playwright/search-glass/ai-copy-*`。

## 2026-07-15 — Reader glass surface

- 站内阅读弹窗复用主界面卡片的 `--blur-panel-bg`、`--card-line` 与 `--card-backdrop-blur`，正文容器改为透明，底部工具栏使用同一半透明表面；深浅主题、Reader 动画、滚动与交互结构不变。
- 自动化约束阅读窗不得重新引入遮住模糊效果的实色正文层，并验证外壳与工具栏持续复用共享卡片令牌。
- 视觉复核发现仅在动画遮罩内部的弹窗上设置 `backdrop-filter` 会被外层实色暗幕削弱为近似透明叠层；阅读遮罩现在直接复用搜索遮罩的主题背景与模糊滤镜，确保它在合成层上实际处理主界面。
- Playwright CLI 使用安全合成文章复核深色 `1280×800`、浅色 `1440×1000` 与窄屏 `390×844`。阅读遮罩在深色模式计算为 54% 主题背景与 `blur(20px) saturate(118%)`，浅色模式为 72% 主题背景与 `blur(18px)`；弹窗维持 72% 卡片表面与 `blur(18px)`，正文和阅读层透明。三个视口横向溢出均为 `0`，控制台零错误与警告。修正截图位于 `output/playwright/reader-glass/reader-blur-fixed-*`，本次未声称加载已解压扩展后的手动 Chrome QA。

## 2026-07-15 — Compact AI article explanations and follow-ups

- AI 搜索的文章解释改为最多三段的“核心判断／关键信息／必要信息边界”：中文目标 `350–500` 字并硬限制为 `500` 字，英文硬限制为 `180` 词；正文上下文从 `12000` 收紧到 `8000` 字，文章与追问使用独立 `900 tokens` 预算，并在支持的 Provider 上关闭非必要思考输出。搜索缓存升级到 v5，避免复用旧长摘要。
- 文章总结成功后，弹窗切换为当前文章会话，保留首轮总结并逐轮追加用户问题与 AI 回答；用户问题使用紧凑的 `1.`、`2.`、`3.` 顺序标记，不显示“第几个追问”。新网址开始新会话，关闭弹窗后全部清空且不持久化。Provider 只接收最近 `6` 轮、合计不超过 `4000` 字的历史；追问优先使用已授权 Reader 缓存，缓存缺失才重新读取，并在发送前复核文章网址与 Provider 状态。
- 搜索输入容器移除常驻 `:focus-within` 描边和阴影，静态边框、输入光标与按钮键盘焦点仍保留。浅色搜索遮罩改用不透明的中性灰白表面，不再透出头图的粉紫渐变；深色模式保持原有对比。首轮文章报告使用紧凑间距和独立 `69vh` 高度上限；进入多轮对话后恢复 `52vh` 可滚动记录。
- 自动化新增 AI 搜索专项覆盖三语提示词、长度和上下文边界、缓存版本、独立输出预算、Reader 缓存优先、撤权、Provider 变化、追问不落盘、会话重置和可访问日志语义；`.\scripts\verify-extension.ps1` 通过。
- Playwright CLI 使用隔离 Chromium 配置加载未打包扩展，并注入不含私人数据的合成文章与问答。`1280×800` 深色和 `1440×1000` 浅色的 500 字总结均为 `scrollHeight === clientHeight === 549px`，无需内部滚动；`1440×1000` 与 `390×844` 的八轮记录可滚动，三个视口横向溢出均为 `0`。输入框聚焦前后边框颜色一致且 `box-shadow: none`，控制台零错误与警告；截图位于 `output/playwright/ai-search-followup/`。本次未使用真实 API 密钥或私有文章，也未声称真实 Provider 响应时延 QA。

## 2026-07-15 — Unified select comboboxes

- 设置页与首次引导的全部 9 个单选 `select` 由公共 Combobox 渐进增强：收起态保持 `38px` 紧凑高度、控件圆角和主题表面；展开态改为同一套顶层主题列表，统一活动、悬停、已选、禁用、勾选和滚动状态。设置页不再维护重复的 `.field select` 样式。
- 原生 `select` 仍是选项、值与表单事件的数据源；增强层监听动态选项、字段禁用和程序设置的 `value` / `selectedIndex`，并保留冒泡的 `input` / `change`。现有设置结构、选项顺序、协议和本地化文案未改变。
- 触发框使用 `combobox` 语义，顶层面板使用 `listbox` / `option`、`aria-expanded`、`aria-selected` 与活动后代；支持 Tab、方向键、Home/End、Page Up/Down、Enter、Space、Escape 和中英文按键检索。强制高对比度模式跳过增强，恢复系统原生控件与箭头。
- 点击和聚焦不改变盒模型或位移；焦点提示为重点色边界与轻量 `1px` 外框，`box-shadow: none`。展开面板最大 `280px`，长列表内部滚动，空间不足时向上展开；触发值和选项文字各自安全省略，完整文字保留在 `title`。
- 自动化增加键盘索引、禁用项跳过、中英文按键检索、ARIA 语义、动态同步、事件兼容、高对比度回退、左右边距和顶层列表样式断言。本次 Combobox 聚焦测试、全部浏览器模块语法检查、差异格式检查与 `.\scripts\verify-extension.ps1` 均通过。
- Playwright 安全本地预览覆盖深色 `1280×800`、浅色 `1440×1000` 与 `390×844`。桌面触发框和列表均为 `442px` 宽；移动端均为 `344px` 宽，页面与设置弹窗横向溢出为 `0`。短列表不预留滚动条槽，选项左右外边距实测均为 `5px`；10 项合成长列表限制在 `280px`，位于视口底部时从触发框上方展开，仅实际溢出时显示滚动条。长标签安全省略，禁用项可辨认。键盘选择会同步原生值、关闭面板并返回焦点，Escape 不提交；控制台零错误与警告。截图位于 `output/playwright/native-selects/`。本次未读取真实书签、密钥或运行时缓存，也未声称加载已解压扩展后的手动 Chrome QA。

## 2026-07-15 — Intentional first-frame motion system

- `assets/styles/tokens.css` 成为唯一动画曲线与时长来源：standard、enter、exit、move、ambient、brand 六条语义曲线对应 `100/180/240/300/360/1600ms` 时长层级。CSS 动画与 WAAPI 共用相同字符串；架构测试拒绝组件内匿名 cubic-bezier、`ease` 系列，以及旋转和骨架扫光白名单外的 `linear`。
- 新标签页首屏保留同步主题和最终布局，导航、标题日期、搜索和快捷栏按 `24/40/88/120ms` 分层进入；数据返回后效率区与三列工作台只动画直接内容，使用 `360ms enter`、`32ms` 错峰并封顶 `96ms`。头图独立以 `480ms`、`1.018 → 1` 进入，不等待 Dashboard 数据。
- 初次加载在 `160ms` 前隐藏骨架、`160–700ms` 显示静态占位、`700ms` 后才启用 `1600ms linear` 低对比扫光；Reader 复用同一阶段控制。已有内容刷新继续保留正文。AI 答案取消逐字定时器，完整结构化 DOM 以 `240ms enter` 一次进入。
- 设置、AI 搜索和首次引导使用 `300ms enter / 180ms exit`；Reader 由旧的 `650ms` 收敛为 `360ms enter`。Provider、图片搜索和快捷书签设置使用可快速反向的 `240/180ms` 展开收起。待办、阅读队列和快捷方式补齐 keyed 新增、`120ms` 删除、`240ms move` FLIP 重排与 `180ms standard` 图标确认；Logo 保留唯一 brand 回弹。
- reduced-motion 与后台标签跳过首屏、位移、缩放、回弹、FLIP 和循环；骨架保留静态忙碌状态。动画完成会清理 `will-change`、临时 class、展开高度和 finished WAAPI effect，关闭途中重新打开会恢复可交互状态。
- Playwright 安全本地预览以合成 API 响应采样首屏 `0/160/320/520ms`、三阶段加载、设置浮层 `300/180ms` 进退、快速反向操作、`240ms` FLIP、展开动画清理和 reduced-motion。深色 `1280×800`、浅色 `1440×1000`、`390×844` 横向溢出均为 `0`，稳定态无运行中动画，控制台零错误；截图位于 `output/playwright/motion-system/`。本次未声称加载已解压扩展后的手动 Chrome QA。

## 2026-07-15 AI Provider Catalog

- AI Provider 从单一 DeepSeek 模板改为渐进式目录：首屏提供 OpenAI、DeepSeek、Gemini、Kimi、Qwen、OpenRouter、Groq 与 Ollama，两列紧凑显示；SiliconFlow、智谱 GLM、豆包 Ark、Mistral 和自定义地址收进“更多兼容服务”。已有已保存配置默认折叠为服务商、域名、模型、状态和测试／编辑／更换操作。
- Provider 预设只在前端填写实际基础地址、API 风格、推荐模型与模型文档，不保存 Provider ID，也不改变单套本机凭据结构。Kimi、Qwen 提供区域选择；工作空间专属 Qwen 地址仍走自定义模式。模型改为必填，未保存但已可测试的草稿显示“待保存”，避免误报“已配置”。
- 数据传输确认和精确 Origin 请求合并到同一次“理解并授权”用户操作。Ollama 默认使用 `http://localhost:11434/v1` 与 Chat Completions；只有 `localhost`、`127.0.0.1` 可无密钥并省略 Authorization，远程兼容地址仍强制 Bearer 密钥。跨 Origin 切换继续清空草稿密钥、重置同意且不复用旧 Origin 的凭据。
- 自动化覆盖 12 个预设的地址、分类、区域映射、端点拼接与识别，自定义地址回退、模型必填、跨 Origin 凭据隔离，以及回环 Ollama 无密钥请求和远程地址拒绝；`node tests\extension.mjs` 与 `scripts\verify-extension.ps1` 通过。
- Playwright 安全本地预览使用模拟设置与权限，不读取真实密钥、书签或缓存。深色 `1280×800`、浅色 `1440×1000` 与窄屏 `390×844` 的页面、设置弹窗和 Provider 区横向溢出均为 `0`；窄屏目录为单列，键盘 Tab 可在选项间移动，模拟 Ollama 授权后只开放必填模型与额度字段。截图位于 `output/playwright/ai-provider-catalog/`。本地 HTTP 预览的扩展 API 请求按预期返回 `404`，因此未声明解压扩展控制台或真实 Chrome 权限弹窗 QA。

## 2026-07-15 Viewed Archive Default

- “保留已看归档”现在默认开启；新安装和尚未保存过该字段的配置会持续保留已看内容，直到用户手动清空。已经显式关闭该选项的现有配置仍保持关闭。
- 自动化锁定默认值，避免后续设置调整意外恢复为隔天清除；本次没有改变设置界面布局，因此无需新增视口截图。

## 2026-07-15 Personalized Ranking Default

- “个性化排序”现在默认关闭；新安装和尚未保存过该字段的配置使用默认排序，用户仍可在设置中主动开启。已经显式开启该选项的现有配置仍保持开启。
- 自动化锁定默认关闭行为；本次没有改变设置界面布局，因此无需新增视口截图。

## 2026-07-15 Personalized Ranking Placement

- “个性化排序”从“抓取与整理”迁移到同页的“高级设置”，字段、三语文案、保存行为和本机反馈数据边界不变。
- 首帧复选框与默认关闭设置保持一致；自动化约束该开关只能出现在高级设置分组中。
- Playwright 安全合成回归覆盖深色 `1280×800`、浅色 `1440×1000` 与浅色 `390×844`：控件只出现一次、位于高级设置且默认未选中，页面与设置弹窗横向溢出均为 `0`，控制台零错误与警告。截图位于 `output/playwright/personalized-ranking-placement/`；本次未声称解压扩展手动 Chrome QA。

## 2026-07-15 Local Header Cover and Height

- “首页头图”新增本地 JPEG、PNG、WebP 与 AVIF 选择、替换和移除。本地源图在浏览器内限制尺寸、按质量阶梯重编码为 WebP，并通过 Canvas 去除 EXIF 等源文件元数据；优化结果以独立版本化记录保存在 `chrome.storage.local`，不进入 Chrome Sync、设置导入导出或 `localStorage` 首帧提示。
- 本地图片优先于保留的 URL；选择与移除先即时预览，保存时与同步设置共同提交，失败时回滚，放弃未保存设置会恢复原图。首帧从本机记录恢复，本地图片损坏或加载失败时回退 URL；本机记录变化随 `settings.changed` 广播到其他已打开页面。
- 标准与固定模式共用 `70–140%`、步长 `5%` 的显示高度；`100%` 保持原断点基础高度，全屏固定视口高度，关闭与全屏隐藏滑块。滑块支持方向键、焦点、`aria-valuetext`、拖动预览弱化和键盘空闲 `600ms` 恢复；拖动圆点或点击轨道产生第一次数值变化时都会立即进入与模糊度相同的暂时隐藏窗口状态，reduced-motion 下过渡降为近零。
- Playwright 使用仓库内安全合成 WebP 验证选择、WebP 重编码、文件名／尺寸／优化大小、本地优先、保存与刷新恢复、取消恢复、移除后 URL 回退、签名合法但无法解码的本机记录回退 URL，以及 SVG 和损坏 JPEG 拒绝。深色 `1440×1000`、浅色 `1280×800` 与浅色 `390×844` 覆盖最小／默认／最大高度；各视口 `scrollWidth === clientWidth`，手机高度分别约为 `200.86 / 286.96 / 401.74px`，控制台零错误。截图与合成接口位于 `output/playwright/header-cover-local/`；本地 HTTP 预览不作为解压扩展手动 Chrome QA 声明。
- `node tests\extension.mjs`、`scripts\verify-extension.ps1`、相关 core/runtime/client 模块语法检查与 `git diff --check` 均通过。

## 2026-07-15 Website Permission List Icon Cleanup

- “网站访问权限”继续在章节标题保留钥匙图标，域名列表移除逐行重复的钥匙图标，让域名与授权状态成为主要扫描信息。
- 权限行从图标、内容、操作三列收为内容、操作两列；窄屏下内容与操作自然堆叠，授权和撤销行为不变。
- 安全合成预览覆盖 `1280×800`、`1440×1000` 与 `390×844`：章节标题保留 1 个钥匙图标、8 行域名不再出现钥匙图标，三个视口横向溢出均为 `0`，行内容无溢出。截图位于 `output/playwright/permission-list-icons/`；本次未声称解压扩展权限流程的手动 Chrome QA。

## 2026-07-15 Review Follow-up Fixes

- 首次“一键已读”先在用户手势内完成网址打开、已读状态与队列提交，再异步保存是否同时打开网站的偏好；当前操作未完成或项目已不在队列时拒绝重复处理，避免重复开页和重复反馈。
- 启动自动缓存现在复用 Dashboard 的缓存可用性门控；未完成书签确认、没有来源或没有已授权来源时不再向后台发起非强制刷新。来源或权限刚变化后的强制刷新继续可用。
- 自动化新增延迟设置保存下的一键已读重入回归，以及不可用自动刷新跳过、强制刷新保留的覆盖。本次未声称解压扩展手动 Chrome QA。

## 2026-07-15 Content Sync Binary Master

- “跨设备同步”总开关不再显示部分选择的中间态：任一内容类型开启时总开关保持开启，全部关闭时明确显示关闭。
- 总开关关闭后，稍后读、待办事项和天气城市三个子选项同步进入与“公共 Feed（Ampira）补盲”相同的整行 `disabled`、`aria-disabled` 弱化不可操作状态；重新开启总开关会恢复并选中三个子项。
- 自动化覆盖部分选择、全部关闭、重新开启和设置保存忙碌状态。安全本地 Chromium 预览覆盖深色 `1280×800`、浅色 `1440×1000` 与窄屏 `390×844`；禁用子项计算样式为 `opacity: .5`、`cursor: not-allowed`，设置弹窗横向溢出为 `0`，控制台零错误与警告。截图位于 `output/playwright/content-sync/content-sync-binary-disabled-*.png`；本次未声称解压扩展手动 Chrome QA。

## 2026-07-15 Settings Status Footer

- 设置草稿状态从内容区顶部迁移到固定底部操作栏，位于支持链接和保存按钮之间；保留原有实时文案、状态圆点与 `aria-live` 播报。
- 状态区域改为透明且无边框，桌面端占用中间剩余空间，窄屏允许文案自然换行。
- 安全本地预览覆盖深色 `1280×800`、浅色 `1440×1000` 与浅色 `390×844`；三个视口横向溢出均为 `0`。本次未声称加载已解压扩展后的手动 Chrome QA。

## 2026-07-15 Image Search Settings Layout

- 图片搜索兜底保留独立总开关；启用后的密钥输入、获取说明、Brave 域名授权、连接测试与密钥删除收拢到同一个凭据配置区。
- 桌面端让密钥输入占满配置区宽度，常规操作成组排列并将危险操作独立靠右；`820px` 以下改为自然纵向换行，功能、文案、权限和密钥存储行为不变。
- 安全本地预览覆盖深色与浅色的 `1280×800`、`1440×1000`、`390×844`；页面、设置内容区和图片配置区横向溢出均为 `0`，操作按钮保持在配置区内，控制台零错误与警告。复核截图位于 `output/playwright/image-search-layout/`；本次未声称解压扩展权限流程的手动 Chrome QA。

## 2026-07-15 Quick Bookmark Settings

- 网站快捷方式管理从“设置 → 浏览器”迁移到“设置 → 书签”，首页快捷栏、存储结构和交互保持不变；从首页空状态或右键编辑进入设置时会直接定位到书签页。
- 三种语言将设置内名称统一为“快捷书签 / Quick bookmarks”，书签页导航提示同步说明包含来源与快捷书签；浏览器页只保留新标签页、图标、权限与同步等浏览器集成设置。

- 天气日摘要不再直接展示 Open-Meteo 的“全天最严重天气代码”。预报请求同时取得逐小时天气代码，并以当地时间 08:00–18:00 出现次数最多的天气类型作为当天摘要；例如白天大部分时段多云、仅一小时可能雷暴时，卡片显示“多云”。缺少逐小时数据时仍安全回退到原每日代码；缓存键已升级，旧口径缓存不会继续显示。

## Current Architecture

- `dashboard.html` 是新标签页单页入口；`assets/client/app.mjs` 仅组装 `createDashboardApp()` 并转发运行时事件，浏览器代码使用本地 ESM，不加载远程脚本或执行远程代码。
- `extension/service-worker.mjs` 是最小 composition root；Chrome 事件、消息 route map 和工作流位于 `extension/runtime/`，设置归一化、有界网络读取、配额和客户端状态写入位于 `extension/core/`。
- `assets/dashboard.css` 是只引用本地文件的有序样式清单；tokens、布局、primitives、dashboard、settings、overlays 和响应式规则位于 `assets/styles/`，原有级联顺序保持不变。
- 必需权限保持为 `bookmarks`、`storage`、`alarms`；`favicon` 与网站来源访问均为可选权限，只通过用户手势申请。
- 部分非凭据设置位于 Chrome Sync；阅读队列、待办和天气城市默认位于 IndexedDB，并可通过三个独立的默认关闭开关选择加入 Chrome Sync；本机凭据位于 `chrome.storage.local`，Feed、Reader、摘要、完整阅读状态和天气响应缓存始终位于 IndexedDB。
- UI 支持简体中文、繁体中文和英文；远程文本仅通过结构化 DOM 与 `textContent` 渲染。

## Automated Verification

每次功能修改必须运行：

```powershell
.\scripts\verify-extension.ps1
```

当前测试覆盖：

- Manifest 必需/可选权限、原生 favicon URL 编码与回退、CSP、三语键一致性和远程代码禁令。
- 根页面资源与 `docs/` 全部 HTML 子页的本地资源和链接完整性。
- 书签角色去重、嵌套分类上限、来源排除和精确域名生成。
- RSS、Atom、JSON Feed、XML 实体 URL、来源安全上限、网络失败、URL 去重排序，以及跨来源事件标题聚类。
- 设置白名单与边界归一化、本机密钥隔离、缓存裁剪和旧凭据清理。
- 大型设置的 Chrome Sync 分块与并发写入串行化、客户端状态串行合并与批量发送、可选内容逐条合并／删除墓碑／关闭清理、非法状态载荷拒绝、并发 AI 配额预留。
- Reader 主体提取、安全链接、图片/视频结构、缓存回退、HTTP 分类、超时语义及无长度响应的流式大小限制。
- Brave 图片预览的权限、大小限制、安全搜索、低价值图片过滤、并发请求去重和缓存降级路径。
- 刷新启动锁、强制刷新排队、缓存清理代际失效，以及失败来源保留最后成功内容、有效空来源清理旧内容。
- 设置草稿差异、取消恢复、未知字段隔离、网站权限批量操作状态，以及优先级、去重、未读筛选和分页等前端纯选择器。
- Composition-root 行数、core/runtime 依赖方向、消息 route map，以及所有 dashboard CSS import 的本地性和文件完整性。

## 2026-07-15 Runtime Policy Decoupling

- 待办与天气地点规范化改为 core 领域模块的单一实现，内容同步与 Dashboard 共用同一规则；私网地址判断、JSON-LD 图片与 `srcset` 解析同样收敛为共享安全策略，避免 Reader、Preview 与图片候选逻辑漂移。
- 刷新服务移出卡片摘要策略与 Feed 图片补全；摘要的解析、复用、Provider/语言绑定和失效清洗由独立策略负责，图片候选、缓存时效与质量统计由独立服务负责。
- AI Provider 能力和 AI 缓存许可从搜索服务中拆出；权限撤销后的 search、feed-image、preview、reader、weather 缓存检查通过独立访问策略分发，权限工作流仅保留失效、提交、重试和广播编排。
- 权限代际成为独立对象，Dashboard 内容与权限工作流共享同一代际边界；刷新协调器通过显式 `setRun` 绑定执行器，运行时装配不再依赖未初始化的 service 变量。
- 本次不改变消息协议、权限清单、缓存 schema、可见 UI 或远端数据边界，因此无需新增视口截图。

## 2026-07-14 Daily Brief AI Setup Entry

- 今日简报现在直接使用 Dashboard 返回的 AI 就绪状态；AI 未完全配置时不再显示无效的“生成简报”，而是显示“AI 未就绪”、配置说明与“配置 AI”入口。
- 配置入口会打开设置、切换到 AI 页，并把键盘焦点放到当前第一个缺失步骤；合成未确认数据传输状态验证焦点落在确认复选框，未触发简报生成请求。
- 自动化覆盖 AI 就绪门槛、设置页定向与三语操作文案。安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`，文档与设置弹窗横向溢出均为 `0`，控制台零错误或警告；截图位于 `output/playwright/daily-digest-ai-setup/`。

## 2026-07-14 Dashboard Date Display

- Dashboard 日期改为语义化 `<time>` 结构，将本地化日期、短星期与 24 小时时间拆成独立层级；日期使用点分隔和较强字重，星期与时间弱化，并以现有紫色强调线和克制分隔线保持信息终端风格。
- 可见时间精度从秒调整为分钟，数字启用等宽特性；计时器对齐整分钟刷新并继续检查本地跨日，减少每秒跳动与无效渲染。可访问名称与可见精度一致，`datetime` 保留机器可读时间。
- 自动化覆盖固定日期、短星期、无秒 24 小时文本和 ISO `datetime`。安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`，简体中文、繁体中文与英文日期行均保持单行，页面横向溢出均为 `0`，控制台零错误或警告；截图位于 `output/playwright/date-display/`。

## 2026-07-14 Today News Title Reveal

- 今日资讯继续使用原有卡片背景、边框、间距、标题颜色、悬停与焦点样式以及单行省略规则；仅当标题实际发生截断时，鼠标悬停原文字或键盘聚焦卡片会在相同位置浮出完整标题，不改变列表高度，也不影响点击、右键菜单或行内操作。
- 今日事件中的资讯标题现已统一为单行省略，并复用同一套原位完整标题浮层；灵感卡片仍保留原有双行标题，不受影响。
- 完整标题使用纯文本渲染，并复用当前主题的表面、边框、阴影、字体和强调色；浮层会根据视口空间向下或向上展开，滚动、缩放、失焦或鼠标离开后关闭。触屏点击不激活悬停浮层，减少动态效果下取消位移和延迟。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844` 的短标题、长标题、首尾行、鼠标与键盘状态；三个视口横向溢出均为 `0`，控制台零错误或警告，截图位于 `output/playwright/today-title-reveal/`。

## 2026-07-13 Progressive Decoupling

- `assets/client/app.mjs` 保持最小启动入口；Dashboard 数据编排、Shell、设置、AI 权限、状态概览、书签、摘要、效率面板与每日视图均使用工厂模块组装。
- DOM 查询按 shell、dashboard、settings、overlay 四组公开；各 controller/View 只接收对应元素集合。
- `extension/service-worker.mjs` 保持最小事件入口；设置事务、Dashboard payload、权限、刷新、AI、Reader/preview 与维护工作流位于 `extension/runtime/`。
- `createExtensionRuntime({ chrome })` 在工厂闭包内完成组装，并公开可测试的消息、alarm、bookmark 与 permission handlers；刷新 timer 不再使用模块级共享变量。
- Reader 已拆为网络读取、缓存策略、错误分类和文档提取模块；原 `reader.mjs` 保留兼容 facade。
- Playwright 安全本地预览验证 `1280×800` 设置页、`1440×1000` Dashboard 和 `390×844` Dashboard；三种视口横向溢出均为 `0`，控制台错误为 `0`。
- `node tests/extension.mjs` 是统一测试入口，并直接覆盖 activity-store 持久化、Dashboard 异步旧响应丢弃、runtime handler 接口及各领域测试。
- 本轮截图：`output/playwright/settings-1280x800.png`、`output/playwright/dashboard-1440x1000.png`、`output/playwright/dashboard-390x844.png`。

## Browser Verification

UI 或交互修改后，在安全本地预览或加载已解压扩展中验证：

- 视口：`1280×800`、`1440×1000`、`390×844`。
- 文档、设置弹窗、引导页和 Reader 横向溢出均为 `0`。
- 控制台无错误；刷新、搜索、换批、右键菜单、稍后读、已读和跨重载持久化正常。
- 权限只能由用户操作触发；拒绝权限后页面保持可用，非本地 HTTP 不进入 Reader。网站权限汇总在设置页可见，全部授权后不再显示批量按钮，Chrome 权限增删与页面重新聚焦均会自动同步。
- 深色、浅色、系统模式及三种语言均保持现有信息终端布局和断点。
- 有意保留的截图写入 `output/playwright/`。

## Release Verification

```powershell
.\scripts\verify-extension.ps1 -Package
```

打包脚本从 `manifest.json` 读取版本，使用允许列表复制文件，并在压缩前拒绝以下内容：

- `dashboard-cache/`、测试、输出、隐藏配置或非允许列表文件。
- 疑似 API 密钥、本机绝对路径和远程脚本。
- 非 Manifest V3 清单或与显式版本参数不一致的版本。

发布脚本要求 PowerShell 7 与 Node.js 20+。打包模式会拒绝未替换占位和未配置或不匹配的 `REQUIRED_SUPPORT_URL`。它在覆盖已有版本化 ZIP 前完成环境和输入检查，按稳定路径顺序与固定时间戳创建 ZIP，并同时生成 `.zip.sha256` 与包含逐文件 SHA-256、可选 `GITHUB_SHA` 的 `.manifest.json`。Windows CI 固定使用 Node.js 20 和 `pwsh` 运行验证/打包并上传这三项产物；没有真实支持 URL 时按设计阻止发布。

上传前仍需人工确认 ZIP 根目录包含 `manifest.json`，隐私/支持页面已发布，商店文案、权限说明和实际行为一致。

## 2026-07-11 Quality Pass

- 修复嵌套文件夹突破分类上限、资讯/灵感复用同一文件夹、单源 `0` 退回 5、Feed URL 实体未解码和 URL 端口丢失。
- Feed、Reader、AI 和图片搜索统一使用限时、限流读取；来源失败与空来源分开记录。
- 设置响应只包含已知字段；反馈和客户端状态写入增加大小、类型与动作校验。
- AI 配额改为串行预留，失败调用释放同日预留；客户端状态改为 50ms 合并写入和退避重试。
- 将设置存储、网络、配额、客户端状态、AI 搜索控制器、Reader 控制器和纯分页/随机逻辑拆为独立模块，并移除未使用导出。
- 来源刷新超过 80 个时使用持久化游标轮转；删除或屏蔽来源会清理旧缓存，未轮到的有效来源保留上次结果。
- Reader 在读取重定向正文前复核最终来源权限；AI 与 Brave POST/请求不跟随重定向。
- Brave 图片兜底从占位路由改为真实的有界搜索、筛选、负缓存与客户端失败隔离。

## 2026-07-11 Quality Pass 2

- 刷新协调器在首次异步读取状态前同步占位，消除双刷新窗口；清缓存同时失效旧代际并重置来源游标。
- 来源刷新失败不再删除上一版成功文章，空来源不再计为失败；IndexedDB 分类清理和裁剪改为单事务完成。
- Reader 缓存与 AI URL 搜索缓存按当前精确来源权限复核；权限撤销错误不再被旧 Reader 缓存掩盖。
- Chrome Sync 设置公开写入统一串行，事务回调使用内部写入避免自锁；预览搜索对同一键复用在途请求，缓存写失败不影响有效结果。
- 设置弹窗只提交相对打开快照的变更，取消会恢复独立副本并清除密钥草稿；异步设置动作使用会话代际防止旧响应串入新弹窗。
- 今日内容选择器改为先过滤已看再截断，修复第 34 条以后内容无法补位以及优先级 `0` 被当成缺失；同 key 且内容未变的卡片会复用 DOM。
- Feed 缓存缺失或为空时保持真正的空状态，不再把资讯书签名称合成为资讯卡片；自动化测试覆盖缺失、空和有效缓存三种情况。
- HTML 来源页只有在明确标记为文章时才允许成为资讯；普通网站首页无 Feed、无文章链接时返回空结果，并补充 `/detail/` 新闻链接识别。
- 资讯准入增加不依赖 AI 的本地过滤：移除明确的首页、关于、联系、登录、隐私、条款、招聘、下载、搜索、广告与赞助条目；栏目 URL 必须带具体文章路径或 ID，同时保留无摘要、无日期但链接和标题正常的稀疏资讯。
- 验证脚本会运行全部顶层测试、递归检查本地模块与导入，并校验 Manifest、本地化、HTML 资源和发布包绝对路径。
- 合成数据浏览器回归通过 `1280×800`、`1440×1000`、`390×844`：文档与设置弹窗无横向溢出，设置取消、键盘事件隔离和 keyed DOM 复用正常，控制台零错误。

## 2026-07-11 Release Hardening

- OpenAI 密钥、服务地址、调用方式、模型与凭据代际改为同一条本机原子 Provider 记录；旧本机密钥安全迁移，Brave 密钥继续独立保存在扩展本地。
- 书签同意、首次引导和 AI 数据传输同意改为设备级本机状态；同意必须匹配当前版本，AI 同意还必须匹配当前 Provider origin。
- Provider origin 改变时界面会撤销旧勾选并要求重新确认；测试连接、正常 AI 调用和缓存身份均使用当前本机 Provider 代际。
- Feed 与 Reader 禁止自动重定向；Feed 显式省略凭据和 Referer。权限列表会区分当前需要、宽泛覆盖与遗留 origin，并检查权限 API 的真实布尔结果。缓存提交已串行化且带失效代际；撤权会按 Feed、AI、Brave、Reader 能力定向清理，Feed、摘要、问答与预览还会在读取时按当前权限 fail-closed 复核。
- 三语引导、隐私政策、商店隐私字段和审核说明补齐公共 Feed、Chrome Sync、AI URL/API key、远程图片请求与 Limited Use 披露。
- 发布版本提升至 `26.1.5`；PowerShell 7/Node.js 20 发布门禁、确定性 ZIP、SHA-256/逐文件清单侧车和 Windows CI 已建立。真实支持入口固定为 `https://github.com/sunpanran/Ampira/issues`，当前版本发布包仍需通过同值环境变量与完整发布门禁生成。

## 2026-07-11 Heading Rule Cleanup

- 移除 `DASHBOARD`、`SIGNAL FEED`、`ARCHIVE INDEX` 上方会在英文模式重复含义的本地化文案；保留的装饰横线加长并与标题左缘对齐，首次引导步骤文案不受影响。
- 英文模式浏览器回归通过 `1280×800`、`1440×1000`、`390×844`：三处横线宽度分别为 128px、144px、96px，文案为空，文档横向溢出为 0，控制台零错误。
- 有意保留的截图位于 `output/playwright/heading-rules-en-*.png`。其余顶层测试与全部浏览器模块语法检查通过；`favicon` 现作为可选权限单独断言，必需权限仍保持 `bookmarks`、`storage`、`alarms`。

## 2026-07-11 Permission UX Pass

- 将新标签页接管与网站访问权限拆成两个明确区域，说明精确域名权限可能服务于资讯来源、AI 服务与图片搜索。
- 移除无可见反馈的手动权限刷新按钮；权限事件与页面重新聚焦会自动重新读取 Chrome 权限。
- 批量授权按钮只在存在未授权必需域名时出现，并显示待授权数量；全部授权、无需授权、处理中、拒绝与失败状态均在设置页内联显示。
- 增加权限 UI 纯状态模型与回归断言，覆盖部分授权、全部授权和仅有遗留权限的批量操作状态。
- 合成权限状态浏览器回归通过 `1280×800`、`1440×1000`、`390×844`：部分授权时显示精确待授权数量，全部授权后批量按钮隐藏，三个视口的文档与设置弹窗横向溢出均为 `0`，控制台零页面错误；截图位于 `output/playwright/permission-settings-*.png`。

## 2026-07-11 AI Setup Gate

- AI 设置改为分阶段开放：服务地址可先编辑；确认数据传输说明后才能申请当前精确域名；授权成功后才开放密钥、调用方式、模型、额度和连接测试。
- Provider origin 改变、授权被拒绝或权限被撤销时，AI 表单立即重新锁定；旧 origin 的异步权限结果不能解锁当前草稿。
- Brave 图片搜索授权保持独立，不受 AI 正文传输同意控制；未授权 AI origin 时仍可移除已保存密钥和保存其他设置。
- 合成本地权限回归覆盖同意前、待授权、已授权和切换 Provider 后重新锁定；`1280×800`、`1440×1000`、`390×844` 均无横向溢出，三种语言的阶段提示完成抽查。截图位于 `output/playwright/ai-setup-*.png`。
- 发布前仍需在隔离的解压扩展中复核真实 Chrome 权限弹窗、拒绝、外部撤权与页面重新聚焦；这些路径同时由前端状态、权限事件和后台保存校验保持 fail-closed。

## 2026-07-11 Bookmark Favicons

- 书签、Feed 与 Reader 共用 Chrome 原生 `/_favicon/` 地址；页面 URL 通过 `URLSearchParams` 编码，固定请求 32px 图标，失败或非 HTTP(S) URL 回退到打包的 `favicon.svg`。
- `favicon` 声明为可选权限；首次引导的主授权按钮会在同一次明确用户手势中申请当前来源与网站图标权限，不再显示重复的独立图标按钮；“设置 → 浏览器”保留为重试与管理入口，授权变化立即重绘，用户可随时撤销，不会因扩展升级新增警告权限而停用已有安装。
- 不申请 `tabs` 或 `history`，不使用第三方 favicon 服务，不扩大 CSP 或主机权限。
- 隔离 Chromium 回归覆盖未授权本地图标回退、预置授权后的真实 32px `/_favicon/` 渲染、非网页 URL 回退，以及 `1280×800`、`1440×1000`、`390×844` 三种视口；无横向溢出或控制台错误，截图位于 `output/playwright/favicon-*.png`。发布前仍需人工确认原生授权弹窗、拒绝与撤销流程。

## 2026-07-11 Original Inspiration Images

- 今日灵感改为原站优先：用户授权对应灵感站点的精确 origin 后，后台只读取页面 HTML 前 1MiB 的惰性元数据，依次检查 Open Graph、Twitter Image、`image_src`、JSON-LD、图片预加载、响应式 `picture/srcset` 与语义正文主图；不执行页面脚本、不携带凭据或 Referer，并继续禁止自动重定向。
- 原站图路径与 Brave 配置完全解耦；仅在原站未授权、无可用主图、读取失败，或浏览器实际加载原图失败时才进入 Brave-only 兜底。没有 Brave 密钥或关闭兜底时，已授权原站图仍正常工作。
- 原站与 Brave 使用独立缓存身份和权限复核；原站身份提升到 v3 以主动绕过旧版解析器留下的无图缓存，原站命中保留 24 小时、无图结果仅保留 2 小时，未改变的 Brave v2 正负命中继续保留 24 小时以避免不必要地消耗搜索额度。原站缓存按书签 URL 与站点权限清理，Brave 缓存按 URL、标题、开关、密钥和 API origin 清理，任一权限变化都会失效客户端空结果；书签被删除、改名、移出灵感文件夹或切换灵感文件夹时会立即清除不再匹配的预览记录，Brave 设置或密钥变化也会单独清除 Brave 记录而保留原站记录。
- 权限页、首次引导、三语设置文案、隐私页、商店隐私实践、审核说明和提交清单均补充灵感原站元数据用途及 Brave 仅兜底说明。
- 自动化覆盖元数据属性顺序、JSON-LD、预加载与响应式图片、正文懒加载主图、1MiB 有界扫描、相对 URL、危险协议与私网字面地址、原站命中时零 Brave 请求、无权限/无图/网络失败、正负缓存、过期重试、并发去重、缓存写失败、非灵感 URL 拒绝，以及原图 DOM 加载失败后的 Brave-only 重试。完整 `verify-extension.ps1` 通过。

## 2026-07-12 Original Image Coverage

- 原站解析覆盖从声明式社交元数据扩展到 JSON-LD `ImageObject`、`link[rel=preload][as=image]`、`picture/source/srcset` 和常见懒加载属性；语义评分优先正文、主图、高优先级和大尺寸候选，并排除导航、页脚、头像、Logo、像素图与占位图。
- HTML 前缀上限由 512KiB 提升到 1MiB、超时由 8 秒调整为 10 秒，仍保持有界读取、无凭据、无 Referer、禁止自动重定向和精确 origin 权限复核。
- 原站图片预览缓存身份提升到 v3，确保升级后不被旧版 24 小时无图缓存挡住；原站无图缓存缩短为 2 小时，原站命中时仍不会读取 Brave 设置或发出 Brave 请求，Brave v2 缓存则继续复用以保护搜索额度。

## 2026-07-12 Daily Inspiration Preload

- 当天固定池的 15 张灵感卡（3 批 × 5 张，包含全部“换一换”结果）会在初次 `renderAll()` 前启动并发预览与实际图片预载，当前首屏 5 张排在队列前部；首次加载最多等待 1.6 秒，后续仪表盘更新和换一批最多等待 0.8 秒，超时后全部预载任务继续渐进完成而不阻塞其余界面。
- 预载图片继续使用 `no-referrer`，并在原站图片加载失败时于卡片渲染前触发 Brave-only 兜底；已预载图片在卡片中使用 eager/high-priority 加载，避免再次被 lazy loading 延后。
- 控制器回归覆盖 15 张完整日选池、日选去重、实际图片预载、超时放行和原站失败后的 Brave 预载；完整扩展测试与语法检查通过。
- 隔离临时 Chromium 配置使用 15 条合成灵感书签完成三次换批及 `1280×800`、`1440×1000`、`390×844` 回归：三批各 5 张且合计 15 张唯一卡片，文档横向溢出为 `0`，控制台零错误；截图位于 `output/playwright/daily-inspiration-preload-15-*.png`。
- 隔离临时 Chromium 配置使用合成资讯与灵感书签完成 `1280×800`、`1440×1000`、`390×844` 回归：权限列表包含灵感精确 origin，首次引导与权限说明正确，文档和设置弹窗横向溢出均为 `0`，控制台零错误；截图位于 `output/playwright/original-preview-*.png`。
- 发布前仍需在可操作浏览器外壳中人工接受一次灵感站点的 Chrome 原生可选权限确认，并复核真实站点的原图、防盗链失败与 Brave 兜底；原生浏览器确认气泡不属于页面 DOM，当前无头回归只验证到申请列表和用户手势入口。

## 2026-07-13 Progressive Onboarding

- 首次引导压缩为 3 步：产品介绍、书签文件夹选择、权限说明与可选授权；沿用原有标题、正文与按钮文案，只调整步骤编号和顺序。AI Key 与汇总页退出首次流程，AI 继续从“设置 → AI”按需配置。
- 文件夹保存或跳过后才计算当前精确来源域名；权限仍在首次进入首页前，通过同一次明确用户手势申请网站来源与 `favicon`。拒绝系统弹窗会停留并允许重试，明确选择暂不授权则进入受限模式。
- 书签文件夹继续通过现有设置保存边界持久化，并阻止资讯与灵感选择同一文件夹；本地非敏感进度只记录当前处于文件夹或权限步骤，完成后清空。已确认书签访问的旧版未完成流程安全回到文件夹步骤。
- 自动化覆盖 3 步结构、文件夹先于权限、两个可跳过入口、AI 凭据退出 onboarding、favicon 合并申请及直接完成激活。隔离合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`：三个步骤的文档和弹窗横向溢出均为 `0`，进度均为 3 段，跳过权限后完成态正确，控制台零错误；截图位于 `output/playwright/onboarding-progressive/`。
- 发布前仍需在加载的未打包扩展中人工确认来源与 `favicon` 的 Chrome 原生授权气泡，以及拒绝后停留、重试、跳过和最终首次刷新行为。

## 2026-07-12 First-frame Color Mode

- 页面在样式表加载前通过同源 `theme-bootstrap.mjs` 同步恢复最近一次 `system`、`dark` 或 `light` 选择；无缓存或存储不可用时使用产品默认深色，避免首帧误跟随系统后产生白闪或黑闪。
- 正常设置加载和外观预览会刷新轻量 `localStorage` 主题缓存；完整设置仍以 Chrome Sync 后台存储为准，缓存不包含密钥或其他用户设置。
- 扩展测试、服务工作线程及全部客户端模块语法检查通过；仍需在加载的未打包扩展中分别复核“系统浅色 + Ampira 深色”和“系统深色 + Ampira 浅色”的硬刷新首帧。

## 2026-07-12 Sparse News Ranking

- Feed 或网页链接兜底条目不再把抓取时间写入发布时间；标记为“时间未核验”的条目在时间排序中不再被当作刚发布内容。
- 同时缺少已核验时间、图片和正文摘录的空壳资讯会在“时间”和“重要”两种排序中统一排到具有可读内容的条目之后；其余资讯仍按 AI 重要度、热度和真实发布时间排序。
- 非 AI 的资讯重要度改为可解释的综合评分：Feed 位置只保留最多 6 分的轻量信号，主要依据已核验的新鲜度、内容完整度和公共影响词；促销、购买指南、开箱、教程及软性推荐会被降权。旧版按 Feed 位置固化的缓存分数会自动按新策略重算。
- 自动化增加高热度、高 AI 分数空壳资讯的回归断言；完整扩展测试、服务工作线程及全部客户端模块语法检查通过。

## 2026-07-12 Homepage News Admission

- 资讯准入不再依赖首页标题是否恰好等于站名或域名；根路径、`index.*` 以及单独的 `/news`、`/blog`、`/updates` 等资讯栏目入口会按 URL 结构直接过滤，查询参数和追踪参数不能再绕过判断。
- 多段文章路径继续保留，例如 `/news/specific-story` 与 `/updates/alpha`；自动化覆盖带参数根首页、索引页、栏目页以及应保留的具体文章路径。
- 真实站点 `https://80.lv/` 的首页带有 2019 年 `datePublished` 元数据；根路径和 `index.*` 现为无条件首页边界，不能再由陈旧或错误的文章时间元数据绕过。发布时间例外仅用于非根路径上的直接书签文章。

## 2026-07-12 Cross-source Event Topics

- “事件主题”不再按书签分类、公共 Feed 分类或站点直接分组；现在会在全部已授权 Feed 条目之间比较规范化标题，通过英文关键词与中英文字符二元组相似度识别同一事件。
- 中文标题额外使用浏览器原生分词比较关键事件词，能够聚合语序和措辞明显不同、但共享多个实体与数字的跨站报道；“发布”“宣布”“新款”等泛词不参与匹配，避免无关产品新闻误合并。
- 只有发布时间相距不超过 7 天且至少由两个不同站点共同报道的事件才会展示；同一站点的相似重复稿只保留得分最高的一条，不会抬高来源数或事件分数。
- 事件排序继续使用代表稿基础分，并为额外独立来源增加有限权重；单一来源即使自身分数很高也不会进入事件主题。
- 自动化覆盖中英文混合标题、中文改写标题、跨分类聚合、泛化“发布”标题隔离、同站重复稿去重和单来源高分稿排除；扩展测试、服务工作线程及全部客户端模块语法检查通过。

## 2026-07-12 Settings AI Overview Polish

- AI 运行概览改为紧凑状态面板：增加克制的强调色状态轨与信号标记，强化标题、说明、操作和运行指标的层级，同时保留既有信息终端风格。
- 今日额度与后台缓存改为独立的紧凑指标舱，数字使用等宽数字排版；“去配置”补齐悬停、按压和更明确的主操作反馈。
- 设置状态条使用更轻的独立表面和状态光点，不改变现有文案、可访问性语义或交互逻辑。
- 扩展测试、服务工作线程及全部客户端模块语法检查通过；仍需在加载的未打包扩展中复核 `1280×800`、`1440×1000` 与窄窗口布局。

## 2026-07-12 AI Search URL Classification

- AI 搜索中的 URL 在正文提取成功时继续生成文章总结；当页面没有足够的文章正文时，改用标题、站名和页面描述生成网站介绍，不再显示为空文章。
- 网站介绍提示词明确限制为已抓取的页面元数据，避免将普通网站误写成文章或补造页面未提供的信息；来源访问仍要求用户手势授予精确站点权限。
- 用户从搜索弹窗提交 URL 时会直接触发 Chrome 对该精确来源的授权弹窗；同意后继续分析，拒绝后停止请求并在原弹窗说明如何重试。普通问题不会请求网站权限。
- 扩展测试、正文提取测试、服务工作线程及全部客户端模块语法检查通过。

## 2026-07-12 AI Search Without Local Matches

- Ampira 内容搜索不再因关键词未预先命中本地资讯而跳过 AI Provider；已完成 AI 配置时，每个非网址问题都会进入 AI 回答流程。
- 有本地候选时仍优先使用已授权资讯作为上下文；没有候选时允许模型使用通用知识回答，但必须说明缺少 Ampira 本地依据，并且不得声称执行了实时网页搜索。
- AI 未配置或 Provider 调用失败时继续安全回退到原有本地搜索结果，并显示对应配置或服务错误。

## 2026-07-12 AI Provider Response Compatibility

- AI 文本解析现在兼容 Responses API 的顶层 `output_text`、嵌套 `output[].content[].text`，以及 Chat Completions 的字符串或内容数组 `message.content` 和常见旧式 `choices[].text`。
- 配置的 API 风格仍决定首选响应结构，但兼容服务返回另一种 OpenAI 文本封装时会安全回退；仅提取字符串，不把未知对象转换成可见文本。
- 自动化增加嵌套 Responses、数组式 Chat Completions 与旧式 completion 文本回归覆盖。

## 2026-07-12 Manual News Card Summaries

- 资讯卡片的“手动整理摘要”现在提交被点击文章的稳定身份，不再把整个来源误当成目标；后台读取该文章正文、复验文章来源与 AI Provider 权限后调用已配置模型。
- 手动生成结果以纯文本摘要行写回对应 Feed 条目，并保留 `summaryStatus: ai` 到卡片模型；按钮完成后会显示新摘要并停止重复提示。
- 手动整理继续不占每日自动整理额度；正文、网址和标题只会发送到用户已确认并授权的 AI Provider。
- 连接测试的输出预算由 20 提高到与可用搜索路径相同的 900，文章摘要提高到 1200，今日简报提高到 900；避免推理模型在输出可见文本前耗尽预算。
- 手动整理不再主动申请或强制要求文章域名权限：已有精确文章权限时读取全文，没有时直接使用已授权 Feed 中的标题与摘录；AI Provider 域名授权与数据传输同意仍为必需条件。

## 2026-07-12 Automatic News Card Summaries

- 资讯刷新提交 Feed 后会自动选择尚未 AI 整理且具有摘录的高优先级卡片，通过已确认、已授权的 Provider 生成摘要并逐条写回缓存。
- 自动队列使用共享每日 AI 额度，单轮会按今日剩余额度持续处理全部可整理资讯；候选用完、额度用完、配置变化、权限变化、刷新失效或首次 Provider 错误时才停止。
- 下一次 Feed 抓取会按稳定文章身份保留同一 Provider 生成的 AI 摘要；Provider 改变时不继承旧摘要。手动整理仍不占自动额度。

## 2026-07-12 AI Feature Completion Audit

- 今日简报在资讯刷新后若缺少当天有效 AI 结果会自动生成；同日、同语言、同 Provider 且来源仍获授权的简报会保留，不再被每轮 Feed 抓取重置为本地占位内容。
- Provider 返回的简报正文现在显示在本地重要／追踪／跳过分栏之前；AI 文字与本地可点击文章结构同时保留，不再出现后台生成成功但前端隐藏结果的情况。
- “自动额度”现在只用于自动今日简报与自动卡片摘要；AI 搜索、网址总结、手动简报和手动卡片整理均不占自动额度，与设置说明一致。
- 自动与手动卡片摘要只使用 Feed 自带摘录，发送长度限制为 2,000 字符；即使文章精确域名已授权也不读取或发送正文。自动 Provider 失败会在缓存状态中明确显示暂缓并于下轮重试。
- Ampira 内容问答增加浏览器原生分词和中文二元组匹配，并按标题、摘录、来源分别加权；没有相关本地候选时直接返回本地无结果，不再向模型发送空上下文。
- 自动化增加自动简报触发、手动调用不占自动额度、卡片摘要仅使用有界 Feed 摘录、自然语言检索和简报正文渲染断言。完整扩展测试、服务工作线程及全部浏览器模块语法检查通过。
- 安全合成状态浏览器回归验证 `1280×800`、`1440×1000`、`390×844`：AI 简报两段正文均可见，文档横向溢出为 `0`，控制台无警告或错误；截图位于 `output/playwright/ai-functions/digest-*.png`。

## 2026-07-12 AI Card Title Integrity

- 未整理卡片使用 Feed 原文章标题；整理成功后优先显示独立缓存、最长 64 字的 AI 精炼标题，Feed 标题仍保留为无效或缺失 AI 标题时的回退值。
- AI 卡片整理要求独立的“标题：”输出行；该行不会混入摘要正文。`## 总结`、`**核心内容**` 等 Markdown 与结构标签仍会在渲染和缓存前清理。
- 新生成与既有缓存摘要都会剥离 Markdown 标题、列表符号、粗体标记，以及中英文“总结／核心内容／重要性／行动建议”等纯结构标题；带冒号的结构前缀会移除但保留其后的实际内容。
- 自动化增加 AI 标题提取、64 字上限、独立字段缓存、Feed 标题回退、后台生成清洗和前端旧缓存清洗断言。
- `390×844` 安全合成旧缓存回归确认卡片标题为原文章标题、摘要正文保留、`## 总结`／`## 核心内容` 无泄漏、横向溢出为 `0`、控制台无警告或错误；截图位于 `output/playwright/ai-card-title/card-title-390x844.png`。

## 2026-07-12 Automatic AI Runtime Status

- “设置 → AI 服务 → 运行概览”新增持久化自动整理状态，区分生成今日简报、整理资讯卡片、完成、没有候选、自动额度用完、Provider 未就绪和错误暂缓。
- 运行中显示本轮已处理／总数与实时今日用量；结束后显示最近运行时间。状态保存在扩展本地 state 记录中，Service Worker 休眠或设置面板稍后打开时仍可追溯。
- 每个自动卡片完成后更新进度；清缓存重置运行记录，重置今日额度会把额度停止状态恢复为等待下次刷新；刷新异常不会留下永久“运行中”。
- `1280×800` 与 `390×844` 安全合成运行状态回归确认“正在整理资讯卡片”、`1/3` 本轮进度和 `2/50` 今日用量均可见，横向溢出为 `0`，控制台无警告或错误；截图位于 `output/playwright/ai-auto-status/running-*.png`。
- 未就绪状态进一步区分未保存 API Key、未确认数据传输和未授权 Provider 域名；连接测试使用表单草稿但不会持久化，因此测试成功且存在未保存更改时会明确提示点击“保存设置”。
- 保存完整 AI 配置或为已保存 Provider 新增域名授权后会立即启动一轮强制资讯刷新，无需用户再手动点击“缓存”；同时保存书签来源与 AI 配置时只启动一次，避免重复刷新。
- `1280×800` 未保存 Key 合成状态确认显示“等待保存 API Key”和连接测试不保存密钥的明确操作说明，横向溢出为 `0`、控制台无警告或错误；截图为 `output/playwright/ai-auto-status/missing-key-1280x800.png`。

## 2026-07-12 AI Service Layout Grouping

- AI 设置保持既有配色、边框、字号和控件样式，仅重排信息结构：运行概览置顶；Provider 授权、数据同意与模型参数归为连续主区块；Brave 图片搜索作为独立可选区块。
- 图片搜索区在桌面使用“密钥与授权／开关与操作”双列布局，在窄屏回落为单列；Provider 主区继续使用既有两列字段网格与移动端单列规则。
- 新增简短组标题和说明，明确 Provider 是 AI 主服务、Brave 仅为原图失败后的可选兜底，降低配置项挤在同一层级造成的认知负担。
- `1280×800` 与 `390×844` 安全合成回归确认两个分组均可见，桌面双列和窄屏单列切换正确，横向溢出为 `0`、控制台无警告或错误；截图位于 `output/playwright/ai-service-layout/service-*.png`。

## 2026-07-12 Daily Brief Simplification

- “今日简报”移除“今日最重要／值得追踪／可以跳过”三列标题卡，只保留 AI 生成的文字简报与重新整理操作。
- 每日简报的同一次 AI 调用会按输入序号生成最多 5 个精炼事件标题；“今日事件”优先显示最长 64 字的 AI 标题，同时保留 Feed 原标题作为缺失或无效输出时的回退，不增加 Provider 调用次数。
- 简报正文去除紫色填充和左侧强调线，直接使用卡片原有表面与文字层级，避免重复嵌套视觉。
- “事件主题”改为“今日事件”，直接按今日简报的 `importanceScore` 排序展示前三条新闻；点击仍打开对应资讯。
- “今日事件”沿用原事件主题卡片的容器、列表行、双行信息和分数徽标样式，只替换标题、数据选择与打开目标。
- 服务工作线程不再构建或返回跨来源相似标题聚合，相关聚合函数、辅助算法、旧文案与测试一并移除。

## 2026-07-13 Stable Card Updates

- 手动整理资讯时保留原卡片根节点和未变化的缩略图，只原位更新标题、摘要与操作区，避免整张卡片及图片重新挂载造成闪烁。
- 缓存完成后，同一条资讯即使分数、摘要或其他数据发生变化也沿用现有卡片节点；新增和移除资讯仍保留原有进出场动效。
- 整理中的反馈改为轻微边框与表面状态，不再让整张卡片瞬间降低透明度；卡片根节点复用后，点击、键盘打开和右键菜单始终读取最新数据。

## 2026-07-13 AI Search Analysis Format

- AI 搜索与普通资讯整理改用不同的信息结构：文章总结拆为核心判断、内容脉络、关键信息、阅读价值、下一步与信息边界；本地问答拆为直接回答、本地依据、补充判断与建议动作。
- 文章总结目标长度提高至约 500—800 个汉字，手动 AI 搜索输出预算提高到 1400 tokens；搜索缓存版本同步更新，避免继续展示旧提示词生成的短回答。
- 展示层以安全结构化 DOM 渲染分析栏目和编号，不使用 `innerHTML`；新旧回答中的 Markdown 标题、粗体、代码块、引用与列表标记都会在展示前清理。
- `1280×800`、`1440×1000` 与 `390×844` 安全合成回归确认 6 个分析栏目完整生成、Markdown 标记不可见、横向溢出为 `0`、控制台无警告或错误；截图位于 `output/playwright/ai-search-analysis/ai-analysis-*.png`。
- 安全合成浏览器回归确认手动整理保留卡片根节点、缓存更新同时保留卡片与缩略图；`1280×800`、`1440×1000`、`390×844` 均无控制台错误或警告，窄屏横向溢出为 `0`。截图位于 `output/playwright/card-stability/stable-*.png`。

## 2026-07-13 Unreadable News Filtering

- “时间未核验”且没有 Feed 摘录或摘要正文的条目不再进入资讯缓存，也不会从既有缓存渲染为热门资讯、今日内容或阅读操作项。
- 未核验但具有可读摘录的资讯继续保留；时间已核验的文章即使 Feed 未提供摘要，也仍可作为可打开的有效文章展示。
- 自动化覆盖不可读未定时条目的准入边界，并确认前端在映射卡片前应用同一规则。
- 安全合成浏览器回归验证 `1280×800`、`1440×1000`、`390×844`：不可读未定时条目均未进入 DOM，有摘录的未定时条目仍显示，热门资讯卡片数为 1，横向溢出为 `0`，控制台无错误或警告；截图位于 `output/playwright/unreadable-filter/filter-*.png`。

## 2026-07-13 New-tab Loading Stability

- 头图启用、固定、全屏与公开图片地址会以不含隐私数据的轻量提示同步恢复；新标签页在首帧直接挂载原有头图节点和原有状态类，不新增或修改视觉规则。
- 首屏卡片不再等待灵感缩略图预加载；等待期间使用独立骨架占位，数据返回后立即按原有静态卡片样式替换并执行错峰入场动画，缩略图在后台渐进补齐。骨架和动画不修改 `.daily-card`、`.news-list-card` 或 `.summary-card` 静止后的背景、边框、圆角、尺寸、间距、字体或排版。
- 日期时间栏预留原有单行文字高度，避免首秒写入时间后推动标题区布局；该规则不作用于卡片。
- 卡片入场仅在动画态从下方 `8px` 淡入并轻移到原位，使用缓出曲线和既有错峰延迟；动画结束后不保留任何影响静态卡片外观的样式。
- 扩展测试、Service Worker 语法检查与全部浏览器模块语法检查通过。

## 2026-07-13 Settings Content Redesign

- 设置弹窗外壳、标题栏、底部保存区和七个选项卡保持不变；七页内容统一改为“页面简介、语义分区、紧凑设置行”，连续开关不再各自占用一张卡片。
- AI 运行概览移入 AI 面板并压缩为状态行；概览操作会聚焦当前可执行的配置步骤。Provider 同意、精确域名授权、字段解锁与独立 Brave 授权顺序保持不变。
- 资讯、书签、外观、浏览器和屏蔽页按实际任务重新分组；浏览器页继续明确新标签页由扩展安装状态控制，不提供可写开关。三语分区标题同步补齐。
- 自动化增加七个 tab/panel 对应、关键控件 ID 唯一、AI 概览归属和浏览器无伪开关断言；扩展测试、Service Worker 与全部浏览器模块语法检查通过。
- Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`：七页的文档、表单和活动面板横向溢出均为 `0`；英语、简体中文和繁体中文窄屏检查通过，控制台零警告与错误。
- AI 概览聚焦、同意后的权限门控、固定头图与全屏依赖状态均通过真实交互验证。截图位于 `output/playwright/settings-redesign/`。
- 资讯三项数值控件统一顶部对齐；AI 图片搜索兜底的两栏统一为“字段标题、主控件、说明空间、操作按钮”四行网格，使两侧标题、输入框与开关、操作按钮分别严格平行，窄屏恢复自然单列。对齐复核截图为 `settings-aligned-1440x1000.png` 与 `settings-image-grid-aligned-1440x1000.png`。

## 2026-07-13 Title Rule Removal

- 首页 `DASHBOARD`、`SIGNAL FEED`、`ARCHIVE INDEX` 标题上方的渐变装饰线已移除；首次设置步骤标题也不再附带同类渐变线。
- 清理三个仅用于绘制装饰线的空节点，保留既有标题字号、间距、网格纹理和响应式布局。
- `1280×800`、`1440×1000` 与 `390×844` 本地安全预览均无横向溢出，控制台无错误或警告；截图位于 `output/playwright/title-lines-*.png`。

## 2026-07-13 AI Runtime Overview Polish

- AI 运行概览内部改为两个紧凑指标与一个宽自动整理状态块；今日额度明确显示已用与剩余数量，后台缓存显示刷新进度或上次完成时间。
- 后台缓存仅在实际更新时显示本地旋转同步图标；完成、空记录和连接暂停状态会停止并隐藏动画，状态文字继续承担无障碍说明。
- 自动整理继续复用既有运行、完成、额度和异常阶段颜色；桌面端使用两窄一宽布局，窄屏保留两个指标并排并让自动整理独占下一行。
- 扩展测试、Service Worker 和全部浏览器模块语法检查通过；旋转状态在减少动态效果下缩短为单次极短动画。
- 安全合成浏览器回归覆盖 `1280×800`、`1440×1000`、`390×844`，三语横向溢出均为 `0`、控制台零错误或警告；截图位于 `output/playwright/ai-runtime-overview/`。

## 2026-07-13 Manual Automatic-AI Retry

- 用户主动点击“缓存”时会先读取来源并提交最新资讯缓存，不再等待旧缓存的自动整理；自动简报与卡片整理统一在新缓存提交后运行，自动 alarm 和首次非强制刷新沿用相同优先顺序。
- 资讯来源整体读取失败时会回退到仍通过当前精确来源权限校验的既有缓存，继续生成缺失的今日简报并整理待处理卡片；来源刷新状态仍明确保留为失败，不会因 AI 回退成功而误报缓存更新成功。
- “设置 → AI 服务 → 运行概览”在“去配置”左侧新增同功能“缓存”按钮；两个入口共享强制刷新消息、运行中禁用状态、同步图标和“缓存中”标签，便于从异常状态直接重试。
- 自动整理仍受已保存的 Provider 配置、精确域名授权、数据传输同意与每日自动额度约束，缓存内容会按当前来源权限过滤后才进入重试。
- 合成数据浏览器回归覆盖 `1280×800`、`1440×1000`、`390×844` 设置页：页面、弹窗和按钮组横向溢出均为 `0`，控制台零错误与警告；设置页按钮确认发送 `POST /api/refresh?force=1`，截图位于 `output/playwright/manual-ai-retry/`。

## 2026-07-13 Article Explanation Context Action

- AI 已完成同意、密钥与服务域名授权时，资讯卡片右键菜单新增“解释文章”；书签、灵感与未配置 AI 的状态不显示该操作。
- 操作直接打开 AI 搜索并以文章网址运行现有正文提取与文章解释流程；文章网站仍只会从用户手势申请精确域名权限。
- 简体中文、繁体中文与英文菜单文案同步补齐。
- 扩展测试、Service Worker 与全部浏览器模块语法检查通过；真实浏览器反向验证确认未配置 AI 时不显示该菜单项。
- `1280×800`、`1440×1000` 与 `390×844` 回归均无横向溢出、菜单保持在视口内且控制台零错误或警告；截图位于 `output/playwright/article-explanation/`。

## 2026-07-13 Direct AI Answers

- 普通提问以无栏目、无编号的纯正文展示面向用户的最终回答，不再生成或展示“本地依据、补充判断、建议动作”等模型工作记录；问候会直接简短回应。
- 网页与文章解释仍保留结构化阅读栏目，不受普通问答简化影响。
- 展示层会从旧格式回答中仅提取“直接回答”，避免缓存或未完全遵循新提示词的模型再次露出内部工作记录；搜索缓存版本同步更新。
- 安全合成浏览器回归覆盖 `1280×800`、`1440×1000`、`390×844`：旧四栏回答仅显示最终正文，栏目数为 `0`，横向溢出为 `0`，控制台零错误与警告；截图位于 `output/playwright/ai-direct-answer/`。

## 2026-07-13 AI Search Opening Motion

- AI 搜索浮层打开时背景轻微淡入，搜索框从中心向两侧展开，输入区与提交按钮随后显现；既有布局、配色、尺寸与交互保持不变。
- 动效只在打开时播放，并沿用全局 `prefers-reduced-motion` 规则，在用户要求减少动态效果时缩短为单次极短过渡。

## 2026-07-13 Settings Open and Close Motion

- 设置浮层打开时背景淡入，设置面板轻微上移归位；关闭时面板轻微下沉并与背景一同淡出，延续 AI 搜索的克制动效语言。
- 关闭流程增加短暂 `closing` 状态，动效完成后才隐藏浮层；快速重新打开会取消待执行的隐藏操作，减少动态效果时则立即完成关闭。
- 浏览器回归覆盖 `1280×800`、`1440×1000` 与 `390×844`：打开、关闭、关闭途中重新打开均符合预期，横向溢出为 `0`，控制台无错误或警告；截图位于 `output/playwright/settings-motion/`。

## 2026-07-13 Header Cover Refresh Recovery

- 主应用绑定头图加载监听器后会立即核对图片完成态；刷新时即使首帧恢复脚本已从浏览器缓存加载完图片，也会补齐原有 `is-loaded` 状态，不再保持透明。
- 头图地址切换后同样核对同步完成态；失败图片继续沿用原有隐藏与布局状态清理逻辑。
- 自动化覆盖监听器绑定后的完成态恢复，防止刷新竞态回归。
- Chromium 刷新回归覆盖 `1280×800`、`1440×1000` 与 `390×844`：三个视口均保持 `is-loaded`、图片可见、横向溢出为 `0`，控制台零错误与警告；截图位于 `output/playwright/header-cover-refresh/`。

## 2026-07-13 Dashboard Panel Entrance

- 首次骨架加载完成后，今日事件、今日简报、阅读队列与右侧三列工作台按短间隔错峰淡入，并从下方 `8px` 回到原位。
- 今日简报生成完成后不再保留独立“重新整理”按钮；简报正文自身支持鼠标点击与键盘聚焦触发重新整理。单行 AI 简报会按原有中文分号和句号拆成最多四段、保留全部文字，并在卡片可用高度内均匀排布，减少底部空白且不增加背景、边框或装饰。
- 安全合成浏览器回归覆盖 `1280×800`、`1440×1000` 与 `390×844`：四段正文完整显示，正文内部溢出与页面横向溢出均为 `0`，末段距卡片底部约 `15–16px`；点击正文后新简报成功原位替换，独立整理按钮数量为 `0`，控制台零错误与警告。截图位于 `output/playwright/digest-text-layout/`。
- 动效只作用于首次入场，不改变卡片的背景、边框、圆角、尺寸、间距或静止状态；后续数据刷新和换批继续沿用原有局部更新动效。
- 入场使用浏览器原生动画接口，结束后不保留视觉属性；系统开启减少动态效果时完全跳过。
- 面板外壳固定在骨架占位的位置，只让标题与内容区域从 `20%` 透明度、下移 `8px` 的状态柔和进入；首次后台刷新会复用内容未变化的效率卡与列标题节点，避免骨架切换时整卡跳动、空白帧和动画中途替换。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000` 与 `390×844`：六个面板外壳在动画期间的 transform 始终为 `none`，十二个直接内容区使用 `520ms` 柔和减速与 `48ms` 错峰完成，且在首次二次渲染后仍连接原节点；结束后透明度为 `1`、transform 为 `none`、无行内样式残留，三个视口横向溢出均为 `0`，控制台零错误与警告；减少动态效果模拟下目标动画数为 `0`。截图位于 `output/playwright/dashboard-panel-entrance/`。

## 2026-07-13 Summary Image Dimming

- 热门资讯摘要卡片的真实文章缩略图使用主题化深色遮罩：深色模式为 14%，浅色模式（包括跟随浅色系统）为 6%，既压低深色界面中的刺眼白底，也避免浅色界面出现灰脏感；站点图标回退态与灵感卡片图片保持原样。
- 扩展测试、Service Worker 与全部浏览器模块语法检查通过。Chrome 自动化受内部新标签页安全策略限制，仍需在加载的未打包扩展中人工复核 `1280×800`、`1440×1000` 与窄窗口布局。

## 2026-07-13 Website Shortcuts

- 每日 Dashboard 标题与效率面板之间新增单一紧凑 launcher rail；模块默认关闭，关闭或 Ampira 内容搜索激活时不占空间，开启但列表为空时提供直达“浏览器”设置区的操作。
- “设置 → 浏览器”支持名称与地址手填、新增、编辑、删除和上下排序，最多 10 个；开关关闭后仍保留并可编辑列表，所有变更继续使用全局设置草稿、Chrome Sync 保存与取消恢复边界。
- 地址统一补全裸域名为 HTTPS，仅接受 HTTPS 及 localhost/127.0.0.1 HTTP，拒绝凭据、危险协议、超长和重复地址；快捷方式使用安全 DOM、真实 `_self` 链接，不新增 manifest 或主机权限。
- favicon 已授权时仅使用 Chrome `_favicon` 接口；未授权、撤销或图标加载失败时回退到内置书签图标，不主动申请权限，也不调用第三方图标服务。
- 自动化覆盖默认值、标准化、长度与数量上限、去重、安全协议、Sync 编解码、保存白名单、列表增删改排和设置草稿独立恢复；扩展完整测试、Service Worker 与全部浏览器 ESM 语法检查通过。
- 隔离 Chromium 扩展回归覆盖默认关闭、空状态、1/10 个入口、编辑删除排序、10 个上限提示、当前标签链接语义、搜索隐藏、三种语言、深浅色、头图和 favicon 未授权/授权/失败/撤销；`1280×800`、`1440×1000`、`390×844` 横向溢出均为 `0`，最终控制台零错误与警告。截图位于 `output/playwright/website-shortcuts/`。
- 快捷方式显示开关改为复用现有 `.switch-field` 设置组件，修复无尺寸开关轨道落入下一行并遮挡名称字段的问题；真实扩展在 `820×800` 单列表单与 `900×800` 双列表单下均无元素相交或横向溢出，截图为 `shortcut-toggle-fixed-*.png`。

## 2026-07-13 Today Importance Ranking v3

- 今日资讯在每次刷新时按当前时间重算：新鲜度 `0–30`、公共影响 `0–32`、信息完整度 `0–18`、Feed 位置 `0–3`，并对促销、购买指南、教程盘点、传闻、正文空壳、未核验与异常未来时间分别执行门槛或降权；旧缓存不再冻结初次抓取时的新鲜度。
- 标题使用 NFKC、英文词、中文原生分词、中文二元组和数字实体在 36 小时窗口内聚合事件；同发布方重复稿不增加佐证，独立发布方达到 `2/3/4+` 时代表稿增加 `6/9/12` 分。数字冲突保持为不同事件，今日事件最多保留 1 条最近 24 小时跨日补位，今日资讯最多保留 3 条。
- RSS `<source>` 与 JSON Feed 作者安全元数据会写入 `publisher` / `publisherHost`，聚合 Feed 因而按实际发布方计数；抓取和权限判断仍只使用 Feed 原来源，不新增权限或第三方信誉服务。
- 每日简报升级到 schema v2 和排序策略 v3，同一次 Provider 调用最多提交 12 个已过本地门槛、事件去重且来源多样化的候选。只有全部 `RANK n` 为 `0–100` 且具有区分度时采用 AI 顺序；缺失、越界、重复或全同分时整组回退本地，合法的两行概览与精炼标题仍可保留。
- 简报缓存现在绑定本地自然日、候选指纹、排序策略、语言、Provider 与可见来源权限；日期、候选、发布方限制、Provider 或权限变化后不会复用旧 AI 顺序。个性化行为仍只保存在本地，最近 30 天按 14 天半衰期聚合；无 AI 时最多调整 `±4`，AI 可用时只作为本地同分信号。
- “设置 → 资讯”新增三语“今日单发布方上限”数值项，默认 `0`、范围 `0–10`、`0` 表示不限；今日资讯按每批 10 条应用上限，候选不足时自动补回，不留空。保存后立即使简报候选指纹失效并复用单轮刷新流程。
- 自动化覆盖缓存随时间降权、本地午夜、异常未来与未核验时间、泛词与软文、重大单来源事件、中英文聚合、数字冲突、同源重复、代表稿、跨日补位、发布方限制 `0/1/2/10`、分批放宽、聚合 Feed 发布方、AI 完整与异常输出、候选及权限失效。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`：每批 10 条今日资讯中高分主要发布方保持 2 条，今日事件显示 `4/2/1` 个独立来源，资讯与设置页横向溢出均为 `0`，控制台零警告与错误；设置控件值为 `2`、边界为 `0–10`。截图位于 `output/playwright/today-ranking/`。

## 2026-07-13 Denser Card Summaries

- 资讯卡片整理从“标题 + 2 行、最多 180 字”调整为“标题 + 3 段”：核心事实、实际影响、后续关注或信息边界。摘录充分时目标 `180–260` 字且不少于约 `140` 字；摘录不足时允许缩短，禁止为填充空间而补写、推测或重复。
- “核心事实、实际影响、后续关注、信息边界”只用于指导内容组织，不作为可见栏目名；三语提示词明确要求直接写自然正文，缓存前的清洗器也会移除模型偶尔输出的对应中英文标签或前缀。
- 生成结果在写入缓存前执行 Unicode 安全的 `280` 字硬上限，最多保留 3 段；精炼标题继续保持 `64` 字硬上限，Provider 输出预算不变，因此不增加单次调用或自动额度消耗。
- 卡片可见摘要预算从 `180` 提升到 `280` 字，视觉行数由最多 3 行提升到 6 行。摘要策略升级为 v2，既有短摘要会在下一次正常缓存刷新时进入重新整理候选，当前版本摘要不会重复消耗额度。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`：摘要稳定显示 `5–6` 行，6 张卡片均无内部溢出，页面横向溢出为 `0`，控制台零警告与错误。截图位于 `output/playwright/card-summary-density/`。

## 2026-07-13 Motion Continuity Pass

- AI 搜索与浮动阅读器新增对称退场：关闭时保留 `open closing` 状态，背景和主体分别淡出／收拢，最长 `180ms` 后再清理 DOM 状态；关闭途中重新打开会取消旧定时器。阅读器同时新增 `180ms` 背景、`240ms` 内容入场。
- 设置页签切换使用 `160ms`、`4px` 的轻量淡入；结束监听只接收当前面板自身的 `settingsPanelIn`，避免被子元素动画提前清理。桌面导航标签改用可见性、透明度和位移连续过渡，不再随侧栏宽度变化瞬间跳显。
- 卡片换批调整为 `110ms` 退场、`240ms` 入场、每张 `12ms` 错峰且最大 `84ms`，完整换批预算约 `434ms`；右键菜单使用 `110ms` 入场，并依据点击位置从左／右、上／下方向展开。
- 所有新增 CSS 动画继续受全局 `prefers-reduced-motion` 覆盖；控制器在减少动态效果时以 `0ms` 完成关闭清理。
- 安全合成浏览器回归覆盖 `1280×800`、`1440×1000` 与 `390×844`：三个视口横向溢出均为 `0`；AI 搜索、阅读器、设置页签、导航标签、换批和右键菜单均采样到预期动画名与时长，修正测试 fixture 后控制台零错误与警告。截图位于 `output/playwright/motion-implementation/`。
- Service Worker、全部 `extension/core/*.mjs`、全部 `assets/client/*.mjs` 与测试文件语法检查通过，动效专用回归断言通过。完整 `tests/extension.mjs` 在进入本次动效断言前，被现有资讯排序断言 `soft or commercial content must expose its ranking penalty` 阻断，未将该非动效逻辑纳入本次修改。

## 2026-07-13 AI Search Entrance Continuity

- AI 搜索面板移除从左右裁切近半再展开的 `clip-path` 入场，改为整块表面从下方 `8px`、`98.5%` 缩放与透明度连续进入；关闭同样使用轻微下移和缩小，不再出现输入框或按钮像被切掉的中间帧。
- 安全合成浏览器回归覆盖 `1280×800` 与 `390×844`：入场动画为 `280ms` 的 `aiSearchPanelOpen`，中间态与完成态 `clip-path` 均为 `none`，两个视口横向溢出均为 `0`，窄屏面板保持左右各 `10px`，控制台零错误或警告。截图位于 `output/playwright/ai-search-continuity/`。

## 2026-07-13 Website Shortcut Refresh Stability

- 网站快捷方式开启状态与已保存数量作为不含网址、标题或用户数据的轻量布局提示保存在扩展页面本地存储；第一次缺少提示时会直接从 Chrome Sync 根设置读取开关和数量，再挂载首屏骨架。
- 刷新首帧在真实设置返回前显示与最终快捷方式轨道相同数量、相同网格行数和相同高度的安全骨架；加载完成后只原位替换内容，不再让下方效率面板和每日工作台整体下移。
- 开启但列表为空时使用匹配最终说明与操作按钮高度的空轨道骨架；窄屏单列空状态单独预留 `71px` 内部高度，避免移动布局产生二次位移。关闭状态继续不占空间。
- 浏览器坐标回归覆盖 `1280×800` 单入口、`1440×1000` 十入口、`390×844` 十入口多行、窄屏空列表和关闭状态；五种场景的快捷方式高度差与 `.daily-shell` 顶部坐标差均为 `0px`，横向溢出均为 `0`，控制台零错误或警告。截图位于 `output/playwright/shortcut-refresh-stability/`。

## 2026-07-13 Website Shortcut Rail Spacing

- 快捷方式轨道恢复原有 `7px` 内边距；只把单个快捷方式卡片的左内边距由 `6px` 提高到 `10px`，拉开图标与卡片左边缘的距离，不扩大整个模块的左右留白。
- 桌面快捷方式单项宽度上限由 `106px` 收紧为 `104px`，使最大 10 个入口在 `1280px` 内容宽度下保持单排，避免 `9 + 1` 的孤立换行；窄屏仍使用既有响应式列宽。
- 安全合成浏览器回归覆盖 `1280×800`、`1440×1000` 与 `390×844`：桌面模块内边距为 `7px`、卡片内图标左距为 `10px`，10 个入口在两个桌面视口均为单排，窄屏保持 `4 + 4 + 2`；横向溢出均为 `0`，控制台零错误或警告。截图位于 `output/playwright/shortcut-spacing/multi-icons-1280x800-adjusted.png`。

## 2026-07-13 Feed Coverage and Source Health

- 资讯抓取会优先复用已验证 Feed 路径，并按页面声明、JSON-LD／语义文章链接、WordPress／Ghost 与同源常见 Feed 路径逐级发现；跨域 Feed 只记录为待授权，不会在用户点击精确域名授权前发起请求。
- Feed 解析补齐 Atom alternate、`xml:base`、CDATA 链接、`content:encoded`、乱序 enclosure／media 属性与 XML／HTML 字符集识别；缓存保存 ETag／Last-Modified 并使用条件请求，`304` 保留已缓存条目，空结果与失败结果分开处理。
- 来源健康升级为 schema v2，记录逐源抓取方法、条目数、最近检查／成功时间、连续失败、短期结果、已验证 Feed 路径与待授权 Feed；设置 → 资讯显示有效覆盖率、授权后成功率、四种来源状态以及单源重试。
- `404/410` 会清除失效的已验证路径，`429/503` 按受限 Retry-After 延后检查；来源权限撤销后，缓存会同时按落地页来源和实际 Feed 来源 fail-closed 过滤。
- 自动化覆盖跨域 Feed 零预请求、最多四个常见路径探测、声明 Feed、条件请求、编码、解析兼容、覆盖率公式和权限撤销过滤；完整扩展测试、抓取覆盖、Manifest/安全、运行时架构与权限状态回归，以及 Service Worker 和全部浏览器 ESM 语法检查通过。
- 安全合成 Chromium 回归覆盖英语 `1280×800`、简体中文 `1440×1000` 与繁体中文 `390×844`：四项指标和四种逐源状态完整生成，横向溢出均为 `0`，控制台零警告与错误；精确授权仅请求 `https://feeds.example.net/*`，授权后的单源重试成功更新状态。截图位于 `output/playwright/feed-coverage/`。

## 2026-07-13 Image Candidate Coverage

- Feed、灵感卡与 Reader 共用安全图片候选规则：覆盖 Open Graph、Twitter Image、`image_src`、JSON-LD、图片 preload、Media RSS、图片 enclosure、JSON Feed、`picture/srcset`、转义正文、常见懒加载属性和语义明确的静态背景图；每个位置最多保留三个候选。
- 候选质量优先排除 Logo、头像、广告图标、像素图、占位图、视频地址、危险协议、外部私网地址和带凭据 URL；资讯、Reader 与灵感卡在首图失败后依次尝试剩余原站候选，只有灵感卡候选全部失败后才允许进入已配置的 Brave 兜底。
- Feed 缺图补抓仅处理当前刷新中排名靠前、文章 URL 与已授权来源严格同源的条目，每轮最多 12 篇、单源最多 2 篇、并发 3；HTML 读取限制为前 1 MiB、10 秒、禁止重定向、无凭据和无 Referer。成功、无图与临时错误分别缓存 24 小时、2 小时和 15 分钟，权限撤销时清理对应记录。
- 来源健康缓存增加 Feed 原图、原站补抓、最终有图与缺图计数，只保存在扩展本地；不新增设置开关、manifest 权限或 Brave 使用范围。
- 自动化覆盖候选优先级、响应式和转义图片、Media RSS 视频排除、JSON Feed、Reader 候选、补抓同源与单源上限、缓存时效、灵感原站候选耗尽后才进入 Brave，以及权限撤销清理边界；完整扩展测试与全部相关 ESM 语法检查通过。
- Playwright CLI 使用隔离 Chromium 配置加载未打包扩展，覆盖 `1280×800`、`1440×1000`、`390×844`：横向溢出均不大于 `0`，控制台零警告与错误。截图位于 `output/playwright/image-candidates/`。

## 2026-07-13 Automatic AI Error Detail

- 设置 → AI 服务 → 运行概览在“自动整理已暂缓”下方直接显示本地化错误原因，同时保留本轮进度和最近运行时间。
- 自动整理状态新增安全的 `errorParams` 持久化，确保 HTTP 状态码等翻译参数不会在后台任务与界面之间丢失；未知旧状态使用明确的重试回退文案。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`，以 HTTP 429 验证原因翻译、换行和错误阶段样式；页面与详情横向溢出均为 `0`，控制台零警告与错误。截图位于 `output/playwright/auto-ai-error-detail/`。

## 2026-07-13 Automatic AI Recovery

- 自动今日简报输出预算从 `900` 提升到 `2400` token，为最多 12 个候选的排序、标题和可见简报文本保留足够空间；单卡手动与自动整理继续使用既有 `1200` token 预算。
- Responses API 的 `status: incomplete` 会在本地识别；`max_tokens` 与 `max_output_tokens` 明确显示为输出额度耗尽，其他未完成响应显示独立原因，不再统一误报为“没有返回文本”。
- 自动整理状态持久化失败阶段，并在运行概览区分今日简报、资讯卡片和资讯刷新；今日简报失败后仍继续处理符合条件的卡片，最终保留简报错误供下次刷新重试。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`，验证今日简报输出额度原因、阶段标签、错误样式和多行布局；页面与详情横向溢出均为 `0`，控制台零警告与错误。截图位于 `output/playwright/auto-ai-recovery/`。
- 官方 DeepSeek Chat Completions 在自动简报场景显式发送 `thinking: { type: "disabled" }`，避免 V4 默认高强度思考耗尽简报预算；该选项不应用于手动卡片整理、连接测试或 AI 搜索。
- 今日简报失败卡直接显示持久化、本地化的具体 Provider 原因；安全合成 Chromium 回归覆盖三个既有视口，页面横向溢出均为 `0`，控制台零警告与错误。截图位于 `output/playwright/deepseek-brief-output/`。
## Navigation Logo Feedback

- The desktop navigation logo is now a keyboard-accessible back-to-top button.
- Focus, press, and scroll-triggered feedback use the existing accent and motion tokens; the logo intentionally has no hover surface, and reduced-motion mode keeps the action immediate.
- Safe local preview verified `1280×800`, `1440×1000`, and `390×844` with zero horizontal overflow. The back-to-top action returned `scrollY` to `0` and restored the Today navigation state. Screenshots are in `output/playwright/nav-logo-feedback-*.png`; the preview's two console errors are expected missing `/api/settings` and `/api/dashboard` endpoints outside the extension runtime.

## 2026-07-13 Homepage Feed False-positive Guard

- Feed 发现现在也接受页面正文中 URL 形态明确的 RSS／Atom 链接；仍经过既有精确来源权限和最多三个声明候选限制，不扩大主机权限或跨域预请求范围。
- 自指 JSON-LD `Article` 位于包含至少三篇语义文章、且缺少主文章标题或具有列表／分页标记的聚合页时不再直接进入资讯；同一页面的 HTML 单篇文章兜底也会停止，避免错误元数据再次绕过过滤。
- 自动化覆盖正文 Feed 链接优先、自指聚合页拒绝与直接保存单篇 JSON-LD 文章保留。公开 Notebookcheck 中文首页复现已从 `json-ld` 首页条目切换为其同源 RSS Feed，并返回真实文章。

## 2026-07-13 Locale-aware Public Feed Packs

- 公共 Feed 从 URL 哈希身份改为稳定 ID，并按界面语言选择明确来源包；Google News 的三个地区端点使用各自稳定的变体 ID，避免同源 `resolvedUrl` 缓存跨语言复用。简中、繁中和英文均保持 2 个综合／国际来源与 2 个科技来源，避免旧配置中 6/8 来源集中于科技与消费电子。
- 简中使用 Google News 中国区、BBC World、IT之家与 Solidot；繁中使用 Google News 台湾区、BBC World、The Verge 与 MacRumors；英文使用 Google News 美国区、BBC World、The Verge 与 MacRumors。Google News 的 `hl`、`gl`、`ceid` 参数保持同一区域组合，语言切换会使来源变化进入既有刷新与精确域名授权流程。
- MacRumors 改用可直接读取的 HTTPS Feed 主机，不再依赖旧地址跨域且降级到 HTTP 的重定向；Solidot 的 `/story?sid=...` 条目不再被单段路径规则误判为栏目首页。
- 自动化覆盖三种来源包、稳定且唯一的 source key、HTTPS、2+2 覆盖结构、语言对应权限 origin、MacRumors 新地址和 Solidot 查询参数文章路径。定向实时探测确认三种 Google News 地址与 MacRumors 新地址可由当前解析器直接读取。

## 2026-07-13 Daily Brief Information Gain

- 今日简报从逐条标题改写调整为三段自然正文：综合重要事实变化、解释输入明确支持的影响或关联，并指出后续进展、关键未知或证据缺口；提示词明确禁止按候选顺序复述标题或用改写标题凑数。
- Provider 输入会剔除与标题相同或近似相同的 Feed 摘录，保留标题之后的实质细节；同一事件最多合并三个已授权来源的标题与非重复摘录，没有额外细节时显式标记证据不足。
- “整体态势”“影响判断”“后续关注”等仅作为内部写作逻辑，不进入可见正文；解析层会清除简体、繁体和英文的常见栏目前缀，防止 Provider 偶发违约泄漏标签。
- 英文提示词使用独立的编辑表达，要求把已验证进展组织成 coherent picture，并明确 consequences、affected groups 与 unresolved questions；不以中文提示词逐句直译。
- 每日简报升级到 schema v3，使旧的低信息缓存自动失效。自动化覆盖标题重复识别、标题前缀裁剪、栏目前缀清洗和新缓存版本；完整扩展测试、Service Worker、core、runtime 与 client ESM 语法检查以及 `git diff --check` 通过。

## 2026-07-13 Corroborated Daily Events

- “今日事件”不再用单来源资讯硬凑前三条：只展示获得至少两个独立发布方佐证的事件，同一事件只保留一行；没有满足条件的事件时明确显示多来源确认规则。
- 事件卡先按独立来源数、再按公共重要度排序，并继续限制最多一条最近 24 小时的跨日补位；“今日资讯”仍按资讯重要度排序，因此两块不再共享一个固定的前三条列表。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`：`4/2/1` 来源的候选只显示前两条多源事件，单来源资讯仍保留在今日资讯；全单来源状态显示规则说明。三个视口横向溢出均为 `0`，控制台零警告与错误；截图位于 `output/playwright/today-events/`。

## 2026-07-13 Release 26.1.10 Verification

- 完整 `node tests\extension.mjs` 回归通过，覆盖权限、Provider、Reader、Feed 覆盖、图片候选、重定向策略、刷新协调、设置存储与扩展集成。
- Service Worker、`extension/core/`、`extension/runtime/`、`assets/client/` 与测试 ESM 文件全部通过 `node --check`；`git diff --check` 通过。
- 发布包使用显式 allowlist 生成，ZIP 根目录包含 `manifest.json`，并同时生成 SHA-256 sidecar 与逐文件 manifest；打包检查排除密钥、本地绝对路径、远程脚本和非 allowlist 文件。

## 2026-07-13 Efficiency Card Text Containment

- 所有紧凑型空状态统一使用所在表面提供的完整正文宽度，覆盖今日事件、今日简报、阅读队列、来源诊断、屏蔽列表与来源建议；简繁中文的今日事件说明同步精简，避免桌面窄列中仅剩一两个字落到下一行。普通大面积空状态仍保留 `42ch` 可读行宽，并用自然换行规则避免连续字符越界。
- “今日简报”保留完整正文；长英文、URL 和其他连续字符会在卡片内断行，固定高度桌面布局在内容过长时改为卡片内部纵向滚动，不再越过卡片边界。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`：今日事件、阅读队列、来源诊断、屏蔽列表与来源建议均使用完整可用宽度，当前简中说明保持单行，卡片与页面横向溢出均为 `0`；1440 桌面窄列验证超高简报使用内部滚动，控制台零警告与错误。截图位于 `output/playwright/compact-empty-states/` 与 `output/playwright/digest-containment/`。

## 2026-07-13 Source Coverage Scrolling

- “设置 → 资讯 → 来源覆盖率”逐源诊断列表改为独立纵向滚动区域，明确覆盖紧凑列表的裁切规则；最大高度同时受视口约束，滚动不会继续传递到设置弹窗。
- 列表支持键盘聚焦和方向键、Page Up／Page Down 滚动，并保留稳定滚动条空间，避免滚动条出现时挤压逐源文字与操作按钮。
- 安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`：三种尺寸的列表均满足 `scrollHeight > clientHeight`、计算样式均为 `overflow-y: auto`，Page Down 后滚动位置分别达到 `292`、`365`、`296`；横向溢出均为 `0`，控制台零警告与错误。截图位于 `output/playwright/source-coverage-scroll/`。

## 2026-07-13 Empty-State Copy

- 仪表盘、搜索、快捷方式、来源诊断、权限与屏蔽设置的空状态统一为短标题和一句操作说明，并同步简体中文、繁体中文与英文。
- 首次简报空状态使用“生成简报”，失败或重试状态继续使用“重新整理”；来源建议按是否已有抓取统计区分“等待来源数据”和“暂无处理建议”。
- `node tests\extension.mjs`、Service Worker 与全部 core/runtime/client ESM 语法检查、`git diff --check` 均通过。
- 安全本地预览覆盖 `1440×1000` Dashboard、`1280×800` 屏蔽设置以及 `390×844` Dashboard／屏蔽设置；页面和设置弹窗横向溢出均为 `0`，控制台零错误与警告。截图位于 `output/playwright/empty-state-copy/`。

## 2026-07-14 News Image Search Fallback

- 热门资讯卡片会先尝试 Feed 与原站图片候选；候选缺失或全部加载失败后，才进入与灵感卡片相同的可选 Brave 图片搜索兜底，并使用资讯标题与文章域名生成查询。
- 预览消息仍按当前数据严格限制目标：只接受已授权来源过滤后、实际显示在资讯缓存中的文章 URL，以及已选择的灵感书签 URL；任意外部 URL、已撤销来源和阅读器图片不会获得 Brave 兜底。
- Brave 资讯图片缓存与当前资讯条目一同参与权限清理和过期目标裁剪；设置说明、三语隐私页、商店隐私实践、审核说明与提交检查表已同步新的使用范围。
- Playwright 安全合成回归覆盖无图、原图解码失败和原图正常三种资讯卡；前两张只请求各自精确的预览 URL 并稳定切换为兜底图，正常原图不请求预览。`1280×800`、`1440×1000`、`390×844` 横向溢出均为 `0`，最终回归控制台零错误与警告；截图位于 `output/playwright/news-image-fallback/`。

## 2026-07-14 Header Cover Blur Controls

- “设置 → 外观 → 首页头图”新增默认关闭的模糊开关和 `0–24 px` 强度滑块；开启后即时预览，数值输出、滑轨进度与键盘方向键同步，关闭时隐藏并禁用滑块但保留上次强度。
- 模糊开关和强度随外观设置保存；轻量首帧头图提示同步缓存模糊状态，刷新时直接恢复。图片按当前模糊半径增加安全出血范围，避免边缘露出背景。
- 英文、简体中文和繁体中文文案均通过真实语言切换验证。安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000`、`390×844`：页面与设置弹窗横向溢出均为 `0`，刷新后保持 `blur(18px)` 和已加载状态，控制台零错误与警告；截图位于 `output/playwright/header-cover-blur/`。

## 2026-07-14 Cover Blur Peek Mode

- 调节头图模糊度时，设置遮罩、导航、状态和非当前控件临时退到低透明度；模糊滑块保持完整对比度，并使用半透明实色承托，便于直接观察头图变化。
- 指针持续按住约 `80ms` 后进入预览，`pointerup`、`pointercancel` 或失去指针捕获时立即退出；键盘方向键、Home/End 与 PageUp/PageDown 调节时进入，停止输入 `600ms` 后退出。
- 预览态只改变视觉层级，不关闭设置弹窗或指针事件，避免点击穿透。窄屏保留底部保存区的弱边界；全局减少动效规则会把透明度恢复过渡缩短为即时变化。
- 安全本地 Chromium 回归覆盖 `1280×800`、`1440×1000` 与 `390×844`：指针拖动时进入、松手后恢复，键盘调节时进入并在 `600ms` 空闲后恢复；页面与设置弹窗横向溢出均为 `0`，控制台零错误与警告。截图位于 `output/playwright/header-cover-blur-peek/`。
- 模糊度控件进一步收敛为最大 `560px` 的单行仪表条：标签、增粗轨道和等宽数字读数同排，不显示轨道下方刻度；普通状态移除厚重外框，预览状态只提升承托、边框与阴影，不改变拖动几何位置。
- 改造后的普通态与指针预览态已重新截图；三种界面语言均在窄屏验证，英文标签下轨道仍保留约 `135px` 调节宽度，简中与繁中约 `171px`，页面和弹窗横向溢出保持为 `0`。
- 模糊开关关闭时滑块容器以 `aria-hidden="true"` 收起至零高度并停止接收指针事件；开启后通过 `220ms` 的高度、内边距、位移与淡入组合向下展开。展开态固定为 `46px`，标签、原生滑块和读数在可用高度内严格垂直居中，并移除 range 控件的行内基线偏移。保存中的忙碌状态只禁用已展开滑块，不会触发收起；减少动效偏好继续由全局规则即时完成切换。
- 安全本地 Chromium 回归确认展开与收起均保留连续中间态，结束后回到 `0px`、不可见且不可点击；展开后的容器高度在 `1280×800`、`1440×1000`、`390×844` 均为 `46px`，标签、滑轨、range 输入和读数相对容器中心线的偏差均为 `0px`。页面与设置弹窗横向溢出均为 `0`，控制台零错误与警告；截图位于 `output/playwright/header-cover-blur-reveal/`。
- `node tests\extension.mjs`、Service Worker 与全部 core/client ESM 语法检查、`git diff --check` 均通过。

## 2026-07-14 Website Shortcut Reordering

- 今日页与“设置 → 浏览器”中的网站快捷方式均支持整项拖拽排序；设置页继续保留上移／下移按钮作为键盘入口，并在拖动编辑中的项目后保持编辑目标不变。
- 今日页放下后只提交 `websiteShortcuts` 并采用服务端规范化顺序；保存期间暂停再次拖动，模拟写入失败时会恢复原顺序并显示本地化错误。设置页排序仍属于现有设置草稿，统一由“保存设置”提交。
- 今日页快捷方式右键菜单新增“修改快捷方式”，并保留“打开新标签页”和“复制链接”；修改操作按唯一 URL 在设置重新载入后定位目标、进入编辑态并聚焦名称输入框。
- 安全本地 Chromium 回归覆盖 `1280×800`、`1440×1000`、`720×900`：今日即时保存、设置草稿拖动、右键三项菜单、编辑定位和失败回滚均通过，三个视口横向溢出均为 `0`；正常流程控制台零错误，失败分支仅出现预期的合成 HTTP 500。截图位于 `output/playwright/website-shortcuts-*.png`。

## 2026-07-14 Switchable Utility Card

- 第一张效率卡保持同一 DOM 根节点，并通过单一“切换”按钮按“今日事件 → 天气预报 → 待办事项”轮换；标题图标、计数或城市状态同步变化。模式始终保存在扩展本地客户端状态；手动城市与待办默认本地保存，仅在用户分别开启对应内容同步开关后进入 Chrome Sync。
- 天气只在用户提交城市时申请 Open-Meteo 地理编码与预报两个精确 HTTPS 来源；候选必须手动选择。后台限制请求大小、超时、最终来源与响应结构，30 分钟内复用缓存，失败时只回退至 6 小时内并标记陈旧。撤权清理天气缓存并保留城市选择；三语 WMO 状态和隐私／商店披露已同步。
- 待办支持回车新增、完成、恢复与删除；标题栏圆形“+”按需展开编辑框，提交、Esc 或切换模式后收起并恢复焦点。列表使用连续的 `28px` 紧凑行与圆框 checkbox 语义；删除按钮在桌面悬停／键盘焦点时出现，在触控设备始终可用。单项 120 字符、总计 50 项，未完成项优先并按创建时间倒序；普通缓存清理和界面偏好重置不会删除待办。
- Playwright 使用安全合成城市、三日天气与待办数据覆盖 `1280×800`、`1440×1000`、`390×844`。三模式边界在各视口完全一致：前两者分别为 `354.66×168`、`340×229.33`，窄屏为 `362×168`；横向溢出均为 `0`。三行天气在 168px 卡片内完整可见；到 `1440px` 断点后会利用新增高度展开为三条约 `45px` 的双行预报。13 项待办在编辑框关闭时分别获得 `98/159/98px` 列表高度与 `364px` 内容高度，可完整显示 `3/5/3` 项；1440 下展开编辑框后列表仍保留 `124px`，键盘焦点可见，控制台零错误与警告。
- 自动化覆盖非法模式回退、模式循环、待办边界、编辑框默认隐藏、展开状态、Esc、焦点恢复、checkbox 语义、紧凑行与触控删除回退，以及天气响应归一化、固定来源、权限和缓存分支。截图位于 `output/playwright/utility-card/`。
- 标题栏将数量／城市与待办加号排在固定右锚的“切换”按钮左侧。Playwright 在 `1280×800`、`1440×1000`、`390×844` 连续循环三种模式并展开／关闭待办输入框，按钮左右坐标变化均为 `0px`，标题重叠与页面横向溢出均为 `0`。
- 天气三行统一使用 `minmax(0, 1fr) 66px` 固定两列，原有温度胶囊在同一右侧列垂直对齐；标题图标按当前 WMO 状态使用同一套本地 Lucide 晴、多云、阴、雾、毛毛雨、雨、雪和雷暴图标，预报尚未返回时使用中性的多云间晴图标。卡片高度、文案、温度胶囊和当前日强调保持不变。
- 安全合成天气数据复核覆盖 `1280×800`、`1440×1000`、`390×844`：三个温度胶囊在各视口的横坐标、`66px` 宽度与右边界完全一致，距天气行右边界仅保留原有 `5/7px` 内边距；三行日期与降水均上下排列并保持左对齐。当前多云天气使用 `cloud-sun.svg`；天气与条件式 GeoNames 署名合并为右下角 `Open-Meteo · GeoNames`，两个提供方仍保留独立链接，完整说明保留在无障碍名称和悬停提示中，并具有 `2px/1px` 防裁切安全边距。固定高度下三行均为 `27px`，列表 `clientHeight` 与 `scrollHeight` 同为 `83px`，无内容裁切。页面横向溢出均为 `0`，控制台零错误和警告；截图位于 `output/playwright/weather-fixed-columns/`。

## 2026-07-14 Website Shortcut Capacity and Scrolling

- 网站快捷方式上限由 `10` 提升到 `16`；今日页始终保持单行，宽屏直接展示全部入口，空间不足时只在快捷栏内部横向滚动，不增加 Dashboard 高度。右侧渐隐提示尚有内容，到达末端后自动消失。
- 快捷栏支持触控板／横向滚动、键盘方向键和 Home／End；拖拽靠近左右边缘时会自动滚动。设置页的 16 项管理列表使用独立纵向滚动区，拖拽靠近上下边缘时同样自动滚动，避免撑高设置弹窗。
- 为避免 16 个长网址超过 Chrome Sync 单项约束，`websiteShortcuts` 与其他大数组一样使用既有分片存储协议；自动化覆盖 16 个接近 URL 长度上限的项目、跨分片还原和每条同步记录的安全字节上限。
- 安全本地 Chromium 回归覆盖宽屏 `1920×1080`（对应 4K 200% 缩放的 CSS 视口）、`1440×1000`、`1280×800` 与 `390×844`。宽屏 16 项完整同排；其余尺寸均满足 `scrollWidth > clientWidth`、单行高度一致、页面横向溢出为 `0`，键盘 End 可到达末端。设置列表在 `1280×800` 下为 `335 / 842px` 可视／内容高度并可滚至底部，正常流程控制台零错误与警告。截图位于 `output/playwright/website-shortcuts-16-*.png`。

## 2026-07-14 Website Shortcut Empty State

- Source visual truth: `C:/Users/AAA/AppData/Local/SpCat/cache/history/spcat_2026-07-14_12-08-40.png`
- Implementation screenshots: `output/playwright/website-shortcuts-empty-fixed-2048x900.png` and `output/playwright/website-shortcuts-empty-fixed-390x844.png`
- Before/after comparison: `output/playwright/website-shortcuts-empty-before-after.png`
- Viewports: `2048×900` desktop and `390×844` narrow window.
- State: website shortcuts enabled with zero configured shortcuts; dark theme; Simplified Chinese.
- Full-view comparison evidence: the original prompt was constrained to one implicit shortcut column and wrapped vertically; the revised prompt spans the rail, keeps the existing surface and button styling, and groups copy and action at the left edge.
- Focused region evidence: desktop empty content measured `1696.7px` wide and `50px` high with one text line and a `12px` copy/action gap; narrow content measured `336.7px` wide and `58px` high with one text line and a `10px` gap. Both viewports reported zero page horizontal overflow.
- Fonts and typography: existing UI family, weight, size, and antialiasing are unchanged; the empty copy now uses one unbroken line and removes terminal punctuation.
- Spacing and layout rhythm: empty and populated states now use separate layout modes; desktop and mobile rail heights align with their populated shortcut rows, with no extra card height.
- Colors and visual tokens: existing muted text, control border, background, radius, and focus tokens are unchanged.
- Image quality and asset fidelity: no raster or custom decorative asset is involved; the existing icon asset on the action remains unchanged.
- Copy and content: `暂无快捷方式` and `添加快捷方式` form one compact prompt; Traditional Chinese and English receive the same punctuation treatment.
- Primary interaction: the empty-state action opened Settings directly on the Browser section at `390×844`.
- Console: zero warnings and errors in the application and comparison views.
- Comparison history: iteration 1 identified the original implicit-grid-column wrapping as a P1 layout failure. The empty state was moved to a full-width block layout, mobile column stacking was removed, and the first-load skeleton now receives the same empty-state flag. Post-fix evidence shows one-line copy at both target viewports with no P0/P1/P2 findings.
- Follow-up polish: none required for this scoped state.

final result: passed

## 2026-07-14 Checkbox and Switch Controls

- AI 数据传输确认框使用 `28×28px` 命中区与 `24×24px` 可见圆框，复用待办的透明底边框、选中后半透明强调色填充，并通过与待办相同的本地 `check` 图标显示勾选状态；控件与说明文字按行高垂直居中。待办列表原有的圆形 checkbox 样式未修改。
- 原本使用滑动开关的 13 个设置项保留 `44×24px` 胶囊轨道与移动圆点；圆形确认框通过 `.toggle-checkbox` 修饰类复用同一输入、焦点和状态结构，不改变设置字段、默认值或保存逻辑。
- 安全静态 Chromium 回归覆盖 `1280×800`、`1440×1000` 与 `390×844`，圆形确认框与滑动开关均保持预期几何形态，三个视口横向溢出均为 `0`。静态预览控制台仅出现预期的 `/api/settings` 与 `/api/dashboard` 404；Service Worker、全部 core/client ESM 语法检查与 `git diff --check` 通过。截图位于 `output/playwright/checkbox-round/`。

## 2026-07-14 Settings Import and Export

- “设置 → 关于”新增紧凑的“配置迁移”区域，复用现有分隔线、按钮和状态系统；导出文件使用版本化 JSON 与按日期命名的 Blob 下载，不新增 Manifest 权限。共享白名单只覆盖用户可编辑设置（包括三个内容同步开关），明确排除 API 密钥、授权同意、Chrome 权限、onboarding、缓存、阅读记录、偏好行为、待办正文和派生状态。
- 客户端预检与后台强制校验共用纯 ESM 模块：文件上限为 `256 KiB`，未知字段忽略，缺失字段保留当前已保存值，已提供但类型、范围或 URL 非法的字段会拒绝整份文件。后台仍通过既有设置保存链路执行规范化、缓存失效、广播与刷新；AI Provider 域名变化沿用既有安全策略清除当前 API 密钥，同域更新不会误删。
- 自动化覆盖导出白名单、完整与部分导入、未知字段、非法 JSON、格式与版本、空配置、超大文件、非法数值／URL／数组，以及 Provider 同域和跨域判断。浏览器回归验证导出文件名、确认／取消、重复选择同一文件、键盘焦点，以及 `1280×800`、`1440×1000`、`390×844` 三种视口；页面和设置弹窗横向溢出均为 `0`，安全合成接口下控制台零错误与警告。截图位于 `output/playwright/settings-transfer-1280x800.png`、`output/playwright/settings-transfer-1440x1000.png` 与 `output/playwright/settings-transfer-390x844.png`。
- `node tests\extension.mjs`、Service Worker、全部 core/runtime/client ESM 语法检查与 `git diff --check` 均通过。

## 2026-07-15 — Onboarding preset source name

- 首次设置的来源摘要不再写死旧称“灵感预设”；现在与来源选项统一复用三语规范名“灵感预设（Ampira）／靈感預設（Ampira）／Inspiration preset (Ampira)”，与“公共 Feed（Ampira）／Public Feed (Ampira)”保持一致。
- 新安装与首次设置默认选择“公共 Feed（Ampira）”和“灵感预设（Ampira）”；已有设置缺少来源模式字段时仍保留原来的个人资讯与灵感书签文件夹，不强制迁移。
- 书签来源选项现在由后台书签树递归统计一级文件夹中的全部书签，包含嵌套子文件夹，且不受资讯显示条数上限影响；首次设置和设置页不再把缺失数量统一显示为 `0`。
- `node tests\extension.mjs`、Service Worker、全部 core/runtime/client ESM 语法检查通过。

## 2026-07-14 China Weather Location Resolution

- 中国天气地点搜索新增由官方 GeoNames `CN.zip` 生成的县级以上行政中心索引，按 `PPLC/PPLA/PPLA2/PPLA3` 筛选 2391 条记录；生成产物约 `497 KiB`，只包含名称、紧凑查询键、行政层级、人口、类型和 WGS84 坐标。生成脚本固定输出到仓库内并记录 GeoNames CC BY 4.0 来源，扩展包、可见 UI、隐私页与第三方声明均保留署名。
- `慈溪`、`慈溪市` 和 `Cixi` 均解析为 `慈溪市 · 宁波市 · 浙江省 · 中国`（`30.1764, 121.2457`）；本地命中不调用 Open-Meteo 地理编码接口，未命中查询继续沿用既有 Open-Meteo 回退。歧义候选按匹配精度、行政级别和人口排序，始终由用户手动选择；远端普通地名保留“请核对行政区”低可信提示。
- 地点状态与候选归一化现在保留 `admin2`、国家代码、GeoNames 类型、人口、来源和可信度；候选、标题提示和已保存状态均包含完整行政层级。GeoNames 署名在候选阶段及选中后的预报页脚持续可见，Open-Meteo 天气署名保持不变。
- Playwright 安全合成回归覆盖 `1280×800`、`1440×1000` 与 `390×844`，天气卡尺寸分别为约 `355×168`、`340×229`、`352×168`，页面横向溢出均为 `0`。完整验证搜索、候选、GeoNames 署名、选择、持久化和三日预报，最终会话控制台零错误与警告；截图位于 `output/playwright/weather-china-location/`。
- 自动化覆盖慈溪中英文命中、行政层级、朝阳歧义排序、本地搜索零网络、国际地点权限回退、远端字段归一化与原有天气缓存路径。`node tests\extension.mjs`、Service Worker、全部 core/runtime/client ESM 语法检查、`scripts/verify-extension.ps1` 与 `git diff --check` 均通过。

## 2026-07-14 Utility Mode Transition

- “今日事件 → 天气预报 → 待办事项”使用固定方向的双段切换：旧正文以 `80ms` 向左淡出，新正文以 `140ms` 从右侧淡入；标题、图标和模式状态使用更轻的 `70/120ms` 纵向微动。卡片高度、滚动区、标题栏工具顺序和右锚切换按钮均未改变。
- 动效通过本地 CSS 关键帧完成，只改变 `transform` 与 `opacity`；切换编排拆分到独立客户端模块。连续点击会取消当前视觉阶段并按最新模式重新开始，双击可从今日事件稳定推进至待办事项；`prefers-reduced-motion: reduce` 下直接更新内容。
- 安全合成浏览器回归覆盖 `1280×800`、`1440×1000` 与 `390×844`，最终模式分别为今日事件、天气预报和待办事项；卡片尺寸分别约为 `354.67×168`、`340×229.33`、`352×168`，页面横向溢出均为 `0`，控制台零警告与错误。截图位于 `output/playwright/utility-card-motion/`。

## 2026-07-15 Toolbar Permission Boundary

- 此处记录的“只打开 `dashboard.html`”方案已由当前的 Toolbar Read-later Restoration 取代；当前工具栏会在 `activeTab` 临时权限边界内捕获当前页，并在本地浮窗确认。

## 2026-07-15 Business Logic Hardening

- 设置与可选内容同步现在共用 90 KiB 的投影额度检查，写入前会计入现有 `chrome.storage.sync` 全部记录，避免各模块分别通过校验后合计超过 Chrome Sync 总额度。
- 跨设备内容开关的副作用失败时会回写旧设置并反向补偿内容同步，避免界面显示已开启但数据实际未同步，或关闭失败后遗留远端副本。
- 外部存储变更不会再用迟到的旧回声覆盖仍待提交的本地新值；相同回声仍会正常确认并清除本地写入标记。
- 使用“灵感预设（Ampira）”时不再占用个人灵感书签文件夹，该文件夹仍可作为纯书签栏目展示。

## 2026-07-15 Release 26.2.0

- Manifest 版本提升至 `26.2.0`，并使用真实支持入口运行 `scripts/verify-extension.ps1 -Package`；扩展测试、发布安全门禁与打包均通过。
- 最终 `dist/ampira-26.2.0.zip` 为 `1,417,064` bytes，包含 209 个文件和 24 张预设 WebP；根目录 Manifest 版本为 `26.2.0`，不含测试、`output/`、`dashboard-cache/`、非图标 PNG 或其他禁止路径。
- ZIP SHA-256 为 `86cb956c3443b922eda7b76ea79f579245eac811eb3ce3711bd9b0eee03ebc38`，与 `.zip.sha256` 和逐文件 `.manifest.json` 一致。
- 必需权限重新限定为 `bookmarks`、`storage`、`alarms`。稍后读仍由 Ampira 已显示的资讯与书签卡片加入，默认本机保存，可由独立开关选择进入 Chrome Sync。
- Manifest、隐私页、商店披露、审核说明、README、打包验证和自动化断言已同步更新，不再宣称工具栏捕获或浮窗确认能力。

## 2026-07-14 Localized AI Output Guard

- AI 搜索、网页／文章阅读、卡片整理和今日简报共用本地化输出守卫：英文、简体中文和繁体中文界面分别要求所有可见正文及生成标题使用对应语言；守卫同时要求模型静默执行，不得在正文中提及、引用、概括、解释或暴露语言、格式及行为约束。
- AI 搜索缓存版本升级为 `4`，防止继续命中旧提示词生成的回答。卡片摘要策略版本升级为 `3`，新摘要记录 `summaryLocale`；缓存复用现在同时要求策略版本、界面语言和 Provider 匹配，语言不匹配时回退到原始 Feed 摘录并清除 AI 摘要元数据。
- 自动化覆盖三种语言守卫、约束隐藏要求、同语言摘要复用、跨语言摘要拒绝和回退清理。`node tests\extension.mjs`、Service Worker、全部 core/runtime/client/locales ESM 语法检查与 `git diff --check` 均通过。此次未改变可见布局、权限、远端数据边界或打包内容，因此无需新增视口截图。

## 2026-07-14 — About-page copyright attribution

- Added the restrained `© 2026 Panran Sun` ownership line beneath the Ampira tagline in Settings → About.
- Kept the attribution as low-emphasis metadata within the existing identity block, with no new surface, permission, storage, API, or packaging behavior.

## 2026-07-14 — Bookmark subcategory visibility

- Added an immediate-save “隐藏此分类” action to bookmark subcategory context menus. Hidden categories are excluded only from the Archive Index filters and category blocks; Chrome bookmarks, news feeds, AI briefs, and cache inputs remain unchanged.
- Added a recoverable all-hidden state and a Settings → Bookmarks list with staged individual and bulk restore actions. Visibility rules sync as normalized parent-section/category pairs and participate in settings import/export without triggering a news refresh.
- Playwright QA used inert fixture bookmarks and no extension profile data. Verified context-menu placement, consecutive hiding, the all-hidden recovery route, staged restore/save, and zero horizontal overflow at 1280×800, 1440×1000, and 390×844. Intentional screenshots: `output/playwright/bookmark-category-context-1280x800.png`, `bookmark-category-context-1440x1000.png`, `bookmark-category-context-narrow-390x844.png`, `bookmark-categories-all-hidden-1280x800.png`, and `bookmark-hidden-settings-1280x800.png`.
- Verified the About panel at 1280×800, 1440×1000, and 390×800; the narrow viewport remained at `scrollWidth === clientWidth` with no horizontal document overflow.
- Screenshots: `output/playwright/about-copyright-1280x800.png`, `output/playwright/about-copyright-1440x1000.png`, and `output/playwright/about-copyright-390x800.png`. The local HTTP preview's expected `/api/settings` and `/api/dashboard` 404s were the only console errors; they are absent when the unpacked extension supplies its Chrome runtime.
- `node tests\extension.mjs`, the service-worker syntax check, all required core/client module syntax checks, and `git diff --check` passed.

## 2026-07-14 — Optional cross-device content sync

- 第一阶段新增默认关闭的“同步稍后读”：使用独立 `ampira.content.reading.v1.*` 命名空间逐条保存标题、网址、来源与加入时间，优先同步最近记录，数量上限为 40，并受 `30 KiB` 数据集预算和 `7 KiB` 单记录安全上限约束。
- 第二阶段新增默认关闭的“同步待办事项”与“同步天气城市”。待办逐条保存文字、完成状态与时间；天气只保存用户明确选择的城市、行政区、提供方标记和查询所需坐标。实用卡模式、天气响应、资讯、摘要、完整阅读状态、AI 配置和密钥不进入内容同步。
- 三类数据使用独立记录版本与删除墓碑进行跨设备合并；同一记录以较新的更新时间／修订号为准。墓碑远端保留最多 30 天并限制数量。关闭某项会删除其 Chrome Sync 记录和本机同步元数据，同时保留该类别的本机内容。
- 设置页在“浏览器”新增“跨设备内容”区，明确默认本地、同步字段、关闭清理行为、待办隐私和 Chrome Sync 不适合机密数据；三个开关参与设置导入／导出，但正文内容不进入设置文件。简中、繁中、英文 UI、隐私页、数据删除说明、商店隐私实践与审核说明已同步更新。
- 自动化使用两个隔离的合成设备状态覆盖初次上传、第二设备拉取、待办逐条更新、阅读队列删除墓碑回传、最近 40 条选择、单项字节上限、关闭后远端清理与本机保留。未读取真实 Chrome Sync、书签、配置文件或密钥。
- Playwright 安全本地预览覆盖 `1280×800`、`1440×1000` 与 `390×844`，三项整行点击均可切换；页面与设置弹窗横向溢出均为 `0`。英文 `390×844` 下三个开关行 `scrollWidth === clientWidth === 342px`，长说明自然换行且开关保持右对齐。截图位于 `output/playwright/content-sync/`；本地 HTTP 预览仅出现预期的 `/api/settings` 与 `/api/dashboard` 404，零警告，解压扩展运行时由 Chrome 消息接口提供这些数据。

## 2026-07-14 — Ampira Inspiration Preset v1

- 两个内置来源使用唯一规范名：简中为“灵感预设（Ampira）/ 公共 Feed（Ampira）”，繁中为“靈感預設（Ampira）/ 公共 Feed（Ampira）”，英文为“Inspiration preset (Ampira) / Public Feed (Ampira)”；设置选项与说明、状态摘要、首次引导、每日灵感区标题、扩展说明、隐私页和商店材料均已统一，个人书签文件夹名称与内部选项值保持原样。自动化逐项约束所有内置来源文案必须包含对应规范名，避免旧名称回流。安全本地预览在 `1280×800`、`1440×1000`、`390×844` 下确认两个名称完整显示，设置弹窗与页面横向溢出均为 `0`，控制台零错误与警告；截图位于 `output/playwright/public-feed-source/ampira-source-names-*.png`。
- 灵感来源新增互斥的“Ampira 灵感预设 / 我的书签文件夹”选项；新安装默认预设，缺少字段的既有设置迁移到个人书签。切换来源会保留原来的个人灵感文件夹，且预设模式不创建、修改或删除 Chrome 书签；首次引导在没有一级文件夹时仍可选择预设继续。
- v1 收录 48 个唯一公开 HTTPS 入口，分类配额为 `8/8/6/6/6/6/4/4`；24 个封面键各引用两次且不跨分类。每日首三批为 15 个不同网站和 15 张不同封面，每批优先覆盖 5 个分类并保持封面不重复。
- 24 张封面为无文字、无商标、无网站截图的原创编辑视觉。生成工具输出的原始母版为原生 `1536×1024`，保存在不打包的 `output/inspiration-masters/`；未为凑齐名义尺寸进行放大。扩展仅包含 Lanczos3 缩放、WebP 质量 82 的 `960×600` 版本。Squoosh 页面已验证，但内置 QA 浏览器不支持本地文件上传，因此实际压缩使用本机 libwebp/Sharp 等价参数。
- 优化后 24 张图片合计 `934,334` bytes（约 `0.89 MiB`），最大单张 `107,364` bytes；逐张原图、卡片尺寸和联系表检查未发现色带、纹理破碎、伪文字、畸形物体或危险裁切。联系表位于 `output/inspiration-contact-sheet.jpg`。
- 预设封面直接读取扩展本地资源，不进入原站元数据、远程图片预加载或 Brave 搜图路径；48 个预设域名不进入来源权限清单。正文读取在用户手势后先尝试网站公开允许的 CORS 路径，受阻时才按单个精确域名提供授权；AI 分析仍由用户手势按需申请权限。
- 安全本地预览覆盖 `1280×800`、`1440×1000`、`390×844`，三个视口横向溢出均为 `0`，每日 5 张卡片的图片均以 `960×600` 完成加载，控制台零警告与错误。简中、繁中与英文的 8 个分类和每日卡片标签均完成回归；首次检查发现的内部分类键显示已修复。截图位于 `output/playwright/inspiration-preset/`。
- 设置页实测从预设切到个人书签后仍保留合成文件夹“个人灵感”；来源选择器、卡片遮罩、窄窗口裁切和标题对比度通过。测试只使用安全合成设置与预设公开数据，未读取真实 Chrome 书签、用户导出文件、浏览器配置、密钥或运行时缓存。
- 后续回归修复把个人书签的稳定 `categoryKey` 明确限定为内部身份，不再拼成翻译键；预设分类仍使用三语 `category.inspiration.*`，未知翻译键会回退到原始可见分类名，因此 `category.bookmark:*`、`bookmark:*` 等内部值不会进入标签。
- 灵感图片失败切换改为等待预览控制器返回下一候选，并检查失败图片仍是当前 DOM 节点，避免第一张加载失败后旧错误处理覆盖已经切换成功的第二张。个人灵感在真实原站图等待中、无权限、无元数据或候选耗尽时使用按稳定身份选择的本地抽象封面；原站图可用后仍会替换本地兜底，预设继续只读取其指定本地封面且不发起站点预览请求。
- 原站图预览的瞬时消息／网络异常和后台 `originalStatus: error` 不再写入会话级空缓存；当前页面按 `750 ms / 2000 ms` 最多自动重试两次，成功、确认无元数据或明确 `retryable: false` 时结束并保留正常结果，页面数据失效会取消旧重试。自动化覆盖两类瞬时恢复、不可重试负缓存和连续失败上限；合成页面以 15 个首次失败、第二次成功的原站预览验证共 30 次请求后完成恢复。
- 原站瞬时恢复页面在 `1280×800`、`1440×1000`、`390×844` 均为 5/5 张 `960×600` 图片、横向溢出 `0`、内部标签泄漏 `0`，控制台零错误与警告。截图为 `output/playwright/inspiration-preset/transient-origin-retry-1280x800.png`、`transient-origin-retry-1440x1000.png` 与 `transient-origin-retry-390x844.png`。
- 灵感来源在“Ampira 灵感预设 / 个人书签文件夹”之间切换时，当前设置页、设置导入和其他已打开新标签页收到的 `bookmarkSourceChanged` 广播都会清空客户端预览缓存；重新加载 Dashboard 后个人灵感会重新发起预取，不再复用切换前的空结果。预设仍只读本地封面，个人书签在原站图返回前继续显示本地兜底，因此切换过程不会出现空白卡片。
- 安全合成回归以内部形态分类键和“第一张不可解码、第二张有效”的双候选复现问题：`1280×800`、`1440×1000`、`390×844` 均为 5/5 张 `960×600` 图片、零内部标签泄漏、横向溢出 `0`、控制台零错误与警告。预设模式另行确认 5/5 张本地封面并且网络记录中没有 `/api/site-preview`；截图为 `output/playwright/inspiration-preset/personal-preview-fix-1280x800.png`、`personal-preview-fix-1440x1000.png`、`personal-preview-column-fix-390x844.png` 与 `preset-cover-fix-390x844.png`。
- `node tests\extension.mjs`、Service Worker、全部 core/runtime/client/locales ESM 语法检查和 `git diff --check` 通过。最终 `ampira-26.1.11.zip` 为 `1,423,925` bytes（约 `1.36 MiB`），包含 24 张 WebP、根目录 `manifest.json`，不含 PNG 母版、书签导出、`output/`、`dashboard-cache/`、桌面路径或私人数据。

## 2026-07-14 — 今日事件抓取与确认策略 v4

- 单源 Feed 先按 `min(48, max(12, 单源上限 × 4))` 扩大扫描，再过滤并按本地今日／最近 24 小时、公共重要度、发布时间和原始顺序回选；刷新批次优先容纳最多 80 个公共来源，其余书签来源保持轮转。仅有可复用缓存时才发送条件请求验证器。
- 空结果采用两次宽限：24 小时内的已核验缓存会在第一次和第二次连续空结果时保留，第三次或缓存过期后清除；网络错误继续沿用原退避与旧缓存策略。
- 36 小时聚类改为从全部兼容簇中选择最高相似度并约束整簇跨度，清理发布方后缀、英文停用词和中文整句伪 token；日期／时间不冲突，只有相同语义上下文中的数量、金额、版本或型号矛盾才拆分。同发布方只计一次，AI 证据最多取三个不同发布方。
- 排序与每日简报 schema 升级为 v4，条目携带 `corroborated`、`high-confidence-single` 或 `single-source`。今日事件始终先排今日多源、最多一条跨日多源，仍不足三条时才在末尾补一条严格达标且显示三语“单源待确认”的单源事件；AI 分数不会改变确认等级。
- Playwright 仅使用安全合成数据覆盖 `1280×800`、`1440×1000`、`390×844`：三个视口均显示三条事件、仅一条待确认且位于多源事件之后，所有行完整可见，页面横向溢出均为 `0`。空状态在 `1280×800` 下为零事件且文案正确；全程控制台零错误与警告。截图与合成预览脚本位于 `output/playwright/today-event-fetch-v4/`。
- 自动化覆盖乱序／前置无效 Feed、48 条扫描上限、无缓存 304、连续空结果、公共来源优先轮转、中英文聚类、数字冲突、36 小时边界、发布方去重、证据选择、单源确认门槛、展示数量和 v4 缓存失效。`node tests\extension.mjs`、Service Worker、全部 core/runtime/client/locales ESM 语法检查与 `git diff --check` 均通过。

## 2026-07-14 — Public Feed-only news source

- “设置 → 书签 → 资讯文件夹”与首次引导的资讯来源选择器新增固定第一项“公共 Feed”；个人文件夹使用编码后的独立选项值，避免与预设值或同名文件夹冲突。既有设置缺少新字段时继续使用原资讯书签文件夹，不改变现有用户来源。
- 选择公共 Feed 后，资讯模型不再读取任何资讯书签，只保留公共 Feed、灵感来源与附加书签；原资讯文件夹仍被记住，切回个人文件夹时可继续使用。该模式在客户端和后台规范化层都强制“公共 Feed 补盲”为开启，开关不可关闭；切回个人文件夹后开关恢复可编辑。
- 公共 Feed 补盲不可关闭时复用“全屏显示”的共享 `aria-disabled` 整行弱化效果，不再维护专用开关配色；开关仍保留右侧“已开启”位置。安全合成 Chromium 回归覆盖 `1280×800`、`1440×1000` 与 `390×844`，页面和设置弹窗横向溢出均为 `0`，控制台零错误与警告；截图为 `output/playwright/public-feed-source/public-feed-disabled-*.png`。
- “模糊头图”在头图关闭或设置保存期间不可操作时，改为在 `.switch-field` 整行同步 `aria-disabled`，与“公共 Feed（Ampira）补盲”复用相同的弱化、禁用光标和无障碍状态；模糊度滑杆的展开与独立禁用逻辑保持不变。
- 设置导入／导出、保存变更识别、缓存失效和首次引导同步支持 `newsSourceMode`。自动化覆盖旧设置迁移、公共 Feed 模式排除资讯书签、保留灵感与附加书签，以及补盲开关后台强制开启。
- Playwright 安全合成回归覆盖 `1280×800`、`1440×1000` 与 `390×844`：公共 Feed 始终位于第一项，个人文件夹模式下补盲开关可编辑，公共 Feed 模式下为已开启且禁用；三个视口的页面与设置弹窗横向溢出均为 `0`，控制台零错误与警告。截图与合成服务位于 `output/playwright/public-feed-source/`。
- `node tests\extension.mjs`、Service Worker、全部 core/runtime/client/locales ESM 语法检查与 `git diff --check` 均通过。
- 完整信息显示复核使用加长的安全合成标题和发布方名称：今日事件标题、来源、来源数和待确认标识全部改为自然换行，不再使用省略号。实用卡外框恢复为三模式共用尺寸，`1280×800` 与 `390×844` 下均为 `168px` 高，`1440×1000` 下均为约 `229.33px` 高；超出固定区域的事件通过卡内列表滚动查看，不改变“天气预报／待办事项”的原始高度。三个视口页面横向溢出均为 `0`，控制台零错误与警告；截图为同目录下的 `today-event-fixed-height-*.png`。

## 2026-07-14 — Compact inspiration folder selector

- 设置页与首次引导删除大面积来源卡片，统一复用原生“灵感文件夹”下拉框；固定首项为 `Ampira 灵感预设`，个人一级书签文件夹使用 `bookmark:${encodeURIComponent(folderName)}` 编码。新安装默认预设，既有来源设置不被覆盖。
- 预设模式不再生成或保留本地化的个人灵感文件夹默认名；切回 `Ampira 灵感预设` 会清空个人灵感文件夹字段，只有用户主动选择实际存在的一级书签文件夹时才保存其真实名称，避免出现“审美（未找到）”等虚构目录。
- 公共资讯源模式同样不再生成或保留本地化的“资讯”文件夹默认名；切回公共资讯源会清空个人资讯文件夹字段，只有用户主动选择实际一级书签文件夹时才保存其真实名称。
- 零一级文件夹时下拉框只显示可用的预设并允许继续；已删除的个人文件夹保留三语“未找到”选项。资讯与灵感同文件夹校验只在个人书签模式阻止保存，切回预设后立即解除。原生 `select` 保留标签关联、焦点与浏览器键盘语义，没有自定义键盘拦截。
- 安全本地浏览器回归覆盖简中设置页、简中零文件夹引导、繁中缺失文件夹和英文窄窗。`1280×800`、`1440×1000`、`390×844` 的页面、弹窗与双列／单列字段网格横向溢出均为 `0`；初始化语言竞态增加空值保护后，最终控制台零错误与警告。截图位于 `output/playwright/inspiration-preset/folder-select-zh-CN-*.png`、`onboarding-zero-zh-CN-1280x800.png` 与 `onboarding-zero-en-390x844.png`。
- `node tests\extension.mjs` 连续两次通过；Service Worker 与 121 个 core/runtime/client/locales 模块通过 `node --check`，`git diff --check` 通过。最终 `dist/ampira-26.1.11.zip` 为 `1,430,816` bytes（约 `1.365 MiB`），SHA-256 与 sidecar 一致，包含 24 张预设 WebP（合计 `934,334` bytes、最大 `107,364` bytes）和根目录 `manifest.json`，不含书签导出、图片母版、`output/`、`dashboard-cache/`、桌面路径或私人数据。

## 2026-07-14 — Archive Index preset exclusion

- ARCHIVE INDEX 不再显示内置“灵感预设（Ampira）”分区、筛选项或 48 个预设链接；每日灵感与设置中的预设来源保持不变，个人灵感书签仍可进入索引。
- Playwright 使用安全合成数据覆盖 `1280×800`、`1440×1000`、`390×844`：预设模式的索引只显示“全部”和空状态，每日灵感仍显示 5 张预设卡片；个人模式仍显示 15 条合成灵感入口。三个视口横向溢出均为 `0`，控制台零错误与警告。截图为 `output/playwright/inspiration-preset/archive-index-no-preset-*.png`。
- `node tests\extension.mjs`、Service Worker、全部 core/client ESM 语法检查和 `git diff --check` 均通过。

## 2026-07-15 — Unified inspiration card height

- “今日灵感”在所有断点统一使用 `120px` 卡片高度，宽屏五行网格也改为相同固定行高；工作台高度不足时仅灵感列表内部滚动，资讯与归档卡片尺寸不变。
- 针对 macOS Chrome 用户截图中标签可见而标题与来源被裁掉的问题，卡片内容改为明确的 `22px / minmax(0, 1fr) / auto` 三行网格，并取消依赖平台字体度量的底部隐式行对齐；统一卡高和两行标题截断保持不变。
- Playwright 安全合成预览在 `1280×800`、`1440×1000`、`390×844` 下逐张读取布局尺寸，三个视口的 5 张卡片均为 `120px`，窄屏横向溢出为 `0`；截图位于 `output/playwright/inspiration-unified-height/`。

## 2026-07-15 — Settings and onboarding copy cleanup

- 精简设置页与三步首次引导的简中、繁中和英文文案，删除重复的页面／区块说明、来源解析诊断、算法计数及 Manifest V3、IndexedDB 等实现细节；保留密钥与数据传输、网站权限、同步关闭、书签不修改原数据、清缓存和配置迁移等必要后果说明。
- 缓存状态仅显示已缓存数量与当前状态；来源状态仅保留可用、待授权、需检查及逐来源操作。书签来源摘要、旧详细状态构造器和无调用方翻译键已清理，三种客户端语言保持一致的 837 个键。
- Playwright 使用空设置／空 Dashboard 的安全本地响应，不读取真实书签、密钥、Chrome 配置或运行时缓存。设置页在 `1280×800`、`1440×1000` 与 `390×844` 下横向溢出均为 `0`；窄窗口逐步检查三步引导，控制台零错误与警告。截图位于 `output/playwright/copy-cleanup/`。
- 自动化继续验证灵感预设名称、逐来源授权与单源重试，并新增断言防止重复来源摘要、解析方式和内部实现说明回流。

## 2026-07-15 — Dashboard and reader copy cleanup

- 精简 Dashboard 空态、今日简报、阅读队列、资讯／灵感／归档、站内阅读器、AI 搜索异常、天气授权及扩展恢复提示；移除多源判定、状态恢复、HTTP 状态码、HTML 大小、JavaScript 渲染和原始错误等用户不需要的实现细节，同时保留缓存、授权、打开原站与重试等下一步动作。
- 归档索引现在区分“尚无书签入口”和“筛选无结果”，避免空库状态错误提示用户更换关键词。站内阅读器的“外部打开”统一为等长且更明确的“打开原站”。简中、繁中和英文同步更新，新增空书签状态键并保持语言键集合一致。
- 安全本地预览覆盖 `1280×800`、`1440×1000` 与 `390×844`，三个视口的 `scrollWidth === clientWidth`，控制台零错误与警告；短文案在卡片、归档空态和窄窗口中自然显示。截图位于 `output/playwright/copy-cleanup-dashboard/`。
- `node tests\extension.mjs`、Service Worker、全部 core/runtime/client/locales ESM 语法检查和 `git diff --check` 通过。

## 2026-07-15 — Personal source permission confirmation

- 用户把资讯或灵感切换到个人书签并保存后，设置先持久化，再在“书签来源”页原地显示授权确认；提示按资讯、灵感或两者分别说明未授权后果，并列出本次新增的精确域名。已有待授权的其他服务域名不会混入本次确认。
- “授权并刷新”按钮直接从该次点击同步调用 Chrome 权限请求，只接受去重后的精确 HTTPS 或本机开发域名，拒绝宽泛匹配与不安全远程 HTTP。授权成功后自动重新加载 Dashboard、强制刷新来源并关闭设置；拒绝或选择“暂不授权”会保留已保存的个人来源、保留重试入口，并明确显示个人资讯不更新／个人灵感图片可能为空。
- 简中、繁中与英文文案均已覆盖。Playwright 使用两个安全合成域名验证提示、键盘焦点、拒绝状态、成功请求的域名数组与自动关闭；`1280×800`、`1440×1000`、`390×844` 的页面和设置弹窗横向溢出均为 `0`，控制台零错误与警告。截图位于 `output/playwright/saved-source-permission/`。
- `node tests\extension.mjs`、Service Worker、全部 core/runtime/client ESM 语法检查与 `git diff --check` 均通过。

## 2026-07-15 — Forced refresh after every primary source switch

- 资讯模式、资讯文件夹、灵感模式或灵感文件夹任一保存值变化时，后台设置工作流都会先作废旧缓存代次，再立即调度一次强制刷新；刷新不再依赖设置弹窗关闭后的前端流程。旧刷新尚未完成时，协调器只排队一个替代任务，并在旧任务退出后使用最后保存的来源重新生成缓存。
- 已授权用户继续切换个人来源时同样执行上述强制刷新。首次切到待授权来源时，保存后的后台刷新与授权成功后的显式刷新均保留：即使授权前任务先启动，授权点击也会保证再排队一次可读取新权限的刷新。公共 Feed 补盲开关、附加来源及其他非主要来源设置的既有行为未改变。
- 自动化新增“作废正在运行的代次后收到强制请求”的回归，确认旧代次不可写入、替代刷新必定运行且拥有当前代次。Playwright 使用已授权的安全合成来源连续执行 `Alpha → Beta → Alpha` 两次保存；两次响应均标记后台强制刷新已调度，Dashboard 每次都显示对应来源，设置页未额外重复发起刷新，控制台零错误，`1280×800` 横向溢出为 `0`。截图位于 `output/playwright/primary-source-refresh/authorized-reswitch-1280x800.png`。

## 2026-07-15 — Today events scrollbar gutter

- “今日事件”固定使用三等分行容纳最多三条新闻，三条内容刚好铺满卡片正文区，不再出现内部滚动条或预留滚动槽；标题仍保持单行省略并可在原位展开完整内容。待办事项继续按内容滚动。
- 安全本地预览覆盖 `1280×800`、`1440×1000` 与 `390×844`，三个视口的事件列表均为 `padding-right: 0px`、`scrollbar-gutter: auto`，页面横向溢出均为 `0`，控制台零错误与警告。截图位于 `output/playwright/utility-events-scrollbar-gutter/`。

## 2026-07-15 — Compact card summaries

- 三语资讯卡片提示将正文目标从 `180–260` 字缩短为 `130–180` 字，最低目标从 `140` 字调整为 `110` 字，并要求前 `120` 字独立、完整地表达核心事实与影响；事实、影响、后续关注三段结构和 `64` 字标题上限保持不变。
- AI 摘要缓存与前端文本边界统一从 `280` 字收紧为 `200` 字。卡片摘要策略升级为 v4，旧版长摘要会在后续刷新时按新规则重新整理；带大图卡片维持五行摘要、favicon 卡片维持六行，卡片尺寸和其他布局不变。

## 2026-07-15 — In-app reader translation

- 站内阅读标题区新增三语文章操作；“返回”仅在正文内发生同站跳转后出现，“翻译”仅在 API 密钥、数据传输确认与 AI 域名权限全部就绪时出现。底部窗口工具栏只保留刷新、打开原站和关闭，未配置 AI 时不会创建翻译入口。
- 翻译请求使用当前界面语言作为目标语言，将正文作为纯文本发送给用户已配置的 AI 服务；译文保持在当前阅读窗口，并可通过“查看原文／Show original”在原文与译文之间切换，不覆盖缓存正文。
- 自动化覆盖按钮存在、AI 就绪门控、扩展消息路由和原文切换。`node tests\extension.mjs`、Service Worker、全部 core/runtime/client ESM 语法检查及 `git diff --check` 通过。本地 `file:` 预览被浏览器安全策略阻止，因此未生成新截图；仍需在解压扩展中复核 `1280×800`、`1440×1000` 与窄窗口工具栏。

## 2026-07-15 Release 26.2.1

- Manifest 版本提升至 `26.2.1`，并使用真实支持入口运行 `scripts/verify-extension.ps1 -Package`；扩展测试、Service Worker 与浏览器模块语法检查、发布安全门禁及打包均通过。
- 最终 `dist/ampira-26.2.1.zip` 为 `1,419,595` bytes，SHA-256 为 `ff6372450a28fb6aa5a137e2dccd79c741ae53be61bb4518df921acc64d2364c`；侧车哈希一致。
- ZIP 包含 209 个文件和 24 张预设 WebP；根目录 Manifest 版本为 `26.2.1`，不含测试、`output/`、`dashboard-cache/`、`dist/` 或其他禁止路径。

## 2026-07-15 Release 26.2.2

- 移除阅读器返回操作的遗留重复事件绑定，确保标题区的当前处理器每次点击只执行一次；Manifest 版本提升至 `26.2.2`。

## 2026-07-15 — Reader opening motion

- 此处记录的旧 `650ms` Reader 方案已由当前语义动效系统取代：窗口、背景、工具栏和正文统一使用 `360ms enter`，退场继续使用 `180ms exit`。
- 内容不通过 JavaScript 延时阻塞；阅读表面使用主题令牌，避免深色模式入场时闪白。Reader 骨架现遵循 `160ms/700ms` 的静态到扫光阶段。
- 新动效沿用全局 `prefers-reduced-motion` 规则，减弱动画环境下立即完成，不保留额外等待。
- 隐私安全的合成浏览器回归覆盖 `1280×800`、`1440×1000` 与 `390×844` 深浅主题：三个视口横向溢出均为 `0`，入场中段未出现表面闪白，稳定态正文完全可见；reduced-motion 下计算时长为 `0.01ms`。截图位于 `output/playwright/reader-opening-motion/`。
- 阅读器加载态不再显示孤立的“正在读取正文”提示，改为复用全局扫光原语的文章结构骨架；原提示保留为屏幕阅读器专用状态，并通过 `aria-busy` 明确加载开始与结束。合成视觉回归覆盖 `1280×800` 深色与 `390×844` 浅色，横向溢出均为 `0`，隐藏状态保持 `1×1px` 且正文排版规则未改动；截图位于 `output/playwright/reader-opening-motion/`。
- 使用真实支持入口运行 `scripts/verify-extension.ps1 -Package`；扩展测试、Service Worker 与浏览器模块语法检查、发布安全门禁及打包均通过。
- 最终 `dist/ampira-26.2.2.zip` 为 `1,419,575` bytes，SHA-256 为 `f8efa39b9140680f45caaed2d9eea32e6cf5b185bd71b43212c9e4b97ac4cc20`；侧车哈希一致。
- ZIP 包含 209 个文件和 24 张预设 WebP；根目录 Manifest 版本为 `26.2.2`，不含测试、`output/`、`dashboard-cache/`、`dist/` 或其他禁止路径。

## 2026-07-15 — Permissionless-first Reader

- 站内阅读不再对每个未授权域名预先弹出权限请求。后台先按普通跨来源 CORS 规则、无凭据且无 Referer 地尝试读取；网站公开允许读取时直接显示正文，不申请 host permission，也不写入依赖权限的 Reader 缓存。
- 公开读取失败后使用限时、无凭据、无 Referer 且不读取正文的 HEAD 请求检查网站可达性：网站不可达时保留网络错误，只有网站可达但正文受浏览器跨来源边界阻止时，阅读窗口才显示当前网站的精确授权操作；授权由用户再次明确点击后申请，成功后自动重试并恢复原有权限绑定缓存、最终来源复核和撤权清理边界。
- 可公开读取但返回 404、拒绝访问、不支持内容或正文为空时继续显示对应的具体错误，不会误导用户授权。未增加 `activeTab`、内容脚本、远端抓取代理或新的权限清单。

## 2026-07-15 — Reader recovery and local excerpt fallback

- 未知的扩展后台瞬时中断不再立即落到“站内阅读暂时不可用”：Reader 保持文章形状骨架并在 `450 ms` 后自动重试一次，关闭或打开其他文章会取消旧重试，避免过期结果覆盖当前内容。
- 正文仍未打开但资讯卡已有 Feed 摘录或当前 AI 摘要时，Reader 直接显示结构化的本地摘录，并明确标注它不是原站完整正文；标题区提供“重新读取正文”，底部仍保留打开原站。书签等没有本地文本的入口不会生成虚假降级内容。
- 无本地摘录的最终失败页移除内部错误码，改为显示站点域名、具体原因和明确下一步；普通失败以“打开原站”为主操作，权限失败仍以精确授权为主操作。简中、繁中与英文文案同步，未改变权限、缓存或远端数据边界。
- 使用隐私安全的合成 Reader 失败态完成 Playwright 检查：浅色 `1280×800`、深色 `1440×1000` 与窄屏 `390×844` 均无横向溢出，主操作可通过键盘聚焦，浏览器控制台无错误。截图仅记录在 `output/playwright/`。

## 2026-07-15 — Medium-priority client decoupling

- Dashboard 的存储 hydration、状态创建、DOM 分组和控制器装配改由显式异步工厂启动，导入 `dashboard-app.mjs` 不再直接触发初始化副作用；入口只负责等待工厂并绑定窗口事件。
- Activity、Reader 与 Summary/Daily 之间原先依赖可变控制器变量的前向调用改为具名 action ports，并在控制器创建后显式绑定。卡片进入、退出、复用判断和 identity hash 抽为共享 transition primitive，Daily 与 Summary 不再通过 Daily controller 共享动画实现。
- Chrome runtime 消息、HTTP 路由映射与权限请求集中到 client gateway；通用空状态、图标标签和 segmented indicator 同步逻辑各自收敛为可复用模块，页面控制器只依赖业务动作。
- 架构测试新增生命周期顺序、显式 action-port 绑定、共享卡片 transition 和浏览器 gateway 边界断言；`node tests\\extension.mjs` 与全部浏览器模块语法检查通过。

## 2026-07-15 — Header image layout selector

- 外观设置将相互依赖的“固定头图”和“全屏显示”开关合并为“标准 / 固定 / 全屏”分段选择器，复用现有 segmented 控件的活动指示器、悬停、键盘焦点和忙碌状态。
- 旧配置继续映射到原有 `headerImageFixed` 与 `headerImageFullscreen` 字段，保存格式和头图渲染行为保持兼容；三语标签和说明已同步。
- `node tests\\extension.mjs`、Service Worker、全部 core/runtime/client ESM 语法检查及 `git diff --check` 通过；仍需在解压扩展中复核设置弹窗的三个目标视口。

## 2026-07-15 — Today news background cache state

- “今日资讯”在没有可显示条目且后台刷新正在运行时，不再落入普通空状态；改为直接显示缓存标题、当前来源处理信息和进度条，完成后由既有 Dashboard 更新自动替换为资讯。加载态不使用独立背景、边框、图标或百分比标签，与所在栏位融为一体。
- 刷新请求返回、后台 `refresh.progress` 推送和轮询状态都会同步重绘今日资讯；已有资讯时继续显示原内容，不用加载状态中断阅读。状态使用 `role=status`、`aria-live`、`aria-busy` 与原生进度条语义，并遵循现有 reduced-motion 规则。
- 简中、繁中与英文文案同步；自动化覆盖刷新启动后立即触发 Daily 重绘，以及运行状态和进度条语义的架构约束。Playwright 使用安全合成的 `3/7` 来源与 `42%` 进度覆盖 `1280×800`、`1440×1000` 和 `390×844`，三个视口横向溢出均为 `0`、控制台零错误与警告；截图位于 `output/playwright/news-cache-state/`。未读取真实书签、密钥或运行时缓存。

## 2026-07-15 — Compact content sync controls

- “跨设备内容”的稍后读、待办事项和天气城市三行仅保留操作标题与开关，移除行内重复解释，使设置列表更紧凑；总开关说明、章节级本机存储说明与 API 密钥安全提示继续保留。
- 清理三种客户端语言中三组不再使用的说明键，功能、同步默认值和关闭后的数据处理行为均未改变。尚未执行解压扩展的手动视口 QA。

## 2026-07-15 — AI connection action placement

- “测试连接”移到 AI Provider 上方操作区，紧邻“移除密钥”左侧，使用户点击后更容易看到设置顶部的连接结果。按钮移出权限门控字段组后仍同步当前 Provider 的授权与忙碌状态，未解锁时不可测试。

## 2026-07-15 — Cache action availability

- Dashboard 与设置概览的“缓存”操作现在共用明确的可用性门控：Dashboard 尚未载入、书签访问尚未确认、没有资讯来源、没有已授权来源、后台连接不可用或已有刷新运行时均不可重复点击；已有缓存、部分来源可用或来源处于退避期时仍允许强制刷新。
- SIGNAL FEED 操作区在原按钮行下方悬挂固定 `18px` 状态行，通过绝对定位与 `visibility` 切换三语原因，按钮使用 `aria-describedby` 关联完整文案；状态行不参与按钮行排版，按钮保持原有垂直位置，原因显隐也不会改变资讯网格位置。设置页复用既有后台缓存详情行，不增加弹窗高度。
- Playwright 安全本地布局检查覆盖 `1280×800`、`1440×1000` 与 `390×844`：原因显隐前后操作区高度差和资讯网格位移均为 `0`，三个视口横向溢出均为 `0`。深色、浅色与窄屏截图位于 `output/playwright/cache-availability/`；本地 HTTP 预览不具备 Chrome 扩展 API，因此没有将其控制台状态作为解压扩展 QA 结果。

## 2026-07-15 — Quick bookmark settings visibility

- “快捷书签”关闭时仅保留模块标题、开关与保留数据说明，隐藏名称、网址、添加操作和书签列表；重新开启后立即恢复管理区域，已添加的网址继续保留。
- 管理区域首帧默认隐藏，待设置载入后再按开关状态同步，避免默认关闭时短暂闪现编辑表单。
- Playwright 安全本地预览确认关闭态管理区域高度为 `0`、开启后立即恢复；`1280×800`、`1440×1000` 与 `390×844` 三个视口的横向溢出均为 `0`。本地 HTTP 预览没有 Chrome 扩展运行时，因此仅用于合成显隐与布局检查，未将预期的 API `404` 计为解压扩展控制台 QA。

## 2026-07-15 — Focus-gated source status scrolling

- “来源状态”列表默认不接管鼠标滚轮，指针经过时继续滚动外层设置页；点击列表、键盘聚焦列表或聚焦行内操作后，才启用列表内部滚动。
- 激活后的列表显示现有主题焦点描边，保留 `tabindex`、键盘翻页、行内授权与重试按钮语义；其他设置列表的滚动行为不变。
- Playwright 使用 18 条安全合成来源验证 `1280×800`、`1440×1000` 与 `390×844`：未聚焦时列表滚动量为 `0`、外层滚动 `240px`；点击或键盘聚焦后列表接管滚动、外层滚动量为 `0`，三个视口横向溢出均为 `0`，控制台零错误与警告。截图位于 `output/playwright/source-coverage-scroll/`。
- 默认 Unsplash 首页头图已缩放至 1920px 宽并以 WebP 质量 70 压缩为 `assets/images/default-header.webp`，体积从 2,868,067 bytes 降至 693,996 bytes；首帧恢复与主渲染都会把原默认 URL 映射到该内置文件，已有默认配置无需迁移，用户自定义 HTTPS/本机 HTTP 图片地址规则保持不变。

## 2026-07-15 — English localization cleanup

- 英文侧栏书签短标签调整为 `Saved`，完整提示和无障碍名称继续使用 `Bookmarks`；设置页提示统一使用完整的 `and`，并收敛 Blocked、Ampira Feed 等术语。
- Onboarding、AI 日报、空状态和支持入口改为更直接的自然英文；面向用户的权限文案以 site、domain 或 service 替代 origin，并以 updates 替代容易暴露实现细节的 cache。
- 本次仅修改英文界面文案，不改变 locale 键、权限行为、缓存实现或业务状态。尚未执行解压扩展的手动视口 QA。

## 2026-07-15 — Daily brief loading states

- 今日简报重新整理时保留已有正文，禁用重复操作并以低对比度逐段脉冲和横向扫描明确后台状态；标题区状态同步改为三语“整理中”，卡片暴露 `aria-busy`，完成后新正文以 `160ms` 原位淡入。
- 首次生成且没有既有简报时，正文区域改用三组与最终段落位置一致的双行骨架及现有自动更新说明；状态使用 `role=status`、`aria-live=polite` 和仅供辅助技术读取的完整说明，不再用普通空状态代替加载反馈。
- 动效严格限定在加载状态：段落脉冲只改变 `opacity`；扫描改为固定覆盖层内移动低对比度渐变位置，并在两端淡出，不再裁切正文容器或阻断长简报滚动。所有动效沿用现有主题令牌和全局 reduced-motion 规则；减少动态效果时移除循环扫描并保留静态忙碌状态。
- 隐私安全的合成样式检查在 `1280×800` 同时覆盖已有正文刷新、首次三段骨架和完成态：横向溢出为 `0`，骨架段落数为 `3`，两个加载卡均保留忙碌语义；reduced-motion 下正文透明度固定为 `.62` 且扫描层为 `display: none`。截图位于 `output/playwright/digest-loading/`。当前本地 QA server 与工作区正在进行的 Dashboard DOM 重构不同步，因此未将该检查声明为完整应用流程或解压扩展手动 QA。

## 2026-07-15 — AI Provider brand icons

- Provider 目录与已配置摘要使用本地打包的品牌 SVG；OpenAI、Qwen、OpenRouter 与 Ollama 分别适配深浅主题，其余已收录品牌使用统一图标。智谱 GLM 与豆包 Ark 补齐 SVGLOGO 收录的纯标识，自定义兼容服务继续使用中性的 `API` 字母标识。
- 图标不在运行时访问外部站点，不增加远程请求、主机权限或存储字段；SVG 来源和商标归属说明已加入第三方声明。
- SiliconFlow 使用官网 `logo-new.svg` 的首条紫色标识路径，移除其余中英文字标并收紧画布；不重绘路径、不改变品牌色，继续以本地资源随扩展打包。
- 真实品牌图标取消外围描边、底色和圆角容器，并从 `20px` 调整为 `22px`；仅自定义兼容服务的中性 `API` 占位保留原边界，避免被误认为品牌资源加载失败。
- 浅色主题的未选中 Provider 选项从灰色 `surface-0` 改为白色 `surface-row`，默认边界降为 `line-faint`，只有悬停时使用轻灰 `surface-row-hover`；深色主题与淡紫选中态保持不变。
- 自定义兼容服务的 `API` 字母占位使用 `10px` 与 `.03em` 轻微字距，并移除外围边框、填充和圆角；只作用于没有品牌 SVG 的占位，Provider 品牌图标尺寸不变。
- Playwright 安全本地预览覆盖深色 `1280×800`、浅色 `1440×1000` 与 `390×844` 单列目录；12 个品牌图标均成功解码，品牌图标容器的描边为 `0px`、背景透明，自定义 `API` 占位仍为 `1px` 边界，页面与目录横向溢出为 `0`。截图位于 `output/playwright/ai-provider-icons/`；预览使用安全合成设置，未声明解压扩展手动 Chrome QA。

## 2026-07-15 — Header image settings layout

- “首页头图”设置重新收敛为模式、图片来源和画面调整三层：修正通用双列规则覆盖专用布局的问题，模式选择改为独占首行；网络地址输入与本地图片选择合并为一个图片来源控件，并在同一区域显示当前生效来源；显示高度与模糊控制使用一致的“标题／数值 + 滑杆”层级。
- 显示高度与模糊强度进一步统一为常驻数值调节项：两者都显示标题、说明、右侧当前值与下方滑杆；模糊强度以 `0 px` 表示关闭，不再暴露冗余开关。旧设置继续兼容，原来关闭模糊时映射为 `0 px`，已启用的模糊值保持不变。
- 宽屏使用“标签／主控件／说明”三列；`1120px` 以下在保留设置侧栏时提前收敛为“标签／内容”两列，`520px` 以下再切为完整单列。网络地址与本地选择在空间足够时同行，手机端将输入框和操作按钮拆行；两项画面调整在 `820px` 以下改为纵向排列。所有原有控件 ID、隐藏兼容字段、保存格式与预览状态保持不变。
- `scripts\\verify-extension.ps1` 全量通过，包括统一测试、浏览器模块语法、HTML 资源、清单、本地化、文档与安全约束检查。浏览器安全策略阻止直接打开本地扩展文件，尚未执行解压扩展的手动视口 QA。
- Playwright 安全本地预览覆盖深色 `1280×800`、深色 `1000×800`、深色 `760×800` 与浅色 `390×844`；手机视口额外合成“更换本地图片 + 移除本地图片”双操作状态。头图设置区域各视口横向溢出均为 `0`，截图位于 `output/playwright/header-image-layout/`。本地静态预览的 5 条控制台错误均来自不存在的 `/api/*` 兼容端点，未发现布局相关脚本或资源错误；未声明解压扩展手动 Chrome QA。
- 模糊强度改为独立的常驻调节项后，移除旧依赖设置遗留的左侧紫色归属线；高度与模糊之间仍使用普通分隔线，滑杆进度色保持不变。
- 全屏头图不再禁用或收起“显示高度”滑杆；全屏高度以视口高度为基准，支持 100%–140% 实时预览，进入全屏时会把低于 100% 的现有值校正为 100%；标准与固定模式继续支持 70%–140%，并在首帧恢复已保存的高度。

## 2026-07-15 — Public Feed language isolation and cross-language access

- 简体中文、繁体中文与英文公共 Feed 各固定为 6 个原生来源，并预留 3 个跨语言重大新闻来源。首次公共 Feed 授权沿用既有来源权限路径，一次请求当前语言包全部 9 个精确域名；预留来源在 AI 完整配置前不读取、不刷新、不进入资讯缓存，服务设置不增加第二套授权入口。
- 公共条目统一经过界面语言过滤；个人书签资讯不受限制。跨语言公共条目只有在 AI 标题与摘要的目标语言、策略版本和 Provider 均验证通过后才进入 Dashboard、今日简报、Reader 预览目标、本地／AI 搜索上下文和可见计数。原始外语内容保留在本地 Feed 缓存中等待重试，但不从旁路显示。
- 自动 AI 顺序调整为必需的跨语言本地化、今日简报、可选的同语言卡片摘要；关闭普通卡片摘要不会关闭必需本地化。Feed schema 升级为 3、文章记录升级为 2、卡片摘要策略升级为 5、每日简报 schema 升级为 5，旧 Feed 缓存直接丢弃。
- 自动化使用固定英文、简体、繁体、混合与不确定文本样本，覆盖 6+3 来源包、首次统一请求 9 个精确域名、AI 前来源停用、部分配置、额度与错误语言保护、Provider／界面语言变化、可见投影和旧缓存淘汰。
- Playwright 安全本地预览在 Browser 权限页合成当前简中包的 9 个已授权来源，确认 AI 服务页不存在第二个授权入口；深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844` 均无页面、对话框或来源行横向溢出，键盘焦点可见，reduced-motion 下相关过渡收敛至近零，控制台零错误与警告。截图位于 `output/playwright/public-feed-unified-permissions/`；未声明解压扩展手动 Chrome QA。

## 2026-07-15 — Summary card hover text

- 资讯卡片的摘要文字现在使用未截断的摘要全文作为原生悬浮提示；卡片标题和其他区域继续显示当前标题提示，不改变点击、键盘访问或上下文菜单行为。
- 增加架构回归检查，防止摘要区域重新继承整张卡片的标题提示。尚未执行解压扩展的手动视口 QA。

## 2026-07-15 — Fullscreen cover style parity

- 移除全屏头图对标题、日期、星期、时间、状态文字、日期分隔线和未选中分段按钮的专属样式覆盖；深色、浅色与跟随系统模式现在均继承对应非全屏模式的颜色、字重、阴影和图标透明度。
- 全屏头图的尺寸、定位、遮罩和高度调节行为保持不变；增加架构回归检查，防止全屏模式重新引入独立文字或控件样式。尚未执行解压扩展的手动视口 QA。
- 浅色全屏头图的浅色遮罩进一步降低：纵向四个节点由 `26% / 20% / 38% / 68%` 调整为 `18% / 14% / 28% / 54%`，横向两侧由 `28% / 22%` 调整为 `20% / 16%`；深色模式与非全屏遮罩保持不变。
