# Ampira MV3 QA

本文件只保留当前 Manifest V3 扩展的 QA 基线和最近一次仍有效的界面结论，不作为逐次改动日志。历史实现与发布记录应通过 Git 历史查询。

已移除的 Node 版不属于当前架构。旧 `dashboard-cache/` 仅包含用户自行管理的历史数据；验证、迁移和打包均不得读取、删除或发布它。

## Current Architecture

- `dashboard.html` 是新标签页单页入口；`assets/client/` 使用本地 ESM 组装控制器、视图、状态和消息客户端，不加载或执行远程代码。
- `extension/service-worker.mjs` 只负责 Chrome 事件注册和运行时组装；Chrome 工作流位于 `extension/runtime/`，确定性领域逻辑与适配器位于 `extension/core/`。
- `assets/dashboard.css` 按固定顺序导入 `assets/styles/`；界面保持高密度信息终端风格、单一网格纹理、桌面浮动导航、移动底部导航，以及 `1120px`、`820px`、`520px` 断点。
- 必需权限为 `activeTab`、`alarms`、`bookmarks`、`storage`。`activeTab` 仅用于用户点击工具栏后的当前页稍后读捕获；书签只读。`favicon`、`search` 和精确网站来源均为用户手势触发的可选权限。
- 非凭据设置可进入 Chrome Sync；API Key 只存于 `chrome.storage.local`。Feed、Reader、摘要、阅读状态及其他缓存通过现有 IndexedDB/存储抽象管理。
- UI 同步支持简体中文、繁体中文和英文。远程文本保持惰性，只能通过结构化 DOM 和 `textContent` 渲染。
- Microsoft Edge 使用浏览器中立文案和扩展管理入口；不请求 Edge 不支持的 Chrome 原生 `favicon` 权限，并回退到随包图标。

## Current UI Baseline

- Dashboard、首次引导和设置使用克制的玻璃表面、细边线与现有主题令牌；不得恢复装饰性卡片堆叠、宽幅渐变或营销式大标题。
- 首次引导保留产品说明、来源选择、网站授权和 AI 交接四步。桌面面板目标宽度为 `600px`；窄屏能力标签使用单列，主操作获得初始焦点。
- 设置保留 AI、资讯、书签、屏蔽、外观、浏览器和关于七类。桌面弹窗目标尺寸为 `1040px × 84dvh`；窄屏使用可横向滚动的分类导航，并恢复各分类滚动位置。AI 顶部的额度、缓存和自动整理状态必须常驻：桌面使用中性磨砂底、带列分隔线的三列状态带，不使用紫色装饰渐变；`820px` 以下纵向排列，不使用折叠容器或独立状态卡片。
- 首页视觉减重仅作用于 `#daily`；设置弹窗和其他页面不得继承首页的透明承载层规则。快捷入口继续支持滚动、拖动、渐隐、空状态和键盘焦点。
- “显示高度”和“模糊强度”常驻为“标题 → 滑动条 → 当前值”布局；禁用时保留可见的原生 `disabled` 状态，拖动预览不得改变轨道宽度。
- 深色、浅色和跟随系统主题均须可用；`prefers-reduced-motion` 下不得保留必要性之外的循环或入场动画；强制色模式及不支持 `backdrop-filter` 时必须回退到可读实色表面。
- 浮动选择器、菜单和弹出列表不得使用 `scrollbar-gutter: stable`，短列表左右内边距应保持对称。
- AI 对话正文继承全局自定义滚动条，不得用局部 `scrollbar-width` 覆盖项目统一的宽度、轨道、滑块和按钮样式。

## Automated Verification

每次代码或内容修改运行：

```powershell
.\scripts\verify-extension.ps1
```

验证至少应覆盖：

- `node tests/extension.mjs` 统一测试入口，以及全部扩展 ESM 的语法检查。
- Manifest 权限、CSP、静态本地资源、非字面量动态导入、远程代码和不安全 HTML sink 禁令。
- 三种客户端语言与三套 Chrome Manifest locale 的键和值完整性。
- 消息信封、请求关联、过期响应保护、权限代际、刷新代际与并发写入顺序。
- 设置归一化、Chrome Sync 边界、本机凭据隔离、IndexedDB 缓存配额、裁剪和失效策略。
- Feed、Reader、图片、网络地址与权限策略的安全边界和失败回退。
- `extension/service-worker.mjs` 的 composition-root 约束、core/runtime 依赖方向和本地 CSS import 完整性。
- 生产模块入口可达性、已删除迁移/版本兼容标识符，以及 CSS 选择器的静态引用卫生；动态类名须在测试白名单中显式登记。
- 文档、商店材料、支持 URL、打包允许列表及敏感内容检查。

对策略、存储、权限、解析器、运行时或 UI 模型的行为改动，应在 `tests/suites/` 增加聚焦覆盖并从 `tests/extension.mjs` 注册。

## Browser Verification

UI 或交互修改后，使用安全合成数据的本地预览或加载已解压扩展验证：

- 深色 `1280×800`、浅色 `1440×1000` 和窄屏 `390×844`；必要时补充相反主题和系统主题。
- 页面、设置、引导、Reader、菜单和弹窗横向溢出均为 `0`，控制台无错误。
- 键盘焦点、Escape/背景关闭、关闭后焦点恢复、滚动位置恢复、加载/空/错误状态均正常。
- 减少动态效果下无持续动画；强制色模式下文字、边界、选中态和焦点仍可辨识。
- 权限只能从用户手势发起；拒绝或外部撤权后界面保持可用并立即反映状态。
- 新标签页安装状态必须如实说明，不得显示可写的伪开关；内部搜索必须明确标为 Ampira 内容搜索。
- 不得使用真实 API Key、私人书签、Chrome Profile 或运行时缓存进行测试或截图。
- 仅将有意保留且隐私安全的截图写入 `output/playwright/`；未实际执行加载已解压扩展的手动 QA 时必须明确说明。

安全合成预览记录只能作为当次变更证据，不构成持续有效的手动浏览器 QA；是否完成真实 Chrome/Edge 验证必须在发布检查中重新确认。

## 2026-08-14 — 侧边导航点击后自动收缩

- 桌面侧边导航继续在鼠标悬停或键盘可见焦点进入时展开，但普通鼠标点击留下的按钮焦点不再通过 `:focus-within` 锁住展开态；鼠标离开后会恢复紧凑宽度，当前选中项与焦点样式不变。
- 安全本地 Playwright Chromium 回归确认：深色 `1440×1000` 下悬停从 `64px` 展开到 `109px`，点击“书签”并移出后恢复 `64px`；浅色 `1280×800` 下移出后恢复 `56px`，随后按 Tab 产生键盘可见焦点并展开到 `100px`。两个视口横向溢出均为 `0`。控制台仅有静态预览缺少三个 `/api/*` 接口的预期 404；没有读取真实书签、密钥、Chrome Profile 或运行时缓存，未保存截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-14 — 浅色全屏头图关闭叠层

- 显式浅色与跟随系统浅色模式下，全屏头图不再绘制 `.cover-fade` 可读性叠层；全屏深色模式以及普通、固定头图继续使用原有渐变，头图图片透明度、模糊、构图和标题阴影不变。
- 安全合成 Playwright Chromium 预览确认：显式浅色 `1440×1000`、跟随系统浅色以及浅色 `390×844` 的计算叠层均为 `none`，图片透明度为 `1`，两个实际视口横向溢出均为 `0`；深色 `1280×800` 仍使用原有双层渐变。控制台仅有静态预览缺少三个 `/api/*` 接口的预期 404；未保存截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-13 — 首屏搜索聚焦去除颜色叠层

- 首屏搜索框聚焦时继续使用现有主题色外圈描边，但不再把玻璃底切换为带重点色的悬停背景；深色与浅色模式保持同一中性表面。

## 2026-08-12 — 首屏与资讯换批玻璃层稳定

- 首屏搜索框不再跟随父容器执行 `opacity + transform` 合成动画；搜索框玻璃层从首帧起保持实时，仅无模糊依赖的搜索操作图标保留原有延迟淡入。
- SIGNAL FEED 换批继续对文字内容和缩略图执行原有先出后入，但右上角操作胶囊移到 View Transition 快照之外，并在同一个持久 DOM 根上更新按钮，避免 `backdrop-filter` 在快照切换时短暂失效。
- 自动验证固定搜索框不得重新进入父级合成动画，并固定资讯操作胶囊必须保持实时节点。安全合成 Playwright Chromium 回归采样 102 个搜索首屏帧（其中 57 个处于入场窗口），模糊始终为 `blur(18px)` 且父层动画始终为 `none`；SIGNAL FEED 在深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 分别采样 17、19、19 个有效换批帧，操作胶囊节点、模糊、透明度与几何全程不变，三个视口横向溢出均为 `0`，没有动画残留或控制台问题。隐私安全截图位于 `output/playwright/blur-stability/`；本次没有读取真实书签、密钥、Chrome Profile 或运行时缓存，未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 今日灵感与 SIGNAL FEED 网络图片淡入

- 两类卡片的网络图片在真正加载、尺寸校验和 `decode()` 完成前保持透明，再使用共享的 `180ms` 状态动画淡入；加载失败仍进入原有候选图恢复或占位图路径，不会先闪出无效图片。
- 首次渲染遇到浏览器缓存命中时直接显示，避免重复闪烁；卡片已经显示后才异步解析出的预览图即使已预加载，也会强制执行一次淡入。动画只作用于图片节点，不改变现有“换一换”的卡片与缩略图换批时序。
- 新增行为测试覆盖慢加载、缓存命中、异步占位替换和无效尺寸。安全合成 Playwright Chromium 在深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844` 下分别验证今日灵感和 SIGNAL FEED：每类均从 `opacity: 0` 开始，解析到 `previewImageIn / 180ms`，每次采到 4–5 个可见中间帧并落到主题目标透明度；三个视口横向溢出均为 `0`，控制台零错误与警告。减少动态效果下时长压缩为 `.01ms` 并直接落到最终透明度。隐私安全中间态截图位于 `output/playwright/image-reveal/`；没有读取真实书签、密钥、Chrome Profile 或运行时缓存，未执行加载已解压扩展后的手动 Chrome/Edge QA。

2026-07-17 的 AI 运行状态带使用安全合成数据覆盖深色 `1280×800`、浅色 `1440×1000` 和浅色 `390×844`：桌面三列、窄屏三行，分隔线方向正确，三个状态值无截断，页面与设置横向溢出均为 `0`，控制台零错误与警告。截图位于 `output/playwright/runtime-status-band/`；未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 资讯缓存期间图标节点稳定

- 图标继续使用 `is-icon-pending` 隐藏尚未加载或失败的本地图片，不改变 broken-image 防护；DOM 内容比较改为只在克隆节点上剔除这一临时类，真实节点的加载状态不受影响。
- 今日资讯、今日灵感列头和换批按钮复用原有已加载图标；每日/摘要卡片标签与效率卡片也使用同一比较规则。真实文本、样式或数据变化仍会触发正常更新。
- 安全合成浏览器回归模拟 20 次缓存进度重绘并强制新图标处于 pending：旧比较产生 60/60 次误判，修复后为 0/60；实际子节点替换为 0，截图中的四个原图标保持 4/4 连接，实时 pending 图标为 0。深色 Dashboard 在 `1280×800` 和 `390×844` 下相关图标均已完成加载且可见，页面横向溢出为 0，控制台错误与警告为 0。
- `node tests/extension.mjs` 与 `scripts/verify-extension.ps1` 均通过。本次使用本地静态页面和安全合成数据，没有读取真实书签、密钥、Chrome Profile 或运行时缓存；未保存截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-11 — 已看归档清空确认

- “已看归档”的“清空”入口移除红色危险文字，恢复为与同级普通操作一致的文字颜色；垃圾桶图标、按钮尺寸和交互状态保持不变。
- 点击后先使用共用原生确认弹窗说明影响。取消、Escape 或点击弹窗外部不会改动归档；只有确认后才清空归档并重置这些内容的已看状态。最终确认按钮继续使用危险操作强调，三种客户端语言同步提供文案。
- 安全合成 Playwright 预览确认浅色入口与同级标题计算颜色均为 `rgb(21, 25, 31)`，深色均为 `rgb(236, 238, 242)`；Escape 取消后保留原有 3 条内容并把焦点恢复到“清空”，确认后才显示“归档已清空”。深色 `390×844` 下页面 `scrollWidth` 为 `390`，弹窗左右各保留 `16px`，无横向溢出。
- 三语言子集、目标模块语法和新增架构断言通过；完整验证继续被当前工作区已有的 `daily-view.mjs` 模块行数预算拦截（636 行，上限 520 行），未擅自改动同文件中尚未提交的换批动画重构。本次未保存截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-10 — 封面显示高度上限提升到 200%

- “显示高度”保持 70% 下限与 5% 步进，上限由 140% 提升到 200%。滑杆属性、界面实时预览、首帧缓存恢复、核心设置归一化、导入导出校验和保存事务使用同一边界；控制器会主动同步滑杆的最小值、最大值与步进，避免 HTML 与运行逻辑再次分叉。
- 聚焦回归覆盖 200% 的归一化、超过上限时的截断、205% 导入拒绝、200% 导入导出和保存后不回落。安全合成 Playwright Chromium 预览覆盖深浅主题的 `1280×800`、`1440×1000` 与 `390×844`：页面和设置横向溢出均为 `0`，键盘 `End` 可到达 200%，全屏头图高度为 2 个视口，刷新后的首帧缓存恢复、减少动态效果和 195% → 200% 保存请求/响应均正常，控制台错误与警告均为 `0`。
- 隐私安全截图位于 `output/playwright/header-height-200-*.png`。本次使用本地静态页面与模拟接口，没有读取真实书签、密钥、Chrome Profile 或运行时缓存；未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-19 — 摘要卡片操作悬停统一

- “手动整理摘要”移除独有的悬停底色与图标放大，和稍后读、已读按钮统一为仅改变前景色与图标滤镜；点击回弹、禁用状态、键盘焦点和减少动态效果行为保持不变。
- 架构测试固定三枚按钮共享同一悬停规则，并防止重新引入仅供手动整理使用的背景或缩放动画。
- 隔离 Playwright Chromium 加载当前工作区扩展并使用安全合成资讯检查深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844`：三枚按钮悬停背景均为透明，图标均保持 `16×16`，按钮与图标无 transform，悬停动画名为 `none`；三种视口横向溢出均为 `0`，控制台错误与警告均为 `0`。本次未保存截图，也未使用真实书签、密钥、Chrome Profile 或运行时缓存；未执行人工 Chrome/Edge QA。

## 2026-07-19 — 商店文案与品牌广告语

- 首屏定位改为“从书签里的网站获取新资讯，再用 AI 整理和解读。”，繁体中文与英文按各自语言习惯改写。Manifest、Chrome Web Store 与 Microsoft Edge Add-ons 的三语短描述完全一致，长度均不超过 132 个字符。
- 六份商店正文保留收藏后很少再打开、关注来源分散这一痛点，并加入长期跟踪行业或研究主题的现有用法。主要功能按书签资讯、排序、AI、每日灵感、阅读流程和辅助工具排列；数据与权限部分继续说明书签只读、本地保存与同步边界、API Key 隔离和精确来源授权。
- 文案没有写入 Pro、价格、订阅、路线图或尚未上线的功能。`humanizer-zh` 复核后，按直接性、节奏、可信度、真实感和精炼度五项各 10 分评分：简体中文 48/50，繁体中文 47/50，英文 47/50。

## 2026-07-19 — 文案规范

- 新增 `copy-guidelines.md`，集中记录核心定位、锁定文案、功能顺序、专业场景边界、权限事实、三语术语、各页面结构和变更检查。文档用于约束消费者文案，不替代隐私实践、权限说明和审核材料。
- 一致性测试确认规范包含三语广告语、商店摘要、痛点开场、专业场景、AI 设置与首次设置标题，以及当前 Chrome Web Store 地址。修改这些锁定内容时，必须同时更新实际页面和规范。
- 规范按 `humanizer-zh` 检查直接性、节奏、可信度、真实感和精炼度，评分为 47/50。本次只新增文档与断言，没有界面或运行逻辑变化，因此未执行手动 Chrome/Edge QA。
- 三语 Manifest 简介改用品牌广告语，使浏览器“扩展程序”管理页直接显示产品核心定位。Chrome、Edge 商店短描述和官网 Meta 描述继续使用包含排序、AI 与本地保存信息的功能摘要。
- Chrome 与 Edge 的三语商店稿拆成 `*-short.txt` 和 `*-description.txt`。文件只保留可提交的纯文本，删除 Markdown 标题、列表语法和字段说明；短描述与详细描述均可全选后直接粘贴到对应后台字段。
- 三语官网首页、设置“关于”以及首次设置第 1 步和第 4 步均通过本机只读预览，在 `1440×1000` 与 `390×844` 下检查。页面与目标容器没有横向溢出或截断；英文“关于”广告语在窄屏自然换为两行。隔离的布局预览没有控制台错误或警告。本次没有保存截图，也未加载已解压扩展执行手动 Chrome/Edge QA。

## 2026-07-19 — 官网品牌 Logo

- 三语官网首页在 AMPIRA 字标前加入现有产品 Logo，固定显示为 `48×48`。GitHub Pages 使用的 SVG 位于 `docs/assets/`，一致性测试会核对它与扩展内图标完全相同。
- 本机只读预览覆盖深色 `1280×800`、深浅色 `1440×1000` 与 `390×844`。三语 Logo 均以 `104×104` 原始尺寸正常载入，显示尺寸、字标和语言导航没有挤压或截断，页面横向溢出为 `0`，控制台没有错误或警告。本次没有保存截图。

## 2026-07-19 — 官网与 GitHub 商店入口

- 三语官网首页在痛点说明后加入 Chrome Web Store 安装按钮；GitHub README 的三语顶部链接行同步加入同一商店地址。按钮使用直接的本地化动作名称，并以新标签页安全打开。
- 安装按钮最小高度为 `44px`，白字与按钮背景对比度约为 `4.9:1`，并提供 `:focus-visible` 焦点轮廓。本机只读预览覆盖深色 `1280×800`、深浅色 `1440×1000`、`390×844`，并补测 `320×800`。三语页面均无横向溢出；繁中与英文按钮只在 `320px` 下自然换为两行。控制台没有错误或警告，本次没有保存截图。

## 2026-07-19 — 首页滚动导航无视觉性能收敛

- 首页导航活动态由逐帧 `scroll` 回调改为 `IntersectionObserver` 的栏目边界观察；栏目激活线、隐藏书签栏目过滤、平滑定位及设置/搜索关闭后的活动态恢复保持不变。
- 导航按钮与栏目节点在控制器内缓存，只有活动入口实际变化时才写入 `active` 类；设置和 AI 搜索入口复用同一更新路径，避免状态缓存与直接 DOM 写入分叉。
- 固定或全屏封面的实时模糊没有改为预生成位图：远程图片的 Canvas 跨域限制和可调模糊半径无法保证像素与实时预览严格一致。本次未修改任何首页 CSS、颜色、透明度、模糊强度、间距或动画。
- 自动验证覆盖观察器、无逐帧滚动监听、隐藏栏目选择和活动态写入去重。安全本地 Chromium `1440×1000` 预览确认滚轮栏目切换、AI 搜索/设置活动态及关闭后恢复正常；控制台仅出现静态预览预期的 `/api/*` 404，没有新增运行时错误。本次未在 macOS 真机或加载已解压扩展后执行手动 Chrome/Edge QA。

## 2026-07-19 — 首页玻璃模糊开关

- “外观 → 交互效果”新增“首页玻璃模糊”开关，默认开启，现有用户与新安装的默认视觉保持不变；非凭据设置继续通过既有 Chrome Sync 设置链路保存，并支持设置导入导出。
- 关闭时只移除 `.main` 首页内容区元素及伪元素的 `backdrop-filter`；侧栏、设置、确认弹窗、搜索、Reader 和头图自身的图片模糊不受影响。透明背景、颜色、边线、阴影、间距与动效保持原样。
- 本地首帧提示会在样式加载前恢复开关状态，避免已关闭用户刷新时先短暂出现玻璃模糊。简体中文、繁体中文和英文文案明确说明性能作用范围。
- 安全本地 Chromium 计算样式确认首页效率卡从开启时的 `blur(18px)` 切换为关闭时的 `none`；同一时刻侧栏仍为 `blur(14px) saturate(1.06)`、设置遮罩仍为 `blur(14px) saturate(1.1)`，头图 `filter` 数值不变。
- 浅色 `1440×1000`、深色 `1280×800` 和深色 `390×844` 的页面与设置横向溢出均为 `0`，窄屏开关完整位于设置面板内。控制台仅有静态预览缺少 `/api/settings`、`/api/settings/header-cover` 和 `/api/dashboard` 的预期 404；本次未在 macOS 真机或加载已解压扩展后执行手动 Chrome/Edge QA。

## 2026-07-19 — 天气多模型降水与极端天气校正

- 天气数据源继续只使用 Open-Meteo。中国地点以 CMA GRAPES Global 为主要降水与天气现象依据，同时请求 ECMWF IFS、GFS 和 ICON 交叉验证；CMA 字段缺失或包含 CMA 的整次请求失败时自动重试全球模型，不复用不同日期或不同模型批次的旧 CMA 数据。
- 降水概率改为 ECMWF 与 GFS 的逐小时均值；未来日期只统计当地 `08:00–20:00`，当天只统计当前小时之后。天气现象先在同一小时内完成跨模型验证，再汇总为日况，错开时段的模型结果不能互相凑票；小于 `0.2 mm` 的原始逐小时降水忽略，雷暴小时原始降水至少需要 `0.5 mm`，全球模型至少两个独立支持才显示降水或雷暴；CMA 雷暴也必须获得同小时全球模型支持。天气现象覆盖 CMA 时，降水量同步改用支持该现象的全球模型；被日况拒绝的低置信度孤立降雨不进入累计，单模型确实主导为雨时仍保留其降水证据，避免天气现象与降水量来源分叉。
- 天气缓存使用尚未发布的日期感知 v3 记录，地点、国家代码、本地日期或模型批次不一致时不复用。界面优先显示平均概率、峰值、预计降水量和有雨小时数；CMA 同意最终现象且至少两个全球模型支持，或三个全球模型一致时才标记高置信度。简体中文、繁体中文和英文文案同步更新。
- 安全合成天气数据在深色 `1440×1000`、浅色 `1280×800` 和深色 `390×844` 下均无横向溢出；长说明使用现有省略样式，完整内容保留在 `title` 中。本次未加载已解压扩展，也未使用真实位置缓存、书签或密钥。
- 聚焦测试覆盖 CMA 可用、字段缺失与请求级失败、同小时/错时模型支持、天气现象与降水证据同步、CMA 冲突置信度、单模型极端天气降级、原始降水阈值、当天已过小时排除、空数值拒绝、国家模型选择和跨日期缓存失效。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-07-19 — 通用图片质量与双用途预览

- 页面图片候选改为统一的带来源记录，并以 `article`、`visual` 两个 profile 分离资讯与审美卡片；结构、声明尺寸、客户端真实尺寸、缓存身份和并发请求身份均使用当前图片策略版本，不包含站点专属规则。
- 安全合成夹具覆盖页头箭头、二维码、页脚 Logo、纯文字新闻、`main` 大图、响应式 `srcset`、固定元数据图、Hero 背景、旧缓存、重复默认图、RSS 图片和 profile 隔离。客户端覆盖成功加载但真实尺寸不足时的候选轮换、会话拒绝缓存、资讯 favicon 回退及审美主视觉保留。
- 缺陷回归补充覆盖 JSON-LD 内嵌 publisher/author 图片、图片节点自身的分享类名、无图与正文图共同参与的默认图占比、Feed 补图复用抑制、已拒绝图片的缓存快路径，以及 visual 候选被会话拒绝后继续 Brave 回退；默认图统计现由 Preview 与 Feed 共用同一串行记录器。
- P1 修复后，article 的 Preview 与 Feed 缓存额外保留最多三条原始候选，缓存重读继续使用原始 metadata/main 签名统计，已抑制的站点默认图不会因空候选或正文回退候选而重新出现；缺少原始候选字段的旧 article 缓存自动失效。结构夹具同时确认 `hidden` 容器及 `aria-hidden="true"` 图片不会进入新闻候选。
- P2 修复后，Hero、Main、Cover 等正向语义只按完整 token 或合法的图片后缀匹配，`maintainer-photo` 不再因包含 `main` 子串被误判，同时保留 `hero-masthead` 等主视觉写法。文章复用身份会移除 UTM、点击 ID 和常见邮件活动参数并排序剩余查询参数，同一文章的跟踪链接变体只占一个观察位。
- 36氪二维码回归定位到正文根节点过宽：通用选择器现会在长文章壳内继续收敛到承载大部分正文与段落的更深正向容器，已有明确正文根节点时不再把外层正向壳中的辅助图片视作 `main` 回退。两篇公开文章的实时 HTML 均只保留 OG 与正文图片，辅助区静态二维码候选为零；没有加入域名或类名专属规则。图片策略缓存提升到版本 3，旧二维码候选自动失效，无需手动清缓存。
- 本地 Chromium 使用 `36×36` 合成坏图确认资讯自动轮换到 `640×360` 正文候选；只有坏图的资讯回退为随包 favicon；五张审美卡均保留 `640×360` visual Hero。预览请求同时实际出现 `article` 与 `visual`，资讯没有进入 Brave 回退。
- 深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 的页面横向溢出均为 `0`，控制台错误与警告均为 `0`。截图和安全合成服务位于 `output/playwright/image-quality-v2/`，不含真实 API Key、私人书签、Chrome Profile 或运行时缓存。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过。本次使用本地 Chromium 静态运行时完成界面验证，未加载已解压扩展，也未执行真实中新网、36Kr 网络抓取。

## 2026-07-19 — 后台启动资源 P0 收敛

- 中国县级以上地点索引从服务工作线程的静态模块图移出，改为用户提交有效天气地点搜索后才读取扩展包内 JSON；首次并发搜索共用一次读取和解析，成功后在当前服务工作线程生命周期内复用。索引响应、大小、记录数、字段和坐标均经过边界校验，读取失败时仅回退到原有、已获授权的 Open-Meteo 地理编码流程。
- 客户端继续保留完整的简体中文、繁体中文和英文词典；服务工作线程改为使用由同一词典按明确键策略生成的精简目录，locale 归一化和格式化逻辑提取为无词典依赖的共享模块。自动验证约束后台依赖图不得重新引入完整客户端词典或旧地点模块，生成词典必须与三套客户端源词典逐键一致。
- 发布白名单仅新增固定的 `extension/data/china-locations.json` 和三份生成后台词典；旧的 eager 地点模块会触发打包失败。地点数据生成器直接输出压缩 JSON，后台词典生成器支持 `--check`，完整验证会拒绝过期生成物。
- 服务工作线程静态模块图实测为 82 个模块、582,579 字节；其中后台三语词典共 55,505 字节，完整客户端词典与 508,524 字节地点 JSON 均不在启动图。架构测试设置 640 KiB 启动预算，并持续约束完整词典和旧地点模块不得重新进入后台图。
- `node tests/extension.mjs`、`.\scripts\verify-extension.ps1` 与设置真实支持地址后的 `.\scripts\verify-extension.ps1 -Package` 均通过。发布 ZIP 为 2,258,847 字节、281 个文件；根 Manifest、地点 JSON、三份后台词典、包清单与 SHA-256 边车均经复核，旧地点模块不存在，三处 ZIP 哈希一致。
- 使用全新临时配置加载 Playwright 开发版 Chromium：service worker 正常启动，打开首页前没有地点 JSON 请求；首次 `慈溪` 搜索读取一次 JSON，后续 `Cixi`、重复搜索和 `390×844` 窄屏 UI 搜索均复用同一解析结果，候选与 GeoNames 署名正常且横向溢出为 `0`。UI 流程仅模拟原生权限气泡返回“已同意”，扩展实际未获得网站来源权限；`Paris` 搜索因此显示本地化授权提示，并确认没有发出 Open-Meteo 请求。页面与 worker 控制台错误和警告均为 `0`，未读取真实 Chrome Profile、书签、密钥或缓存，也未保留截图。

## Release Verification

发布前设置真实公开 HTTPS 支持地址并运行：

```powershell
$env:REQUIRED_SUPPORT_URL = "https://github.com/sunpanran/Ampira/issues"
.\scripts\verify-extension.ps1 -Package
```

- 版本只从 `manifest.json` 读取，不在文档或自动化中硬编码。
- 检查生成的版本化 ZIP、SHA-256 sidecar 和 release manifest；ZIP 根目录必须直接包含 `manifest.json`。
- 确认产物不含 `dashboard-cache/`、测试、输出、隐藏配置、密钥、本机绝对路径、远程代码或允许列表外文件。
- 确认 `docs/` 页面已发布，三语商店文案、隐私披露、权限理由、审核说明与实际行为一致。
- 不得手工编辑 `dist/` 生成物。

## 2026-07-17 — Feed 与刷新策略解耦

- `extension/core/feed.mjs` 保持原公开接口，通用 Feed 文本、URL、去重与标记解析下沉到 `feed-utils.mjs`，降级日报生成下沉到 `feed-digest.mjs`；主模块由 1031 行降至 837 行。
- 刷新源状态、缓存复用和多来源证据选择从刷新编排中拆到 `extension/runtime/refresh-policy.mjs`，保留 `refresh-service.mjs` 的兼容导出和现有调用方。
- 架构测试增加聚焦模块体积上限，防止新边界重新回流成巨石；统一测试覆盖 Feed 解析、摘要、刷新策略和安全约束。

## 2026-07-17 — 三个主模块回退与局部保留

- 主页效率模块、内容栏和摘要卡片恢复基础表面与间距；仅保留每日快捷书签的透明承载层、`4px 0` 内边距、无二次模糊与溢出渐隐收口。
- 资讯卡片恢复基础边线、玻璃底色、阴影、正文间距和 favicon 占位光；资讯标签、时间/重要排序、“换一批”和“缓存”按钮保持不变。
- 书签页不保留任何 `#library` 卡片减重覆盖；分类面板和入口行恢复基础规则，同时保留类型选择器、分类选择器及分类/类型标签。
- 首页、资讯和书签的鼠标跟随光晕恢复为基础规则：大模块 `.10 / 260px`，可点击内容 `.09 / 240px`，显隐使用 `--motion-state-duration`；光晕开关与减少动态效果行为保持不变。
- 主页条目、资讯摘要、书签内容和资讯列表标题恢复共享的原始强调色悬停规则。
- 使用安全合成数据完成深色 `1280×800`、浅色 `1440×1000`、窄屏深色 `390×844` 检查：三个主模块在全部视口的横向溢出均为 `0`，控制台错误为 `0`。主页效率卡和内容栏恢复 `12px / 14px` 间距与内边距；资讯卡恢复 `10px / 16px` 桌面正文参数与 `.46 / blur(22px) / saturate(1.65)` favicon 占位光；书签分类恢复 `12px / 14px`。
- 合成预览包含 4 个每日快捷书签、10 个资讯标签和操作区、2 个资讯排序按钮、3 个书签分类标签及具体类型下的 4 个分类选项。设置样式和 DOM 未修改；截图位于 `output/playwright/dashboard-partial-rollback/`。
- `git diff --check`、`node tests/extension.mjs` 和 `.\scripts\verify-extension.ps1` 通过。
- 截图和预览不含真实 API Key、私人书签、Chrome Profile 或运行时缓存。本次未执行加载已解压扩展的手动 Chrome QA。

## 2026-07-17 — 五入口导航轻量化

- 今日、资讯、书签、搜索、设置五个入口的顺序、图标、文案、DOM 标识和交互保持不变；静态选中态继续使用既有强调色边线、胶囊底色、强文字和阴影。桌面按钮维持 `42px`，`1440px` 以上维持 `48px`，移动端维持 `42px` 纯图标触控目标。
- 桌面轨道改用导航专属的半透明主题表面、弱边线、轻量模糊和克制阴影；移除按钮鼠标径向光晕。未选中项悬停仅显示低对比表面，选中项仅在悬停时轻微调整底色；悬停与 `focus-within` 展开、三语动态宽度和标签淡入逻辑保留，并增加语言安全余量。
- 使用安全合成数据完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查：页面横向溢出均为 `0`，英文 `Settings` 展开标签无裁切，移动端标签保持隐藏，书签关闭后底栏由 5 项正确重排为 4 项。键盘焦点具有清晰 `2px` 轮廓，减少动态效果下标签过渡时长降为 `0.00001s`。
- 今日、资讯、书签滚动定位正常；搜索和设置打开时活动态正确切换，关闭后恢复至当前内容入口。控制台错误和警告均为 `0`。截图位于 `output/playwright/navigation-lightweight/`。
- `git diff --check`、`node tests/extension.mjs` 和 `.\scripts\verify-extension.ps1` 通过。截图和预览不含真实 API Key、私人书签、Chrome Profile 或运行时缓存；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — 浅色确认弹窗表面层级调整

- 共享确认弹窗的浅色主题统一使用同一种浅色叠层材质，内容区与操作区不再出现上浅、下深的材质断层；未保存、导入、清空、删除等所有复用该组件的确认弹窗同步生效，深色主题保持不变。
- 显式浅色与跟随系统浅色使用相同规则；弹窗结构、边线、按钮、焦点、背景关闭和焦点恢复逻辑不变。

## 2026-07-17 — SIGNAL FEED 资讯卡片内部排版微调

- 顶部信息行收敛为分类标签与紧凑操作组；稍后读、已读和手动整理继续保留原交互与可访问名称，桌面使用 `28px` 紧凑控件，窄屏恢复为 `32px` 触控尺寸。
- 来源与发布时间合并到标题下方的单行署名区域，时间改用语义化 `time` 元素；长来源允许安全省略，完整来源保留在 `title` 中，时间保持等宽数字且不换行。
- 标题使用更稳定的两行层级与紧凑字距；摘要紧跟来源信息从顶部自然排列，不使用底部固定、额外留白或分隔线，空摘要也不会占位。真实图片与 favicon 占位光的既有遮罩、卡片尺寸和五/六行摘要额度保持不变。
- 使用安全合成数据完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查：三个视口横向溢出均为 `0`，来源与时间保持同排，四列桌面卡片顶部不再因操作组产生换行，控制台错误与警告均为 `0`。截图位于 `output/playwright/signal-feed-card-layout/`。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过。截图和预览不含真实 API Key、私人书签、Chrome Profile 或运行时缓存；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — 客户端状态写保护生命周期收敛

- `assets/client/storage.mjs` 的本地写保护现在只覆盖仍待确认的有效持久化写入；成功写入或明确不可重试的拒绝都会释放对应键，使内容同步或其他页面之后发来的合法外部状态能够生效。
- 较旧批次完成时不会释放同一键上更新的待写值，继续保留 hydration、运行时回显和重叠写入期间的防陈旧覆盖语义；超限或非法键值只隔离自身，不会永久锁住后续有效外部状态。
- 后台客户端状态仓库跳过空补丁的存储读取，并在补丁值与当前记录一致时跳过整份 IndexedDB 重写，减少同步回显与重复保存造成的写放大；显式替换与重置仍保持原语义。
- 状态补丁复用同一份条目列表完成空值与相等性判断，前后台字节边界校验复用模块级 UTF-8 编码器，减少高频保存路径中的短生命周期分配。
- 状态仓库与浏览器端存储测试分别整理到 `tests/suites/client-state-store.mjs` 和 `tests/suites/client-storage.mjs`；统一入口仅负责注册，覆盖成功确认、重叠写入、不可重试拒绝和超限隔离后的外部恢复。

## 2026-07-17 — CSS 基础规则整理

- 合并资讯、效率、分类等卡片共享选择器的连续重复规则，保留原有边线、圆角、磨砂、隔离和过渡计算值。
- 将天气地点列表与待办列表的共享布局和各自间距拆清，移除待办列表 `3px` 后立即覆盖为 `0` 的冗余层叠。
- 弹窗头部与操作栏只共享布局声明，边线分别归属头部底边和操作栏顶边，不再依赖操作栏覆盖公共底边；选择器特异性与最终视觉保持不变。

## 2026-07-17 — 自定义重点色 HEX 取色器

- 将“自定义”重点色从浏览器原生 `type="color"` 面板替换为 Ampira 自绘顶层浮层；复用现有胶囊按钮、磨砂表面、紧凑控件、焦点环与颜色令牌，默认并仅以大写六位 HEX 作为文本格式，设置字段和保存流程保持不变。
- 浮层提供二维饱和度/亮度区域、色相滑杆、颜色预览和 HEX 输入；拖动、粘贴、Enter 提交、Escape 关闭、外部点击关闭与窗口重排均可用，纯亮色边缘的选中点不会被裁切。
- 二维区域支持方向键微调、Shift 加速、Page Up/Down 与 Home/End；关闭按钮和 Escape 会把焦点返回“自定义”触发按钮，Escape 不再冒泡触发设置页未保存确认。简体中文、繁体中文和英文文案同步补齐，并提供强制高对比度回退。
- 修正取色器处于通用 `.field` 容器时的样式串扰：HEX 输入和色相滑杆不再继承普通文本框的高度、内边距、边框与焦点光圈，HEX 行恢复单层对齐；二维调色区域改用四边内缩的独立渐变层，四个圆角都由中性底色和边框完整包覆，不再透出颜色像素。
- 使用不读取扩展缓存、书签或密钥的本地静态预览完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查；三种视口均无取色器横向溢出，移动端打开时会先将触发按钮滚入可用视区，再把浮层锚定在按钮下方，避免覆盖上方“语言与模式”设置。截图位于 `output/playwright/accent-picker-qa/`；控制台仅出现静态预览刻意不提供 `/api/*` 运行时数据产生的 404，没有取色器脚本错误。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 新增 HEX/HSV 双向转换与边界测试，并通过统一扩展测试和完整验证脚本。

## 2026-07-17 — 今日灵感换批响应优化

- “今日灵感”与“今日资讯”换批现在只重建被点击的栏目；批次、计数和卡片会在点击处理内立即更新，不再等待旧卡片完成 `120ms` 退场，也不会连带创建另外两个栏目的候选 DOM。
- 个人灵感预览继续预热完整三批图片，但预热已移出点击关键路径；新批次先使用现有图片或安全占位渲染，网络图片完成后再原位补齐。栏目头替换后会把焦点恢复到新的“换一换”按钮，支持键盘连续操作且不触发滚动跳动。
- 使用仓库现有的安全合成个人书签数据完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查：桌面与窄屏均能立即推进批次，换批后退场卡片数为 `0`、焦点仍在操作按钮、五张灵感卡完整保留，三个视口横向溢出均为 `0`，控制台错误与警告均为 `0`。本次未记录新截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 架构回归测试约束换批必须先渲染选中栏目、再后台预热图片，并保留即时替换与焦点恢复；`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过。

## 2026-07-17 — 资讯卡片操作图标悬停回弹

- SIGNAL FEED 资讯卡片的稍后读、已读归档与手动整理图标在鼠标悬停时增加 `220ms` 的轻微过冲回弹；最终放大约 `4%`，过冲峰值约 `8%`，不改变按钮尺寸、卡片布局、点击反馈或键盘焦点。
- 书签分类内条目的已读归档图标同步使用相同的悬停回弹；资讯手动整理的禁用态不触发，离开时平滑复位，并继续受全局 `prefers-reduced-motion` 规则约束。

## 2026-07-17 — 换批按钮弹性反馈

- 今日栏目“换一换”与 SIGNAL FEED“换一批”在点击并完成即时换批后，由整个按钮执行一次 `360ms` 轻量弹性缩放；图标不再单独旋转或缩放，悬停不触发动效。
- 修复“换一批”在更新批次文案时替换整个按钮内容、导致换批图标消失的问题；现在只更新 `.btn-label`，图标节点与无障碍结构持续保留。按钮尺寸、焦点恢复与换批逻辑不变，动画继续受全局 `prefers-reduced-motion` 规则约束。
- SIGNAL FEED 换批按钮的三语文案与今日栏目统一为“换一换 / 換一換 / Shuffle”，初始 HTML 与运行时本地化保持一致。
- SIGNAL FEED 顶部“缓存”按钮复用相同的点击后整按钮弹性回弹；图标不单独变形，禁用状态不会产生点击动画。
- 缓存按钮的整按钮回弹改用独立 CSS `scale` 属性，不再与加载图标的 `transform: rotate()` 共享变换通道，避免点击后旋转出现停顿；回弹时长与幅度保持不变。
- 缓存完成时不再从任意旋转角度直接替换图标；按钮等待当前 `spin` 动画触发下一次 `animationiteration`，在完整一圈的终点切回静止图标，使最后角度与起始角度一致。轮询期间复用现有图标节点，避免重复渲染重启动画。
- SIGNAL FEED 卡片的稍后读与已读归档复用“换一批”的整按钮 `360ms` 点击回弹；手动整理继续使用更克制的 `180ms`、峰值约 `2.5%` 回弹。图标不单独变形。
- 已读归档在正常动效模式下等待按钮完成短回弹后再提交状态，避免同步重建卡片使动画不可见；减少动态效果模式不增加等待。
- 已看归档卡片在取消归档、阅读队列卡片在移出队列时统一使用 `160ms` 纯透明度淡出，不再伴随上移；持久化状态先完成，淡出后再重绘对应列表。减少动态效果模式保持立即移除。
- “一键已读”与已看归档“清空”同步覆盖批量退场：当前列表中的全部卡片同时淡出，再统一重绘；批量状态写入及“一键已读”可选的新标签页打开仍在原用户手势流程内完成。

## 2026-07-17 — 封面调节预览取消全屏模糊

- 调整“显示高度”或“模糊强度”时，设置遮罩在现有封面预览状态中明确关闭 `backdrop-filter`，仪表盘封面与内容不再被全屏模糊层遮蔽；滑杆浮层、键盘操作、指针捕获和预览结束恢复行为保持不变。
- 每轮指针或键盘调节开始时平滑滚动到网页顶部，让封面立即进入视区；同一轮持续输入不会重复触发滚动或造成抖动。
- 全屏封面的显示高度下限由 100% 放宽到 70%，与其他封面布局使用相同的 70%–140% 范围；模糊强度上限由 24 px 提升到 50 px，界面、实时预览、首帧恢复、设置归一化和导入导出校验保持一致。
- 全屏高度低于 100% 时，头图本身保持至少一个视口高并从顶部锚定，仅由封面容器裁切底部；拖动高度不会再缩放或纵向移动头图构图。高于 100% 时仍可随容器扩展到 140%。
- 新增源码、设置边界和保存事务回归约束，确保两种调节共用的预览状态同时关闭标准与 WebKit 前缀滤镜、保留单次顶部滚动，并验证 50 px 模糊在实际写入与保存响应中不回落。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — AI 输出语言强一致性

- 所有用户可见 AI 输出统一声明并校验当前界面 locale；AI 问答、文章与网站说明、连续追问、每日简报、资讯卡片整理和阅读器翻译均在请求前后检查语言，连接测试保持原行为。
- 首次输出语言不合格时使用相同原始输入和更强语言约束自动重试一次；第二次仍不合格则丢弃 AI 文本并进入现有本地内容、原文或本地化错误兜底。被拒绝的结果不会进入缓存或追问历史，URL 类响应不再通过仅改写 `locale` 冒充新语言内容。
- AI 搜索缓存、卡片摘要策略和每日简报 Schema 升级至版本 6；缓存读取会再次验证生成正文。一次自动任务的初次调用与语言纠正共用一个任务额度，最终语言失败会保留该次额度以阻止后台无限重试；手动 Token 提示已同步说明最多一次纠正调用。
- 使用不含真实密钥、书签或缓存的内存模拟服务验证简体中文、繁体中文和英文的“错误语言 → 自动纠正”，并覆盖连续两次错误、请求期间切换语言、错误语言缓存和结构化字段混用语言。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 通过；本次无视觉改动，未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — AI 加载视觉语言统一

- AI 搜索、今日简报与资讯卡片手动整理继续使用 `160ms` 静态延迟、`700ms` 环境动效门槛和 `1600ms` 扫光周期；AI 回答与追问复用最终几何骨架，资讯卡片保留原内容并通过扫描、轻脉冲、按钮旋转和 `aria-busy` 表达处理中。
- 移除状态文字的原生 RAF 解码、随机字形池、逐字锁定、专用视觉节点与取消控制器。AI 搜索和今日简报的处理状态现在直接通过 `textContent` 完整显示；`role="status"`、`aria-live="polite"` 和减少动态效果规则继续保留。
- 使用安全合成数据复核深色 `1280×800`、浅色 `1440×1000` 和浅色窄屏 `390×844`：今日简报进入整理状态 `120ms` 时文本已完整为“整理中”，子元素数与解码节点数均为 `0`，没有额外 `aria-label`；`920ms` 时共享卡片扫光正常进入 `ambient` 且 `aria-busy="true"`。
- 三种视口横向溢出均为 `0`，控制台错误与警告均为 `0`；仅保留 Chromium 对页面现有多个独立表单的 verbose 建议。截图位于 `output/playwright/ai-loading-no-decode/`，不含真实 API Key、私人书签、Chrome Profile 或运行时缓存。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。
## 2026-07-17 — 新安装个人资讯书签默认关闭公共来源

- 新安装引导中选择个人资讯书签文件夹并保存时，会明确关闭“公共来源（Ampira）”；保留公共来源作为资讯来源时仍保持开启。
- 全局默认值、设置页来源切换、用户手动开关与现有用户数据均未调整，也不执行设置迁移或版本兼容。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — 普通 AI 问答支持连续追问

- 普通问题的首轮 AI 回答完成后会保留在当前搜索弹窗，并将输入框切换为连续追问；文章总结原有的网页追问能力保持不变。
- 普通问答上下文仅携带受限的首问、首答和最近 6 轮已完成问答；每次追问都会重新检索当前仍获准访问的 Ampira 内容，并在调用服务前复检来源权限。普通追问不写入持久 AI 搜索缓存。
- 简体中文、繁体中文和英文的追问状态、输入提示及系统提示词同步补齐；新增普通上下文边界和服务链路测试。`node tests/extension.mjs` 通过；由于当前 Windows PowerShell 7 进程会话不可用，未能执行 `.\scripts\verify-extension.ps1`。本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-17 — 发布候选浏览器验证

- 使用隔离配置从仓库根目录加载未打包扩展。Playwright Chromium 完成四步引导、无网站权限降级、深色 `1280×800`、浅色 `1440×1000`、浅色 `390×844` 和减少动态效果复核；页面横向溢出为 `0`，控制台错误与警告为 `0`。
- Microsoft Edge `150.0.0.0` 成功接管 `edge://newtab`，完成四步引导和无权限降级；“设置 → 浏览器”显示原生 favicon 不受支持并使用随包图标，“打开扩展管理页”实际打开 `edge://extensions/`。窄屏横向溢出为 `0`，控制台错误与警告为 `0`。
- Edge 实测发现首次引导仍显示“授权网站并启用图标”；现已按浏览器能力切换三语按钮文案，Edge 只显示“授权网站”，并保留不请求 `favicon` 权限的既有行为和自动化约束。
- 商店 Dashboard、精确来源权限和 AI 设置截图已从隔离配置重新捕获为 `1280×800`，不含真实 API Key、私人书签、个人浏览资料或运行时缓存。
- Google Chrome `150.0.0.0` 使用独立临时配置从仓库根目录成功加载未打包扩展；`chrome://extensions` 显示 Ampira 已启用、Service Worker 入口正常且无扩展警告。四步引导的无网站授权与稍后配置 AI 路径可用，真实 `chrome://newtab` 被 Ampira 接管；设置中的安装状态说明准确，“打开扩展管理页”实际打开带当前扩展 ID 的 Chrome 管理页。
- 真实 Chrome 在深色 `1280×800`、浅色 `1440×1000` 和浅色 `390×844` 下均无横向溢出，窄屏五入口导航完整可见；减少动态效果媒体查询生效，动画与过渡降至 `0.00001s`。上述状态下控制台错误与警告均为 `0`。
- Chrome 隔离配置内完成可选权限回归：`favicon` 授予后启用 Chrome 原生图标，撤销后即时回退随包图标；9 个精确资讯来源全部授权后资讯正常加载，单独撤销 `36kr.com` 后来源状态立即变为未授权；恢复出厂设置清理了其余可选来源权限。
- `search` 授予后顶部字段切换为“浏览器搜索”，非敏感测试查询由 Chrome 当前默认服务（Google）打开；撤销后即时回退为“搜索 Ampira”。恢复出厂设置确认清理 Ampira 本机/同步数据及可选权限、看板回到四步引导，未接触 Chrome 书签。
- 工具栏 `activeTab` 捕获、真实网络 Reader、非生产 AI 服务商、双实例 Sync，以及 Edge 垂直标签页与分屏仍需在商店私有测试前人工完成。
- 公开 README 已改为 Chrome 与 Edge 双浏览器口径，补充浏览器默认搜索、Edge favicon 回退、两家商店材料和不硬编码版本的 Latest Release、隐私及支持入口。

## 2026-07-17 — 26.2.9 发布与品牌收口

- `_locales/en`、`_locales/zh_CN` 与 `_locales/zh_TW` 的 Manifest 摘要和 Chrome Web Store、Microsoft Edge Add-ons 三语简短说明保持一致；当前用语以 2026-07-19 的商店文案章节为准。
- `assets/icons/ampira-logo.svg` 是唯一可编辑品牌源；移除光晕、渐变、旧纯紫色标记以及商店目录中的重复矢量副本。关于页、工具栏弹窗和 GitHub README 直接使用该矢量源。
- 扩展必需的 16/32/48/128px PNG 与 Chrome Web Store 使用的 128px 透明 PNG 均从唯一矢量源生成，不保留并行旧版本；128px 商店图标保留透明安全区。
- `store/privacy-practices.md` 提供与当前行为一致的 `favicon`、`search` 及精确来源权限说明；软件包、商店文案和测试统一以 26.2.9 为最终发布版本。

## 2026-07-19 — 三语 UI 文案与 AI 输出长度收敛

- 简体中文、繁体中文和英文同步清理“补盲”“兜底”“候选池”“入口”等内部术语；主动获取内容的按钮与空状态统一使用“刷新／重新整理／Refresh”，技术意义上的本地缓存、缓存清理与缓存记录保持不变。
- 来源、权限、错误、恢复出厂、来源诊断、浏览器搜索和 AI 免责声明改为直接、具体的用户表述；内置来源统一显示为“公共来源（Ampira）／Public sources (Ampira)”。公共来源和灵感预设均不再受规范名称断言约束。首次引导的“让收藏已久的书签，重新流动起来”保持不变。
- 三语支持页将“书签入口／书籤項目／Bookmark entries”统一为“书签／書籤／Bookmarks”；三语隐私页、商店隐私实践和审核说明同步采用“备用图片搜索／全网图片搜索”的界面名称，Brave 的启用条件、来源授权和数据传输行为没有变化。
- AI 输出提示词已恢复原策略：今日简报固定三行；资讯卡片固定“标题＋三行摘要”，信息充分时目标为 130–180 个字符且不少于 110；网站介绍固定三个栏目，中文 250–450 字、英文 250–450 词。
- `dashboard.html` 与工具栏浮窗的加载前文案已和简体 locale 对齐，三套客户端 locale 保持相同键集合。`.\\scripts\\verify-extension.ps1` 已通过；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-07-19 — P1 客户端语言包按需加载

- 首页不再静态导入三套完整客户端词典；启动时只通过本地字面量动态导入加载当前语言，切换语言前先完成目标词典准备，并复用并发中的同一加载任务。设置读取、保存、取消、导入和首次引导均保留原有语言状态与持久化语义。
- 10 个跨语言状态识别文案和工具栏浮窗使用的 11 个文案分别生成独立子集；工具栏浮窗改为直接使用微型三语词典与运行时消息端口，不再引入首页 API 和完整 i18n 依赖图。旧的生产完整词典适配器已删除，完整词典一致性比较仅保留在测试辅助模块。
- 首页两个入口的静态模块并集由 105 个模块、957,126 B 降至 102 个模块、740,080 B，减少约 22.7%；工具栏浮窗由 12 个模块、244,767 B 降至 6 个模块、14,846 B，减少约 93.9%。两条静态图均不包含 `en`、`zh-CN` 或 `zh-Hant` 完整词典，并由 760 KiB 与 40 KiB 回归预算约束。
- 使用隔离配置加载未打包扩展完成 Playwright Chromium 回归：英文冷启动只请求英文完整词典，之后繁中与简中各按需请求一次；并发繁中准备、设置页三语切换、繁中保存和浮窗跟随均正常。浮窗只请求 `locales/popup.mjs`，没有请求完整词典。
- 深色 `1280×800`、`1440×1000` 和繁中窄屏 `390×844` 均无页面或设置浮层横向溢出；冷启动没有长任务，页面与浮窗控制台错误均为 `0`。测试使用空白临时浏览器资料目录，结束后已关闭并删除；未记录截图，也未读取真实密钥、私人书签或运行时缓存。
- 客户端词典生成器 `--check`、统一扩展测试、`.\\scripts\\verify-extension.ps1` 和带 `-Package` 的最终验证均通过。`dist/ampira-26.2.9.zip` 包含 283 个文件、大小 2,260,971 B，SHA-256 为 `13559cdec9a1441cb8e98d4b6d293c4b747ee36482e2348758c0d0f414e140aa`；ZIP、sidecar 与发布清单一致，包内无旧完整词典适配器、测试、脚本或 `dashboard-cache/`。

## 2026-07-19 — 天气主导状态与次要信息收敛

- 天气请求不再显式组合 CMA、ECMWF、GFS 与 ICON，也不再执行自定义投票、降水阈值、云量回退和 CMA 失败重试；省略 `models` 参数，由 Open-Meteo 原生 `best_match` 自动选用当地适用模型。
- 今天直接使用官方 `current` 状态、当前温度和体感温度，并从当前小时读取降水概率；明天、后天直接使用官方 `daily` 天气码、最高/最低温度和每日降水概率峰值。
- 天气响应保持独立的实时 `current` 与两项未来 `daily`。缓存继续使用尚未发布的 v3 键，不增加数据版本；缓存按坐标和当地日期失效。
- 小字恢复旧版的信息组合：今天显示当前温度、体感温度和降水概率，明天、后天仅显示降水概率。由于当前 Windows PowerShell 登录会话不可用，未能执行 `.\\scripts\\verify-extension.ps1`；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-10 — SIGNAL FEED 换批原位交叉淡化

- “换一换”不再等待旧卡片退场后逐张上移进入；只在 SIGNAL FEED 内使用同文档 View Transition，旧内容 `180ms` 淡出、新内容 `240ms` 淡入并行完成，卡片不位移、不缩放、不交错。按钮回弹、批次文案和排序、刷新、单卡整理等其他更新路径保持不变。
- 连续点击会跳过当前视觉过渡，并以请求令牌丢弃过时回调；减少动态效果、后台页面、API 不可用或过渡启动异常时即时切换。完成、跳过与取消路径均会清理临时类和活动状态。
- 使用 37 条安全合成 Feed 数据完成深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 检查。两批 16 张卡片换批前后坐标与尺寸完全一致，滚动位置不变，焦点保留在“换一换”；末批 5 张、首批回绕、快速连点、减少动态效果和 API 回退均正常且无残留状态。
- 三种视口横向溢出均为 `0`，控制台错误与警告均为 `0`；截图位于 `output/playwright/signal-feed-batch-crossfade/`，不含真实 API Key、私人书签、Chrome Profile 或运行时缓存。本次使用本地 Chrome 静态预览，未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-12 — 缓存期间资讯悬停稳定

- 缓存进度仍会刷新今日区域的状态，但资讯列表不再把全部卡片移入 `DocumentFragment` 后整体替换。未变化卡片保持原位置和同一个 DOM 根；内容确有变化时只同步卡片内部内容，外层交互节点、焦点和悬停表面继续保留。
- 卡片复用比较会在克隆节点上忽略 `--mx`、`--my`、卡片入退场时长、View Transition 名称和 `will-change` 等临时视觉属性；空 `style` 属性也会被规范化。真实内容或非临时内联样式仍会触发同步，不放宽内容一致性判断。
- 隔离 Playwright 静态回归加载仓库实际 `dashboard.css`、`card-transition.mjs` 与 `daily-batch-transition.mjs`。带 `--mx: 120px`、`--my: 46px` 的悬停资讯卡连续执行 `20` 次缓存进度同路径 diff，卡片根和内容节点均保持不变，`MutationObserver` 记录为 `0`，伪元素 opacity 从执行前到 `30ms / 250ms` 始终为 `1`，指针坐标未丢失。
- 新增原位复用、内容原位同步及临时样式规范化回归测试；`node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 今日三列玻璃层闪烁修复

- 上一版把 `opacity` 与 `translateY` 直接施加到三块 `.board-column`。列本身带 `backdrop-filter`，替换加载占位后又从完全透明开始，会让已经显示的玻璃底先消失、短暂露出背景，再重新建立模糊合成层，因此视觉上会闪一下。
- 初次数据到达时不再删除三块加载列并创建新外壳，而是按 `data-column-id` 原位复用已有 `.board-column`，只换入正式标题和列表。玻璃底全程保持 `opacity: 1`、`transform: none` 和同一个 DOM 根；减少动态效果下的后续同步也不再重建外壳。
- 标题与列表继续使用 `420ms`、`opacity: 0 → 1`、`translateY(4px) → 0` 的淡入，但六个直属内容节点统一为 `0ms` 错峰，三列看上去同时上移并显现，不再分别跳出，也不会重复动画卡片内部。
- 安全合成 Playwright Chrome 覆盖深色 `1280×800`、浅色 `1440×1000`、深色 `390×844` 和减少动态效果：三块加载外壳标记均保留，整列动画调用为 `0`，内容淡入调用为 `6`，横向溢出与控制台错误/警告均为 `0`。中间帧截图位于 `output/playwright/dashboard-panel-entrance/daily-content-fade-stable-140ms.png`；未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-11 — 今日资讯与今日灵感换批顺序淡化

- “今日资讯”和“今日灵感”的“换一换”不再交叉叠化。旧内容先用 `90ms` 完全淡出，新内容保持透明并延迟 `90ms`，随后用 `150ms` 淡入；总时长仍为 `240ms`，中间不会同时显示两批文字或图片，也不位移、不缩放、不交错。新快照与入场图片默认强制为透明，封住 View Transition 初始化首帧的可见竞态。
- 每个共享槽位继续保留真实卡片外壳，只给 `.daily-batch-content` 建立过渡快照，边框、圆角、背景模糊、指针反馈和列表几何不参与切换。今日灵感在生命周期内仍保留退场图与入场图两个 DOM 图层，但两层的可见阶段互斥；异步图片更新只命中当前入场图，结束后清理旧图、阶段属性与临时过渡名称。
- 使用 30 条安全合成资讯、15 条本地预设灵感在 Playwright Chrome 检查深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844`。逐帧读取 View Transition 与图片图层透明度，三个视口中今日资讯文字、今日灵感文字、今日灵感图片的双层同时可见采样均为 `0`，且都完整经过“仅旧内容可见”和“仅新内容可见”阶段。
- 三个视口横向溢出均为 `0`；桌面换批后按钮焦点和滚动位置保持，结束后没有图片阶段、活动类或 View Transition 名称残留，控制台错误与警告均为 `0`。隐私安全截图位于 `output/playwright/daily-batch-crossfade/`，不含真实 API Key、私人书签、Chrome Profile 或运行时缓存。本次使用本地静态预览，未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 全部浏览器模块语法检查、`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-11 — 刷新首帧重点色稳定

- 外观控制器把规范化后的重点色主题与实际 HEX 色值作为单条非敏感首帧提示写入 `localStorage`；`chrome.storage.sync` 中的设置仍是正式数据源，不新增隐私数据或同步字段。
- 页面头部引导脚本在样式表加载前校验并恢复 `--accent`、`--accent-rgb` 与主题标识，刷新时不再先显示默认紫色；提示缺失、损坏或字段非法时继续安全使用样式表默认值。
- 使用隔离的本地 Chrome 静态预览，将 9 个 CSS 请求暂时阻塞后检查自定义色 `#2F81F7`：样式加载前根变量已是 `47, 129, 247`，加载完成后保持一致；损坏提示会回退到 `#9152FF`。控制台没有脚本异常，仅有静态预览预期的 3 个 `/api/*` 404；未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-11 — 刷新背景图提前加载

- 缓存中的网络头图和内置默认头图会在页面头部恢复阶段创建高优先级图片预加载；真实头图元素同步声明 `loading="eager"` 与 `fetchpriority="high"`。本地 data URL 不建立额外预加载，也不会为了本地图片提前访问网络后备地址。
- 首帧脚本直接监听头图 `load` 并立即写入既有 `is-loaded` 状态，不再依赖主应用模块完成加载后补同步。只提前请求与触发既有显示状态，CSS 的 `720ms` 淡入、状态滤镜时长与 `900ms` 缩放保持不变。
- 使用隔离的本地 Chrome 静态预览并把主应用模块替换为空模块：1920px 测试头图由 `link` 发起，优先级为 `high`，实际图片保持 eager/high，只有 1 次请求；主应用未绑定时头图仍能完成显示。计算动画时长为 `0.72s / 0.18s / 0.9s`，控制台错误与警告均为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 全部浏览器模块语法检查、`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-11 — SIGNAL FEED 换批模糊表面稳定

- 修复整张 `#summaryGrid` 快照叠加半透明卡片表面、导致换批中 `backdrop-filter` 视觉变亮或短暂消失的问题。换批现在保留原卡片外壳节点，只为每个槽位的 `.summary-card-content` 建立快照；卡片边框、半透明背景、指针光效和 `18px` 背景模糊始终由真实 DOM 持续渲染。
- 旧内容仍以 `180ms` 淡出，新内容仍以 `240ms` 淡入；同页 16 张卡片的外壳节点、坐标、尺寸、滚动位置和按钮焦点均保持不变。末批多余外壳退场、回到首批时新增外壳入场，快速连点、排序打断、减少动态效果和 API 不可用路径都会清理临时过渡名称与活动状态。
- 使用 37 条安全合成 Feed 数据复核深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844`。动画中 16 张卡片的 `backdrop-filter` 持续为 `blur(18px)`，半透明背景计算值与静止状态一致；三个视口横向溢出均为 `0`，控制台错误与警告均为 `0`。对比截图位于 `output/playwright/signal-feed-batch-crossfade/`；本次仍为本地 Chrome 静态预览，未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-11 — SIGNAL FEED 换批图片圆角与淡化稳定

- 图片不再包含在 `.summary-card-content` 的 View Transition 快照中，而是作为真实卡片外壳的直属图层保留；旧图 `180ms` 淡出、新图 `240ms` 淡入，两者只改变透明度，不位移、不缩放。卡片持续使用真实 DOM 的 `overflow: hidden` 与 `12px` 圆角，因此过渡顶层不会再短暂绕过图片裁切。
- 图片与 favicon 占位图各自保留独立的尺寸和透明度状态；异步完成的旧图片加载或回退不会再修改新批次卡片的占位布局。完成、快速连点、排序打断、减少动态效果和 API 回退路径都会移除旧图、阶段属性及临时过渡名称。
- 使用 37 条安全合成 Feed 数据和仓库本地 WebP 图片复核深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844`。中间帧中每个共享槽位同时存在一张淡出旧图与一张淡入新图，两者透明度均处于 `0–1`；图片始终位于真实卡片裁切层内，卡片节点、坐标、尺寸、滚动位置和按钮焦点不变。
- 末批 5 张、首批回绕、快速连续点击、排序打断、减少动态效果和 API 不可用路径均无图片或过渡残留；三个视口横向溢出均为 `0`，控制台错误与警告均为 `0`。中间帧和静止帧截图位于 `output/playwright/signal-feed-batch-crossfade/`，不含真实 API Key、私人书签、Chrome Profile 或运行时缓存；本次为本地 Chrome 静态预览，未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-11 — SIGNAL FEED 换批顺序淡化

- SIGNAL FEED 的文字内容与卡片图片不再交叉叠化。旧批次先用 `90ms` 完全淡出，新批次保持透明并延迟 `90ms`，随后用 `150ms` 淡入；总时长仍为 `240ms`。新内容快照与入场图片默认强制为透明，View Transition 初始化首帧也不会出现双层内容。
- 卡片外壳、`18px` 背景模糊、圆角裁切和操作按钮继续由真实 DOM 持续渲染；生命周期内允许退场图与入场图同时存在，但两者的可见阶段互斥。完成、打断和回退路径的节点清理逻辑保持不变。
- 使用 37 条安全合成资讯和仓库本地 WebP 图片，在 Playwright Chromium 检查深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844`。分别采到 `12`、`11`、`15` 个活动帧，三个视口中文字快照和图片图层的同时可见采样均为 `0`，并都完整经过“仅旧内容可见”和“仅新内容可见”阶段。
- 三个视口的卡片节点、坐标、尺寸、按钮焦点与滚动位置保持不变，横向溢出均为 `0`；结束后图片阶段、活动类和 View Transition 名称残留均为 `0`，控制台错误与警告均为 `0`。隐私安全的淡入中间帧截图位于 `output/playwright/signal-feed-batch-crossfade/`；本次未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-11 — 刷新头图与看板解耦

- 首页初始化不再等待 `headerCoverReady`：本地头图的 `chrome.storage.local` 读取和后续图片解码作为独立视觉任务继续运行，缓存的布局提示仍会在样式加载前预留正确几何；设置、看板数据和快捷入口布局保持原初始化链路。
- 头图加载动效由 `720ms` 淡入加 `900ms` 缩放收敛为 `180ms` 纯透明度过渡；图片构图从加载前到加载后始终不缩放、不位移，模糊设置仍使用既有 `180ms` 状态过渡，减少动态效果规则不变。
- 隔离 Playwright 合成测试把 `headerCoverReady` 人为延迟 `900ms`，看板约 `306ms` 完成，不再等待头图任务；另将头图请求延迟 `650ms`，看板约 `317ms` 完成、图片约 `717ms` 可用，图片中间帧透明度为 `0.833`、最终为 `0.9`，全过程 `transform: none`。
- 深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 的 `scrollWidth` 均与 `clientWidth` 一致，控制台错误与警告均为 `0`。头图聚焦测试、`node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过；未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-11 — 回滚工作台统一入场

- 撤回本轮为统一入场新增的 `.daily-shell` 待入场状态、整块内容动画、骨架根节点水合以及灵感图片 `1px` 外扩。效率区恢复直接替换加载占位并对三张卡片执行原有入场；今日区恢复替换三列占位并对六个列直属内容节点执行原有入场。既有换批动画、头图加载解耦及 `100vw` 横向稳定修复保持不变。
- 原有入场参数恢复为 `360ms`、`opacity: .2 → 1`、`translateY(8px) → 0` 和 `32ms` 递增延迟（最大 `96ms`）。工作台外壳本身始终为 `opacity: 1 / transform: none`，不再挂载 `data-workbench-entrance` 或 `is-workbench-entering`。
- 隔离 Playwright 使用安全合成数据记录到 `9` 个原有 WAAPI 入场调用：三张效率卡和今日三列的标题/列表；所有调用均为纵向位移，没有横向位移。深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844` 的 `scrollWidth` 均与 `clientWidth` 一致，控制台错误与警告为 `0`；减少动态效果下入场调用为 `0`。
- `node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过；未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 首屏卡片柔和淡入

- 原入场虽然标称 `360ms`，但使用高加速度的 `cubic-bezier(.16, 1, .3, 1)`，同时从 `opacity: .2` 和 `translateY(8px)` 开始。隔离 Chrome 逐帧采样中，约 `114ms` 时透明度已到 `0.78`、位移只剩约 `2.2px`，主要视觉变化挤在前几帧，刷新负载稍高时会像直接跳出。
- 效率卡与今日三列继续使用原有独立入场节点，不改变工作台、骨架、图片裁切和头图。入场调整为 `420ms`、`opacity: 0 → 1`、`translateY(4px) → 0` 与现有标准缓动；错峰由 `32ms / 96ms` 收紧为 `20ms / 60ms`，柔和度增加但最后一项的完成时间只由约 `456ms` 增至 `480ms`。
- 修复后逐帧采样在约 `123ms` 时透明度为 `0.496`、剩余位移约 `2.02px`，中段变化连续；共记录 `9` 个入场调用，全部只有纵向位移。深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844` 均无横向溢出，减少动态效果下入场调用为 `0`，控制台错误与警告为 `0`。
- 安全合成中间帧截图位于 `output/playwright/dashboard-panel-entrance/entrance-fade-160ms.png`；`node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过，未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 今日三列外壳同步淡入

- 左侧效率区把动画直接施加在三张 `.efficiency-card` 上，而今日资讯、今日灵感和已看归档此前只动画各列内部的标题与列表；三块 `.board-column` 玻璃外壳在替换加载占位时已是完整透明度，因此内容虽淡入，红框区域的整体背景仍像直接跳出。
- 首次加载现在直接对三块 `.board-column` 外壳调用现有入场动画。三列与左侧效率卡共同使用 `420ms`、`opacity: 0 → 1`、`translateY(4px) → 0`、`20ms` 错峰；不新增工作台级状态，不改变骨架、图片裁切、头图或后续换批动画。
- 隔离 Chrome 记录到三张效率卡和三块今日列外壳共 `6` 个入场调用，不再对列内标题、列表重复动画。约 `140ms` 中间帧中首张效率卡与首块今日列的计算透明度均为 `0.799748`，剩余位移均约 `0.801px`；两类节点起始帧均为完全透明并下移 `4px`。
- 深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844` 均无横向溢出，减少动态效果下入场调用为 `0`，控制台错误与警告为 `0`。安全合成截图位于 `output/playwright/dashboard-panel-entrance/daily-column-fade-140ms.png`；`node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过，未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 快速头图恢复原入场动画

- 高优先级预加载保持不变；缓存头图完成后不再在 CSS 首次绘制前直接写入最终态。共享显示入口会等待 `dashboard.css` 与 `extension.css` 就绪，并跨两个动画帧保留一次初始透明绘制，再添加既有 `is-loaded` 状态。
- 主应用与首帧脚本复用同一显示入口；快速缓存、正常网络加载和运行时换图都不会重复触发，也不会被旧来源回调误显示。此前把原有 `720ms` 淡入与 `900ms` 轻微缩放压成 `180ms` 纯淡入，实际改变了用户要求保留的动画速度；本次恢复原参数，减少动态效果规则不变。
- 隔离 Chrome 静态预览先将 1920px 本地 WebP 预热进缓存，并把两个主应用模块替换为空模块。页面 `pageshow` 约发生在 `47.9ms`，`is-loaded` 在页面可见后的 `81.9ms` 写入；入场后约 `90ms / 240ms / 480ms` 的透明度分别为 `0.422 / 0.790 / 0.893`，约 `720ms` 达到 `0.9`，轻微缩放在 `900ms` 完成。计算过渡时长为 `0.72s / 0.18s / 0.9s`，控制台错误与警告均为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-12 — 左右工作台整块入场统一

- 今日资讯、今日灵感和已看归档首次加载改为与左侧三张效率卡相同的整块入场：正式外壳替换轻量骨架后，三块 `.board-column` 依次执行 `420ms`、`opacity: 0 → 1`、`translateY(4px) → 0`，延迟为 `0 / 20 / 40ms`。列内标题、列表和图片不再另做一次首屏入场。
- 为避免再次出现“已显示的玻璃外壳先消失再淡入”，加载阶段不再提前挂载正式 `.board-column` 玻璃层，而使用与左侧一致的普通 `.card-skeleton`；正式列只在数据完成后创建。动画调用前先写入 `will-change: transform, opacity`，结束或取消后清理，避免带 `backdrop-filter` 的新层在首个可见帧临时重建。
- 安全合成 Chrome 在彩色背景下逐帧检查深色 `1280×800`、浅色 `1440×1000`、深色 `390×844` 与减少动态效果：正常场景均只有 6 个整块入场目标，左右延迟同为 `0 / 20 / 40ms`，首个采样帧透明度均为 `0`，右侧透明度逐帧单调增加；最终三列为 `opacity: 1`、`transform: none`、`will-change: auto`，模糊保持 `18px`。四个场景横向溢出、控制台错误和警告均为 `0`；减少动态效果下入场调用为 `0`。
- 彩色背景 `120ms` 中间帧截图位于 `output/playwright/dashboard-panel-entrance/unified-shell-entrance-120ms.png`；未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 日期栏首帧高度稳定

- 日期、星期和时间由主应用模块写入前，`.today-meta` 原先按自身 `12px × 1.5` 只预留 `18px`；日期实际使用 `14px × 1.5`，写入后整行变为 `21px`，因此刷新首帧会产生可测量的 `3px` 高度跳动。
- 空状态预留改为相对字体的 `min-height: 1.75em`，即当前字号下的 `21px`。不写死像素，浏览器缩放或最小字体设置变化时仍随容器字号调整；日期、星期和时间的字号、间距及内容逻辑均未改变。
- 隔离 Chrome 将主应用模块延迟 `800ms`，分别检查深色简中 `1280×800`、浅色英文 `1440×1000` 与深色繁中 `390×844`。三种场景的文字写入前后高度均为 `21px`，高度差和横向溢出均为 `0`，控制台错误与警告为 `0`；未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-12 — 首页头部与卡片入场参数统一

- 首页头部左侧标题/日期原为 `360ms`，右侧搜索框原为 `300ms`；两者都从 `opacity: .2`、下移 `8px` 开始，并使用较快的入场缓动。现在两组元素统一为与下方效率卡和今日三列相同的 `420ms`、`opacity: 0 → 1`、`translateY(4px) → 0` 与 `cubic-bezier(.2, 0, 0, 1)`。
- 只新增头部专属关键帧并替换这两个 CSS 动画声明；首帧脚本、数据请求、资源加载和卡片创建逻辑均未修改。原有 `40ms / 88ms` 头部延迟保持不变，侧栏与网站快捷方式继续使用各自原来的动画。
- 隔离 Chrome 在 `1280×800` 和 `390×844` 读取实际动画对象，两组头部元素与卡片的时长、透明度、位移和缓动完全一致；右侧搜索框开始入场后，首张卡片分别约晚 `29ms / 45ms` 开始，原有头部优先顺序保持。两个视口横向溢出、控制台错误和警告均为 `0`。
- 减少动态效果场景中头部为最终 `opacity: 1 / transform: none`，卡片 WAAPI 入场调用为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA；`node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-12 — 桌面侧栏柔和左侧淡入

- 桌面浮动侧栏原为 `300ms`、`opacity: .2 → 1`、左移 `6px → 0` 与快速入场缓动；刷新时主要变化集中在前段。现改为 `420ms`、`opacity: 0 → 1`、左移 `4px → 0` 与 `cubic-bezier(.2, 0, 0, 1)`，和首页头部及卡片使用相同的时长与变化幅度。
- 侧栏原有 `24ms` 首帧延迟、首帧脚本和加载顺序均未修改。`1120px` 以下的底部导航显式保留原来的 `300ms`、下移 `6px → 0`、`opacity: .2 → 1` 与入场缓动，避免桌面需求改变移动端方向。
- 隔离 Chrome 实测桌面 `1280×800` 的动画对象为 `0.42s / 0.024s / cubic-bezier(0.2, 0, 0, 1)`，关键帧为 `translate3d(-4px, -50%, 0) → translate3d(0, -50%, 0)`；移动端 `390×844` 的原动画参数保持不变。两个视口横向溢出、控制台错误和警告均为 `0`。
- 减少动态效果下侧栏不创建入场动画，直接保持最终透明度和基础居中定位。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA；`node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-12 — 统一首帧入场时长令牌

- 卡片整体入场原先调用 `firstFrame`，其 CSS/JavaScript 对应值为 `360ms`；此前为了局部调整新增了 `panelEntrance: 420`，头部与桌面侧栏则直接写入 `420ms`，形成三处重复时长来源。
- `firstFrame` 现统一更新为 `420ms`：CSS 使用 `--motion-first-frame-duration: 420ms`，JavaScript 使用 `MOTION_DURATION.firstFrame = 420`。临时的 `panelEntrance` 已删除，卡片重新调用 `firstFrame`，头部标题/日期、搜索框与桌面侧栏全部引用 CSS 令牌；各组件原有关键帧、缓动、延迟和加载顺序保持不变。
- 原本使用首帧令牌的导航 Logo 回弹会按语义同步采用 `420ms`。`1120px` 以下底部导航仍显式使用独立的 `300ms`，不会被桌面侧栏调整影响。
- 隔离 Chrome 在 `1280×800` 与 `390×844` 读取实际值：根令牌均为 `420ms`，桌面侧栏、头部两组元素和工作台卡片均解析为 `0.42s`；移动端底栏仍为 `0.3s`。两个视口横向溢出、控制台错误和警告均为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA；`node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-12 — 内容换批淡化延长

- 今日资讯、今日灵感与 SIGNAL FEED 的内容换批由 `90ms` 退场加 `150ms` 入场延长为 `160ms` 退场加 `260ms` 入场，总时长从 `240ms` 调整为 `420ms`。原有退场/入场时长比例基本保持，视觉变化不再集中在极短的前几帧。
- 仍然先将旧文字和图片完全淡出，再延迟到退场结束后淡入新内容；退场继续使用 `cubic-bezier(.4, 0, 1, 1)`，入场继续使用 `cubic-bezier(.16, 1, .3, 1)`，不增加位移、缩放或交叉重叠。卡片外壳、圆角裁切与背景模糊保持不变。
- “换一换”按钮点击反馈未修改，仍为 `shuffleControlClickSpring 360ms ease`。隔离 Chrome 读取到两类换批变量均为 `160ms / 260ms / 160ms 入场延迟`，按钮计算时长为 `0.36s`，横向溢出为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA；`node tests/extension.mjs` 与 `.\\scripts\\verify-extension.ps1` 均通过。

## 2026-08-12 — 全项目动画令牌精简

- 完整盘点 `assets/` 下所有 CSS 动画自定义属性和客户端 `MOTION_DURATION / MOTION_EASING` 消费者。8 个全局 CSS 时长令牌与 6 个全局 CSS 缓动令牌均有实际调用；`emphasis` 与 `move` 虽同为 `240ms`，但分别服务强调入场与几何移动，保留独立语义。Reader、浮层、环境循环和品牌缓动也都有 CSS 消费者，未按数值强行合并。
- 删除 JavaScript 中没有任何调用的 `overlay / reader / ambient` 时长镜像和 `ambient / brand` 缓动镜像；补齐实际需要的 `emphasis`。JavaScript 动画令牌现在只暴露 5 个时长和 4 个缓动，并增加 CSS/JavaScript 同名令牌数值一致性断言。
- 今日资讯、今日灵感与 SIGNAL FEED 原有 6 个重复局部换批变量全部删除，改用全局 `state 180ms` 退场和 `emphasis 240ms` 入场，入场延迟直接复用 `state`，总时长仍为 `420ms`。卡片默认入场、AI 搜索/Reader/设置关闭的重复 `240ms / 180ms` 常量也改为共享语义令牌；“换一换”按钮专用 `360ms` 弹性反馈、移动端导航和头图专用时序保持不变。
- 增加全局 CSS 动画令牌消费者审计：任何定义后没有实际 `var(...)` 调用的令牌都会使架构测试失败。隔离 Chrome 实测两类换批均为 `180ms` 退场加 `180ms` 延迟、`240ms` 入场，退场/入场缓动不变；AI 搜索、Reader 与设置关闭均为 `0.18s`。减少动态效果下不挂载首帧类、卡片 WAAPI 调用为 `0`，控制台错误、警告和横向溢出均为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 共用确认弹窗补齐动画

- 逐一核对设置、AI 搜索、Reader、首次引导与原生确认弹窗；前四类已有入场动画，只有被清空归档、重置设置、导入设置和手动 AI 用量提示等操作共用的 `confirmationDialog` 完全没有动画。颜色选择器是非模态 popover，不纳入本次范围。
- 确认弹窗复用现有设置模态的动效与令牌，不新增局部参数：面板和原生 `::backdrop` 使用 `300ms` 浮层入场，面板从透明、下移 `14px` 和 `scale(.992)` 收敛到最终态；关闭使用 `180ms` 状态退场，面板淡出并下移 `8px`，遮罩同步淡出。
- 关闭按钮、点击遮罩和 Escape 统一先添加 `closing` 状态，原生 dialog 在退场期间继续留在 top layer，动画完成后才调用 `close()`、清理状态、恢复触发控件焦点并结算确认结果。重复关闭会被拦截；减少动态效果时 CSS 时长压缩为 `.01ms`，JavaScript 不再等待 `180ms`。
- Playwright 使用安全合成数据实测深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844`：弹窗均完整位于视口内，横向溢出为 `0`；入场解析为 `settingsModalOpen / 0.3s`，Escape 后退场解析为 `settingsModalClose / 0.18s`，结束后焦点回到触发按钮。减少动态效果下 Escape 后立即关闭，控制台错误与警告均为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 今日资讯换批操作图标静止

- 今日资讯换批不再把右侧收藏与已读操作区包含进 View Transition 快照。每个共享槽位只为网站图标和标题/来源内容建立独立过渡名称，操作按钮由真实 DOM 在新批次写入时直接切换状态；今日灵感和 SIGNAL FEED 的既有换批边界保持不变。
- Playwright 使用 8 条安全合成资讯与正式 CSS/换批模块复核深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844`。三个场景都采到旧批次与新批次状态，内容仍出现 `dailyBatchOut / dailyBatchIn`；操作区的 `view-transition-name` 始终为 `none`、子树动画数始终为 `0`、隐藏图标帧为 `0`，图标最低透明度保持既有静止态 `.72`。
- 三个视口横向溢出均为 `0`，完成后临时 View Transition 名称残留均为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-12 — 今日灵感刷新重绘稳定

- 问题发生在刷新进度轮询触发的重复渲染，不是“换一换”过渡。下一次渲染中的相同图片会暂时带上 `is-preview-image-pending` 或 `is-preview-image-revealing`；旧比较把这些瞬时加载类当成内容变化，继而替换已经显示的图片节点，让新节点以透明待加载态出现一帧，形成稳定闪烁。
- 卡片内容比较现在只在克隆节点上忽略图标及预览图的瞬时加载类，并清理移除最后一个瞬时类后留下的空 `class` 属性。真实标题、链接、图片地址、样式或其他类仍会触发正常同步；“换一换”的 View Transition、批次顺序与时长均未修改。
- 聚焦回归覆盖待加载与正在显现两种图片状态。Playwright 使用 5 张安全本地灵感卡片模拟 8 次刷新进度重绘，在深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 下，卡片替换、图片替换、透明帧、待加载残留和已移除图片节点均为 `0`；最低图片透明度保持 `.82`，三个视口横向溢出均为 `0`，控制台错误与警告为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-12 — 卡片操作状态切换不再重建图片与图标

- “稍后读”原先通过 `replaceChildren()` 销毁并重建按钮里的图标，本机写入稍后读后返回的相同同步记录又会调用 `renderAll()`；“归档/已看”也直接触发整页重绘。两条路径叠加后，即使数据没有变化，今日灵感与 SIGNAL FEED 仍会重新比较带临时加载类的图片并短暂替换已显示节点。
- 稍后读现在在原按钮内直接更新图标类、遮罩、标签和按压状态；归档按钮同样只更新现有控件。完全相同的稍后读同步回声直接返回，真实外部变化只刷新效率区；归档只重绘效率区、今日区与 SIGNAL FEED，不再刷新无关页面区域。
- 今日灵感与 SIGNAL FEED 的缩略图同步比较统一忽略 `is-preview-image-pending` 和 `is-preview-image-revealing`。图片地址与内容相同时保留已经绘制的图片，丢弃尚未挂载的重复待加载节点；真实图片变化和“换一换”过渡仍按原逻辑执行。
- Playwright 对今日灵感与 SIGNAL FEED 分别执行“稍后读”和“归档”，每次连续采样 5 帧。深色 `1280×800`、浅色 `1440×1000` 与深色 `390×844` 中，2 个图片节点和 4 个操作图标节点始终保持同一实例，移除的图片与图标均为 `0`；图片最低透明度为 `.82`，图标最低透明度为 `.72`，待加载图片残留与横向溢出均为 `0`，控制台错误与警告为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-12 — 阅读队列标题图标原位保留

- 列表卡片里的“稍后读”按钮已改为原位切换，但阅读队列此前仍按整张卡片的 DOM 内容决定是否复用。队列数量或列表一变化，整张 `.queue-card` 连同 `.efficiency-title` 的本地图标都会被替换，所以标题图标仍会闪一下。
- 阅读队列现在保留现有卡片外壳和标题节点，只替换右侧“一键已读/未处理数”，并在空状态与列表之间切换或按 `data-key` 增删队列行。未变化的队列行及 favicon 也会继续复用。
- 修复前使用真实 `createEfficiencyView` 复现时，添加和取消后的卡片与标题图标节点都会改变，初始标题图标被移除 1 次。修复后 Playwright 在深色 `1280×800`、浅色 `1440×1000` 和深色 `390×844` 下，添加和取消均保持同一卡片与同一标题图标，节点移除数、隐藏帧、待加载残留和横向溢出均为 `0`；图标最低透明度保持 `.72`，控制台错误与警告均为 `0`。未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-12 — 界面等宽字体统一替换为无衬线

- 移除全局 `--font-mono` 等宽字体令牌，以及弹窗、设置、操作浮层和扩展专属界面中直接声明的 `monospace`、Cascadia Mono、SFMono、Consolas 与 Liberation Mono 字体栈。
- 原先使用等宽字体的英文 kicker、技术标签和状态文字统一改用现有 `--font-ui` 无衬线字体栈；字号、字重、行高和字距保持不变，避免扩大本次视觉调整范围。
- 架构测试新增全量 CSS 约束，后续重新引入等宽字体声明会直接失败。`rg` 复查 `assets/` 与 `dashboard.html` 后不存在等宽字体声明。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-12 — 搜索改为 LLM 式对话工作台

- 搜索弹层改为近全高的单一玻璃工作台，保留 Ampira 的网格背景、紧凑无衬线排版、细边框和单一紫色强调。空会话的多行输入区位于中央，首条消息后使用现有 `240ms move` 动效移到底部；用户与 Ampira 使用全宽扁平行和顺序编号，不使用气泡、头像、渐变或历史侧栏。`520px` 以下工作台全屏，桌面最大宽度为 `920px`、最大高度为 `880px`。
- 首轮问题和回答现在与追问共用同一消息流。回答内保留结构化分节、最多 6 个参考来源和复制操作；AI 生成、缓存、本地结果、错误与权限提示属于各自消息，不再使用弹层外的全局声明。输入区支持自动增高至 `144px`、Enter 发送、Shift+Enter 换行和中文输入法保护。
- 会话只存在于当前页面内存。关闭弹层不会清空记录或取消请求，重开后恢复；“新对话”、顶部携带新问题进入和卡片网址解读会显式失效旧响应。AI 模式继续复用现有文章/问题上下文及六轮上限，本地模式的每次输入仍是独立检索。焦点在弹层内循环，关闭后归还触发元素，只有真实背景点击才会关闭。
- 隔离 Playwright 使用正式 CSS、正式搜索控制器和安全合成数据复核深浅主题下的 `1280×800`、`1440×1000` 与 `390×844`。六个组合的页面和面板横向溢出均为 `0`，消息区底部始终位于输入区上方；六轮对话、网址结构化回答、参考来源、本地回退、错误、权限拒绝、关闭恢复、旧请求失效和键盘焦点循环均通过，控制台错误与警告为 `0`。减少动态效果下未创建输入区 WAAPI 位移动画，关闭立即完成。
- 安全合成截图位于 `output/playwright/ai-search-empty-dark-1280x800.png`、`ai-search-conversation-dark-1280x800.png`、`ai-search-conversation-light-1440x1000.png` 和 `ai-search-conversation-light-390x844.png`。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过；未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-13 — 搜索折叠输入态与左右玻璃对话

- 空会话不再展示完整工作台、标题栏、模式说明、空态标题、新对话、关闭按钮或快捷键提示，只保留同一套多行输入框和发送按钮。桌面稳定为 `740×65.33px`，`390×844` 下为 `366×65.33px`；点击背景或 Escape 仍可关闭，已有会话重新打开时直接恢复展开状态。
- 首次提问、网址解读和卡片外部查询会把同一面板通过 `240ms move` FLIP 展开；“新对话”用相同方式收回，不复制输入区 DOM。展开态继续采用桌面 `920px / 880px` 上限和 `520px` 以下全屏规则，表面改为复用设置面板的背景、`28px` 模糊、阴影与无模糊回退，内部工作区保持透明。
- 用户消息改为右侧轻紫气泡，桌面最大宽度 `68%`；Ampira 回答改为左侧中性玻璃气泡，最大宽度 `86%`。移动端分别放宽到 `86% / 94%`。可见轮次与重复身份标签已移除，内部轮次仍保留并通过本地化 `aria-label` 提供；结构化摘要、来源、AI/本地状态、缓存、核对声明和复制操作归入同一个回答气泡。
- 隔离 Playwright 使用正式 CSS、正式搜索控制器和安全合成数据复核深浅主题下的 `1280×800`、`1440×1000` 与 `390×844`。六个组合横向溢出均为 `0`，展开面板分别为 `920×752px`、`920×880px` 和 `390×844px`，消息区底部始终位于输入区上方；移动端根滚动被锁定，不再露出右侧滚动条细缝。
- 六轮对话保持 `12` 条左右消息并自动停在末尾；本地结果不显示 AI 声明，错误与权限拒绝保留已有消息，请求中关闭后重开仍显示加载并继续完成，新对话会使旧响应失效。紧凑态 Tab 留在唯一输入控件，展开态输入框与标题栏操作可循环聚焦；减少动态效果下 FLIP 不创建、关闭立即完成，控制台错误与警告均为 `0`。
- 安全合成截图位于 `output/playwright/ai-search-compact-dark-1280x800.png`、`ai-search-bubbles-dark-1280x800.png`、`ai-search-bubbles-light-1440x1000.png`、`ai-search-compact-light-390x844.png` 和 `ai-search-bubbles-light-390x844.png`。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过；未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-13 — 双范围研究搜索

- 搜索输入区新增两个相互独立且默认关闭的范围控件：单选书签文件夹与 Brave 联网搜索。默认模式继续走原有 Ampira 搜索和普通 AI 问答；文件夹模式只研究本轮选择的目录，联网模式不读取私人书签目录，双开时两条链路并行。粘贴网址和卡片解读仍优先走原有文章解读。打开文件夹组合框时才读取书签树；“新对话”恢复未选目录与联网关闭，关闭重开则保留当前页面会话。
- 研究结果使用本轮稳定的 `S1…S12` 证据编号，回答中的引用只关联本轮证据。回答气泡内依次包含直接结论、可折叠参考来源、逐批授权/继续检索操作和单行覆盖信息；每轮独立记录实际文件夹、联网状态、网站覆盖、正文读取数、证据数与真实发布日期跨度。标题栏四种状态实测为“默认”“AI 视频 · 收藏夹”“联网搜索”“AI 视频 + 联网”。
- 书签研究最多并行刷新 4 个相关且已授权的网站、读取排名最高的 3 篇正文，未授权网站仍进入覆盖统计并按最多 3 个精确来源分批申请。研究缓存复用 IndexedDB `records`，仅保存标题、网址、摘录、来源身份、发布日期与抓取时间，按 30 天、2,000 条或 6 MiB 限制清理，不保存另行提取的全文。Brave Web Search 结果只留在当前页面会话；开关关闭时不会发起 Web Search 请求。未新增 Manifest 权限或数据库版本迁移。
- 自动测试覆盖四种范围隔离、嵌套/同名/空/大目录、时间意图与扩展、URL 去重、缓存裁剪、Brave 缺密钥/权限/订阅及危险网址、合并来源类型、引用校验和游标。运行中删除目录、撤销精确网站权限或更换 Brave 配置时会在合并与返回前再次过滤证据，并降级为确定性结果；有效的另一条检索链路仍可保留。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。
- 隔离 Playwright 使用正式 CSS、正式搜索控制器和安全合成 API 数据复核深色 `1280×800`、浅色 `1440×1000` 与浅色/深色 `390×844`。三个视口横向溢出均为 `0`；展开面板分别为 `920×752px`、`920×880px` 与 `390×844px`，紧凑移动态为 `366×143.33px`。面板实际计算模糊为 `blur(28px)`；来源展开后最后一条消息可完整滚动到输入区上方，控制台错误为 `0`。
- 安全合成截图位于 `output/playwright/research-search/research-both-dark-1280x800.png`、`research-both-light-1440x1000.png`、`research-sources-light-390x844.png` 和 `research-compact-default-dark-390x844.png`。未使用真实书签、真实 API 密钥或生产网络结果，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 用户反馈回归后，收藏夹组合框改为在真实打开动作后立即显示“正在读取文件夹”，并明确区分未同意读取、没有网页文件夹和读取失败；安全合成的延迟接口随后显示“研究 / AI 视频 · 18 条 / 6 站”与另一独立目录，整个流程使用自定义组合框的实际点击和选项提交，没有直接修改隐藏的原生 `select`。
- 生成期间范围控件现在保持可编辑，但当前请求继续使用提交时快照。浏览器回归捕获的第一轮请求为 `folder-ai + webSearch:false`；第一轮生成中打开联网后，第二轮请求为 `folder-ai + webSearch:true`，第一轮页脚仍只标记收藏夹，第二轮才标记收藏夹与联网。运行时测试另以同名兄弟目录和嵌套目录验证所选文件夹的元数据、来源刷新、正文读取与证据均不会跨目录；回归截图为 `output/playwright/research-search/research-scope-switch-regression.png`，控制台错误与警告均为 `0`，仍未使用真实书签或密钥。

## 2026-08-13 — 搜索对话高级极简重构

- 空会话继续只渲染同一个玻璃 composer，桌面为 `720×109.33px`，`390×844` 下为 `366×109.33px`；标题栏、全局模式和可见请求状态均不出现。收藏夹与联网范围改为内容宽度的紧凑标签，默认收藏夹触发宽度实测 `69.33px`；同名目录的触发文案为“父级 / 名称”，下拉仍显示完整路径、书签数和站点数。
- 展开态为 `960px` 阅读工作台，`1280×800` 下为 `960×768px`、`1440×1000` 下为 `960×968px`，移动端为 `390×844px` 全屏。消息与 composer 共用 `760px` 阅读列；用户问题保留右侧低饱和重点色气泡，Ampira 回答的计算背景为透明、边框为 `none`，正文实测宽度约 `558px`，不再嵌套玻璃回答卡。
- 回答操作栏只保留 AI/本地状态、可选缓存、复制和“来源 n”。来源在操作栏下方原位展开，开关本身不会位移；本轮范围、`4/6` 网站覆盖、`8` 条联网结果、`2` 篇正文和 `2026-08-10—2026-08-12` 真实日期跨度随后显示，三条来源使用透明分隔行并保留安全的行内 `S1…S3` 引用。
- 范围状态回归把第二轮以 `folder-ai + webSearch:true` 提交，在请求期间关闭联网；输入区立即显示“下一轮”，当前加载文案仍为“正在检索收藏夹与网络…”。完成后输入区保持联网关闭，但该轮来源详情仍显示“AI 视频 + 联网”和 `8` 条联网结果，证明 draft、active request 与 message 三层范围没有互相覆盖。
- “联网搜索”图标改用共用的 `currentColor` 蒙版图标；开启态取消图标透明度，与按钮文字一起准确继承当前主题重点色，自定义重点色同样生效。自动回归同时约束静态结构、蒙版来源和开启态不透明度。
- 安全本地 Playwright 预览以绿色自定义重点色复核浅色 `1280×800`、深色 `1440×1000` 与深色 `390×844`：三种场景的文字与图标填充均为 `rgb(0, 168, 112)`，图标透明度为 `1`，横向溢出为 `0`；深色桌面键盘焦点仍停在开关，窄屏减少动态效果查询为启用。隐私安全截图位于 `output/playwright/web-search-active-accent-light-1280x800.png`、`web-search-active-accent-dark-1440x1000.png` 和 `web-search-active-accent-dark-390x844.png`。静态预览仅保留预期的三个 `/api/*` 404，没有新增控制台警告或前端错误；未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 隔离 Playwright 使用正式 CSS、正式搜索控制器和安全合成 API 数据复核深色 `1280×800`、浅色 `1440×1000` 与深浅 `390×844`。所有场景横向溢出为 `0`；六轮对话为 `6` 条用户消息和 `6` 条回答，最后消息底部 `642.77px`、记录区底部 `651px`、composer 顶部 `663px`，没有遮挡。减少动态效果下相关动画解析为 `0.01ms` 且已完成；强制颜色模式下工作台、用户气泡和范围标签均回退为系统 Canvas/CanvasText。控制台错误和警告均为 `0`。
- 隐私安全截图位于 `output/playwright/research-search/minimal-compact-final-dark-1280x800.png`、`minimal-next-turn-final-dark-1280x800.png`、`minimal-sources-final-light-1440x1000.png`、`minimal-sources-light-390x844.png` 和 `minimal-compact-dark-390x844.png`。本轮没有读取真实书签、真实 API 密钥或生产网络结果，也没有执行加载已解压扩展后的手动 Chrome/Edge QA；`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-13 — 浅色弹窗背景叠层统一

- 设置、AI 搜索、Reader、共用确认弹窗和首次引导的全屏叠层在浅色与跟随系统浅色模式下统一使用 `#EEF0F2 / 72%` 的主题遮罩；共用确认弹窗的原生 `::backdrop` 和公共弹窗兜底不再硬编码黑色。深色模式继续使用深色主题遮罩，颜色选择器和组合框等非模态 popover 仍保持透明 backdrop。
- 架构回归覆盖显式浅色、跟随系统浅色以及设置、确认弹窗、首次引导三个共享消费者，后续重新引入黑色确认遮罩或降低其中一处浅色遮罩强度会使测试失败。
- 本地静态预览通过真实界面流程切换浅色模式并从“关于 → 配置迁移 → 恢复出厂设置”打开共用确认弹窗。`1440×1000` 下设置叠层与原生 dialog backdrop 的计算背景均为 `color(srgb 0.933333 0.941176 0.94902 / 0.72)`，模糊为 `blur(14px) saturate(1.06)`，横向溢出为 `0`；隐私安全截图位于 `output/playwright/light-modal-overlay-1440x1000.png`。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。静态 HTTP 预览未连接扩展运行时，因此控制台保留 5 条预期的接口错误；本轮未读取真实书签或密钥，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-13 — 搜索打开时首页横移修复

- 根滚动容器现在始终预留页面滚动条槽位；搜索弹层打开并锁定根滚动时，可用布局宽度不再变化，居中的首页卡片不会向右跳动。
- 自动回归断言同时约束搜索层滚动锁和根滚动条槽位，避免后续只保留其中一侧而重新引入横移。

## 2026-08-13 — AI 补齐为通用文字 LLM 工作台

- 默认 AI 对话不再局限于研究摘要：普通解释、写作、改写、翻译和代码生成共用通用助手提示，用户明确指定的输出语言优先于界面语言。问题上限统一为 `8,000` 个 Unicode 字符；上下文保留最近 `12` 个已完成轮次并限制在约 `12,000` 个字符，回答预算提升为 `4,096 Token`。自动摘要、每日简报和 Reader 翻译仍使用原来的严格界面语言规则。
- 回答保留模型原始 Markdown，并通过纯结构化 DOM 渲染标题、段落、列表、引用、表格、行内代码、代码块和链接；没有使用 `innerHTML`。原始 HTML 作为普通文字显示，危险协议不会形成链接，研究回答的 `[S1]` 引用只关联本轮证据。代码块、表格和长网址限制在回答列内部滚动，并提供独立的代码复制按钮。
- 扩展内部新增 `ampira:ai-stream` 端口，按 `requestId` 隔离 `status / delta / complete / cancelled / error`。OpenAI Responses 与 Chat Completions 都支持跨数据块 SSE，兼容服务返回普通 JSON 时只结算一次；只有在产生文字前明确返回不支持流式的 `400 / 404 / 415 / 422` 才回退一次非流式请求。正式 OpenAI Responses 请求显式使用 `store: false`，网络超时调整为 `120s`。
- 请求中发送按钮切换为停止按钮；取消信号贯穿模型、Brave、收藏夹刷新、Feed 和 Reader 正文读取。停止或中断后保留已产生文字，但不写入后续上下文或回答缓存。关闭弹层继续运行当前请求，新对话、页面断开或主动停止会真正取消。研究模式仍先完成证据合并和引用校验再一次性显示，避免把未经验证的流式草稿作为研究结论。
- 用户消息支持编辑；编辑旧问题会明确确认并移除后续轮次后重新生成。最新完整回答支持重新生成，错误或停止回答支持重试，长度截断结果支持按上一段回答语言继续生成。重新生成复用该轮范围快照并绕过回答缓存，输入区当前收藏夹与联网开关仍只影响下一轮。研究追问也改为原生 `user / assistant` 消息顺序，历史仅用于理解指代，本轮证据继续独立隔离。
- Provider 回归覆盖 Responses SSE、Chat SSE、跨块事件、普通 JSON、显式不支持流式回退、`429`、主动取消、长度截断、生成中断、原生角色顺序和 `store: false`；UI 回归覆盖停止、编辑、重试、重新生成、继续生成、旧响应失效、安全 Markdown 与三语言键值。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。
- 隔离 Playwright 使用正式 CSS、正式搜索控制器和安全合成 API 数据实测深色 `1280×800`、浅色 `1440×1000` 与浅色 `390×844`。空态面板稳定为 `720px` 且标题栏隐藏，展开桌面为 `960px`，移动端工作台完整覆盖 `390px` 视口；所有场景均无正向横向溢出，代码与表格没有突破面板，最后消息底部分别为 `626.92px / 626.92px / 621.42px`，均位于 composer 顶部 `662px / 862px / 729px` 之上。停止按钮在加载中可见，用户编辑、回答重新生成和代码复制操作均存在，危险链接数量为 `0`，控制台错误与警告为 `0`。
- 减少动态效果下工作台和 composer 过渡均压缩为 `0.01ms`；强制颜色模式已实际启用并保留系统边框的发送控件。隐私安全截图位于 `output/playwright/ai-workbench/general-compact-dark-1280x800.png`、`general-loading-dark-1280x800.png`、`general-markdown-dark-1280x800.png`、`general-markdown-light-1440x1000.png` 和 `general-markdown-light-390x844.png`。
- 本轮没有读取真实书签、真实 API 密钥或生产网络结果，也没有调用真实 OpenAI/兼容服务，因此流式服务商差异目前由合成 SSE 与 JSON 测试覆盖；未执行加载已解压扩展后的手动 Chrome/Edge QA。会话仍只保存在当前页面内存，没有新增附件、持久聊天历史、模型切换器或 Manifest 权限。

## 2026-08-13 — 收藏夹路径完整展示

- 收藏夹组合框不再把“完整路径 + 数量”压缩在单行中。列表选项改为两层信息：首行显示完整目录路径并允许自然换行，次行单独显示书签数和站点数；桌面优先使用 `360px` 列表宽度，窄屏自动限制在视口边缘内。触发标签继续保持短名称，不增加 composer 宽度。
- 当返回的全部目录共享同一个浏览器顶层时，展示层自动移除统一前缀；例如“书签栏 / 财务 / 购买”显示为“财务 / 购买”，顶层“书签栏”显示为“全部收藏”。文件夹 ID、内部完整路径和研究范围均未改变；如果未来同时出现“书签栏”和“其他书签”等多个顶层，顶层名称会保留用于消除歧义。
- 隔离 Playwright 使用 6 个安全合成长目录复核浅色 `390×844` 与 `1280×800`。两个视口的列表宽度均为 `360px`，分别位于 `21—381px` 和 `287—647px`，没有正向横向溢出；所有完整路径的 `scrollWidth` 均未超过 `clientWidth`，列表中“书签栏”出现次数为 `0`，数量信息独立显示，控制台错误与警告均为 `0`。
- 隐私安全截图位于 `output/playwright/research-search/folder-menu-complete-light-390x844.png` 和 `folder-menu-complete-light-1280x800.png`。没有读取真实书签或改动真实文件夹；`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。

## 2026-08-13 — AI 请求断线恢复与真实错误呈现

- 修复流式端口在首个文本增量前断开时直接结束整轮对话的问题。页面现在只在尚未收到任何生成文字时，通过既有 `/api/ai/search` 消息链路安全回退一次；已经收到文字或服务商明确返回错误时不会重放请求，避免重复回答和潜在重复计费。流式客户端同时把端口创建及首条消息发送失败统一归类为连接中断。
- 待处理回答不再把“正在分析…”保存为消息正文，加载状态只存在于可访问的 loading surface。停止、断线或请求失败时，只有真实生成增量才会作为部分回答保留；否则展示运行时返回的具体错误，因此不会再出现红色“正在分析…”与“请求失败”并存的假状态。
- 自动回归覆盖断线前零输出回退一次、断线前已有输出不重放、服务商错误不回退以及加载占位符不进入回答数据。`node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。
- 本地安全预览使用正式页面和控制器分别模拟 `503` 与成功响应：失败回答正文显示“模拟服务错误：请检查 AI 设置”，正常回答进入 AI 生成、复制和重新生成状态，控制台错误与警告为 `0`。Chrome 中已识别到打开的 Ampira 标签页，但自动化安全策略禁止读取 `chrome-extension://` 页面，因此未声称真实扩展交互 QA；加载当前代码后仍需在扩展管理页重新加载扩展并刷新 Ampira 标签页。

## 2026-08-13 — AI 收藏夹列表收窄

- AI 搜索的收藏夹下拉列表改用专用的固定 `240px` CSS 宽度，不再把较大数值作为最小宽度；这比此前 `300px` 再缩小 `20%`，只影响该列表。完整路径仍可自然换行，触发标签与其他共享组合框尺寸不变，窄于列表的视口仍由既有边界约束保护。
- 隔离 Playwright 使用安全合成收藏夹数据复核深色 `1280×800`：列表计算宽度严格为 `240px`，位于约 `287—527px`，完整路径标签溢出和页面正向横向溢出均为 `0`，控制台错误与警告为 `0`。截图位于 `output/playwright/research-search/folder-menu-fixed-240-dark-1280x800.png`。
- `node tests/suites/research-search.mjs`、相关模块语法与差异检查通过；`./scripts/verify-extension.ps1` 完整通过。

## 2026-08-13 — SIGNAL FEED 已读操作保留图片节点

- SIGNAL FEED 的已读状态不会改变卡片集合或顺序，但此前仍随效率区和今日区调用 `renderSummaries()`；同时原位已读同步遗漏 `.summary-card`，导致已显示的缩略图进入无必要的卡片协调路径。
- 已读与取消已读现在直接更新现有 SIGNAL FEED 按钮、`.seen` 和 `.opened` 状态；活动区刷新只处理会因已读状态改变成员的效率区与今日区，不再重绘 SIGNAL FEED。操作回调改为读取实时已读集合，连续切换不依赖重新创建按钮。
- 本地安全合成预览加载 8 张 SIGNAL FEED 卡片。首张卡片连续标记与取消已读后，按钮标签正确往返，图片始终保持 `complete`、`640px` 自然宽度与同一地址，离场缩略图数量为 `0`；随后连续标记 3 张卡片，主图与 favicon 的地址和自然宽度均保持不变，离场缩略图仍为 `0`。未读取真实资讯缓存，未记录截图，也未执行加载已解压扩展后的手动 Chrome/Edge QA。
- 本次 activity-store 定向回归与相关模块语法检查通过，`node tests/extension.mjs` 和 `.\scripts\verify-extension.ps1` 也曾在本次修改后完整通过。随后无关的 `extension/core/web-search.mjs` 于 17:28 被并发改动，最终复跑在 Service Worker 静态图预算检查处失败：`656205` 字节，超过 `640 KiB` 上限 `845` 字节；本次未改动该后台模块。

## 2026-08-13 — 联网研究限流恢复与错误状态

- 修复 Brave 联网研究在“近期结果不足 → 立即扩大日期范围”时连续发起请求并触发每秒限流的问题。扩大检索前现在至少间隔 `1.05s`；若请求仍返回 `429`，只等待并重试一次。后一批失败时保留已经成功取得的首批证据，不再把整轮结果清空。
- 合法但不含 `web.results` 的 Brave 响应现在作为“无结果”处理，而不是错误 JSON。完全失败、缺少密钥、密钥无 Web Search 能力或缺少 Brave 来源权限会返回明确失败状态；对话界面据此显示“请求失败”和重试/设置操作，不再误标为“本地结果”。成功但没有证据时单独显示“未找到结果”。
- 合成回归覆盖天气类中文查询、首次 `429` 后成功、扩大检索失败但保留首批结果、`401` 设置恢复，以及无 `web` 结果的合法响应。没有读取真实密钥、真实 Brave 返回或私人收藏夹，也未执行加载已解压扩展后的手动 Chrome/Edge QA。

## 2026-08-13 对话联网与备用搜图解耦

- 修正了错误的产品与运行时耦合：对话中的“联网搜索”不再读取或校验 `braveSearchApiKey`，也不再请求 Brave Web Search。它现在调用当前 AI 服务商的原生托管搜索工具；已实现 OpenAI Responses `web_search`、OpenAI Chat Completions 的 `gpt-5-search-api` 路径，以及 Gemini Interactions `google_search`。不支持原生搜索的服务商或模型会在本轮回答中明确提示更换配置，不会把模型常识伪装成实时检索。
- 服务商返回的 URL 引用会被过滤为安全 HTTPS 来源、合并去重并映射为 Ampira 的 `[S1]` 引用与可展开来源。联网关闭时不会在 AI 请求中加入搜索工具；收藏夹单开仍不调用任何搜索引擎，双开则并行执行收藏夹读取与服务商原生搜索。
- Brave Search API 密钥继续保留为完全可选的“备用图片搜索”配置，只用于原图不可用时查找替代图片。关闭“全网图片搜索”后，密钥、授权和测试区域会立即折叠；该设置不再说明或控制对话联网。
- 自动测试新增 OpenAI 与 Gemini 原生搜索请求体、鉴权头、引用提取、危险来源过滤、无引用降级、不支持服务商提示、四种研究范围隔离及 Brave 独立性。隔离 Playwright Chromium 使用全新临时 Profile 加载当前扩展，确认“全网图片搜索”关闭时密钥、授权与测试区不可见，开启后展开，再次关闭后完整收起；控制台错误与警告均为 `0`，隐私安全截图位于 `output/playwright/provider-web-search/settings-fallback-image-collapsed.png`。本轮未使用真实 AI 密钥、真实书签或真实服务商请求，因此尚未执行真实 Provider 联网与 Edge 实机验证。
- `node tests/extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过；Service Worker 静态图为 `655,123` 字节，仍在 `640 KiB` 预算内。

## 2026-08-13 — 收藏夹推荐证据质量修复

- 修复收藏夹标题和 URL 被直接当成可引用研究证据的问题。未读取的书签现在只用于候选网站排序、精确来源授权和正文发现，不再进入回答、来源列表或获得 `S1…S12` 编号；只有读到至少 `40` 字符的有效摘要或 Reader 正文后才可参与比较、推荐和 AI 整理。
- 收藏的网站首页不再被 Reader 阶段排除。Feed 发现不到内容时，已授权的主页和文章型书签都可进入最多 3 页的正文读取；读取失败或未授权时不会再仅凭网站名称推断工具优劣、商品类型或购买价值。
- 收藏夹确有候选网站但本轮没有可核对内容时，回答会明确说明证据缺口，并引导授权或重试网站；宽泛推荐同时提示补充品类、预算、用途或判断标准。相关授权和继续检索操作仍使用既有界面与精确来源限制。
- 研究引用校验新增“裸证据编号”约束：`S1、S3 [S1][S3]` 一类重复表达会触发一次模型修复，仍不合规则回退到确定性证据列表。系统提示同步要求证据编号只出现在 `[S#]` 引用标记中。
- 合成回归覆盖未授权目录不暴露书签元数据、授权主页正文读取、Feed 与 Reader 都失败时的比较问题、运行中撤销权限、裸编号回答和提示注入。没有读取真实书签、真实 API 密钥或生产网站；本轮没有视觉结构变化，也未执行加载已解压扩展后的手动 Chrome/Edge QA。`node tests/extension.mjs` 通过，Service Worker 静态图为 `655,020` 字节，仍在 `640 KiB` 预算内。

## 2026-08-13 — 研究证据改为 Candidate → Document → Passage

- 收藏夹标题、URL、主页与按 origin 聚合的网站现在显式属于 `Candidate` 层，只负责来源排序、Feed 发现、Reader 选页和精确权限申请；它们不会获得 `S1…S12`，也不会进入模型输入。Feed 摘要和 Reader 正文先形成瞬时 `Document`，再按约 `680` 字符、`120` 字符上下文重叠切成稳定 `Passage`。Reader 不再固定截正文开头 `1,600` 字，而是从完整的安全提取文本中召回与当前问题相关的片段。
- 本地召回同时计算关键词相关性与时效排序，再以 RRF 合并；每个 Document 最多进入两条 Passage，避免同一网页淹没来源。`research-corpus-v1` 记录升级为内部 `version: 2`，只保存本轮最相关的少量 Passage、前后文、来源身份和时间元数据，不保存整篇 Document；旧版摘要缓存只会先转换为 Document 再切片，不会直接作为来源级证据复用，无需 IndexedDB schema 迁移。
- 新增确定性问题规划器，输出 `intent / standaloneQuery / subqueries / requiredEvidence / freshnessIntent`。比较题要求至少两个 Document 共享功能、价格或使用场景字段；“最近”要求可核对发布日期；“热门”还要求带数值的浏览、评分、评论、销量等指标，否则只允许称“值得关注”。“什么商品值得买”这类缺少品类、预算和用途的宽泛问题会在读取收藏夹或启用联网工具之前先追问。
- 回答提示明确只允许使用当前轮 Passage，引用不再要求每句话堆叠。点击行内 `[S1]` 会在同一回答内展开“参考来源”，把焦点移到对应 Passage，并显示其前后文；来源标题仍是打开原网页的独立入口。三语言无障碍标签已从“打开来源”改为“查看证据片段”。
- 隔离 Playwright Chromium 使用正式渲染模块和安全合成 Passage 验证：初始来源区域隐藏；点击 `[S1]` 后 `aria-expanded` 生效，焦点落在 `data-evidence-id="S1"`，高亮文本严格等于支持结论的片段，前后文同时可见，容器横向溢出为 `0`，控制台错误与警告均为 `0`。隐私安全截图位于 `output/playwright/research-search/passage-citation-context.png`；没有读取真实书签、密钥或生产网页。
- Chrome 官方说明扩展 Service Worker 不支持动态 `import()`。因此研究链路继续静态导入，并顺带把既有 AI 流式端口从动态导入修正为静态注册；架构测试新增全图动态导入禁令。新增证据层后启动静态图为 `684,285` 字节（`668.25 KiB`），预算按新增实际能力仅上调 `5%` 至 `672 KiB`，仍由自动测试硬限制。
- 完成实现后重新运行 `node tests\extension.mjs` 与 `.\scripts\verify-extension.ps1`，两者均通过；统一测试覆盖问题规划、Document 切片、Passage 召回、证据门槛、缓存迁移、权限撤销、提示注入、引用定位、三语言和 MV3 静态导入约束。本轮没有执行真实书签目录、真实 AI 服务商或真实 Brave 搜索的端到端 Chrome QA，这些仍是发布前的人工边界。

## 2026-08-13 — 对话联网与 Brave 图片搜索清除残留耦合

- 截图中的旧 Brave 密钥错误提示已不在当前源码或三语言目录中；隔离加载当前目录后也无法复现。该截图来自仍在运行的旧版扩展 Service Worker 或其尚未关闭的页面会话。当前联网开关只把 `webSearch: true` 交给已配置 AI 服务商的原生托管搜索能力。
- 删除研究响应、会话模型和研究操作区遗留的 `bravePermissionRequired`、`bravePermissionOrigin` 与“授权联网搜索”兼容路径。`research-search-service.mjs`、`ai-search-service.mjs`、`ai-search-ui.mjs` 和 `ai-research-view.mjs` 现在均不含 Brave 密钥、权限或图片搜索引用，自动测试会对这些文件做静态隔离检查。
- 图片搜索连接测试从对话服务拆到独立的 `image-search-settings-service.mjs`。Brave 密钥只由备用图片搜索设置和 Preview 缺图替换链路读取；对话研究服务不读取 `readSecrets`，也不接收图片搜索连接测试依赖。
- 设置文案统一为“备用图片搜索 / Brave 图片搜索 API 密钥 / 授权 Brave 图片搜索”，删除与对话联网作比较的帮助句；三语言、隐私页、商店文案和审核说明同步使用图片搜索语义。
- 隔离 Playwright Chromium 在无 AI 密钥、无真实书签和无 Brave 密钥的 Profile 中验证：主动打开“联网搜索”后提交“联网测试”，界面返回“AI 服务尚未完成设置或授权，无法执行原生联网搜索”，只提供“打开服务设置”，没有 Brave 提示或授权按钮；设置页展开备用图片搜索后显示新的图片专用文案。控制台错误与警告均为 `0`。
- `node tests\extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过；三语言键值、服务模块语法、MV3 静态依赖、对话/图片搜索隔离断言和现有图片回退测试全部通过。

## 2026-08-13 — 联网搜索按服务商能力显示

- 对话 composer 的“联网搜索”现在默认隐藏，只在后台确认当前官方服务商与模型支持原生托管搜索后显示。DeepSeek、OpenRouter、其他未知兼容服务和未知模型均按不支持处理，不会因为兼容 Responses 或 Chat Completions 协议而误显示。
- 能力判定继续集中在 AI 核心层：当前放行 Google Gemini 官方接口的 Gemini 模型、OpenAI 官方 Responses 接口的明确模型系列，以及 OpenAI 官方 Chat Completions 搜索专用模型。后台只向页面返回布尔能力，不暴露密钥；设置保存并重新加载 dashboard 后，已打开的 composer 也会同步显示状态。
- HTML 初始状态即隐藏联网按钮，避免 dashboard 尚未加载时闪现；如果服务商切换到不支持的模型，下一轮草稿中的联网状态会自动关闭，历史回答保存的实际范围不变。
- 合成 Provider 回归覆盖 DeepSeek Responses、未知 Responses 兼容服务、未知 OpenAI 模型和 Gemini embedding 模型的默认拒绝，以及受支持 OpenAI、Gemini 模型的放行。`node tests\extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。
- 安全静态 Playwright 在 `1280×800` 下验证：不支持状态的按钮属性为 `hidden`、计算显示为 `none`；支持状态显示为 `flex`。开启联网后切换到不支持状态会同时隐藏按钮并把 `aria-pressed` 恢复为 `false`。截图位于 `output/playwright/provider-web-search/unsupported-web-search-hidden.png` 和 `output/playwright/provider-web-search/supported-web-search-visible.png`。
- 静态预览没有连接扩展运行时，因此保留 `/api/settings`、`/api/settings/header-cover` 和 `/api/dashboard` 三条预期 `404`；本轮没有读取真实密钥、真实书签或调用真实 Provider，也未声称已完成加载解压扩展后的实机验证。

## 2026-08-13 — 未配置 AI 时隐藏研究范围

- 收藏夹与联网搜索现在共同以 AI 服务配置为前置条件。没有可用凭据或没有填写模型时，两项控件均隐藏；配置完成后先显示收藏夹，联网搜索仍需额外通过官方服务商与模型能力白名单。
- 两个控件在 HTML 初始状态均为隐藏，避免 dashboard 数据返回前闪现。配置被移除时，隐藏的收藏夹与联网草稿范围会同步清空，下一次普通提问不会意外沿用不可见范围；历史回答记录的实际范围保持不变。
- 安全静态 Playwright 在 `1280×800` 下合成验证三态：未配置时收藏夹增强 wrapper 与联网按钮均为 `display:none` 且研究范围无障碍标签为空；已配置但不支持联网时收藏夹为 `block`、联网为 `none`；已配置且支持时分别为 `block / flex`。未配置截图位于 `output/playwright/provider-web-search/unconfigured-research-controls-hidden.png`。
- `node tests\extension.mjs` 与 `.\scripts\verify-extension.ps1` 均通过。静态预览没有连接扩展运行时，因此仅保留三个预期 `/api/*` 404；未读取真实 AI 配置、密钥或书签，也未执行真实 Provider 请求。

## 2026-08-14 — 未配置 AI 的 composer 收紧

- 未配置 AI 时不再保留空的研究工具栏占位。composer 使用同一套 textarea 与发送按钮切换为单行纯输入态，紧凑面板最小高度由带范围工具时的 `108px` 收紧为 `64px`；发送按钮贴合输入框右下侧，不增加配置提示或替代标签。
- 配置 AI 后仍恢复原有第二行“收藏夹 / 联网 / 发送”布局；如果正在编辑旧问题，必要的编辑状态和取消按钮会临时恢复工具行，不会因纯输入态而失去操作入口。
- 安全静态 Playwright 使用正式页面与控制器验证：未配置状态的桌面面板为 `720×66px`，移动端为 `366×66px`；textarea 与发送按钮底线完全对齐、间距 `6px`，范围容器为 `display:none`，横向溢出为 `0`。展开态的 composer 保持 `760×61px`，同样没有错位或溢出。
- 合成切回已配置但不支持联网的状态后，composer 恢复 `108px` 高度并显示收藏夹、隐藏联网，证明纯输入态不会破坏原有范围工具栏。隐私安全截图位于 `output/playwright/ai-workbench/unconfigured-plain-composer-1280x800.png` 和 `unconfigured-plain-composer-390x844.png`；静态预览仅保留三个预期 `/api/*` 404，未读取真实配置、密钥或书签。
- 纯输入态发送按钮按输入行垂直中心对齐，不再与 textarea 底边对齐；Playwright 在桌面与 `390px` 窄屏实测两者中心差值均为 `0px`、水平间距为 `6px`，且没有横向溢出。带收藏夹与联网范围的工具栏布局保持原样。

## 2026-08-14 — AI 研究交互与时间扩展修复

- 补齐搜索工作台关闭按钮的点击绑定，按钮、Escape 与背景点击现在共用同一关闭流程。授权相关网站和“继续检索其余网站”改为原位刷新对应回答，不再重复追加相同用户问题或污染六轮上下文；刷新完成后会同步更新该轮上下文答案。
- 收藏夹列表刷新失败时不再把旧目录重新标记为读取成功。界面会清空无法确认的新列表并显示读取失败状态，重新打开组合框仍可重试。
- 服务商原生联网检索开始执行真实的分阶段扩展：普通问题先查近 31 天，证据不足再解除日期限制；“最近/最新”先查近 7 天，再扩大到 31 天，仍缺少足够且可核对的证据时才解除限制。显式日期范围始终只执行该范围，不自动放宽。回答覆盖信息记录是否发生扩展。
- 合成测试覆盖关闭绑定、研究操作原位替换、旧收藏夹错误状态、7 天到 31 天再到不限时间的三阶段请求，以及无可核对日期时继续扩大。没有读取真实书签、真实 AI 密钥或调用真实服务商，本轮未执行加载已解压扩展后的 Chrome/Edge 手动 QA。
